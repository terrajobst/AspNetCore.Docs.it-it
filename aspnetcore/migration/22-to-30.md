---
title: Eseguire la migrazione da ASP.NET Core 2.2 a 3.0 Preview
author: tdykstra
description: Informazioni su come eseguire la migrazione di un progetto ASP.NET Core 2.2 in ASP.NET Core 3.0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 13108732042244764d5c134771f701030630a260
ms.sourcegitcommit: 040aedca220ed24ee1726e6886daf6906f95a028
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 07/15/2019
ms.locfileid: "67892873"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="2a4ef-103">Eseguire la migrazione da ASP.NET Core 2.2 a 3.0</span><span class="sxs-lookup"><span data-stu-id="2a4ef-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="2a4ef-104">Dal [Scott Addie](https://github.com/scottaddie) e [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="2a4ef-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="2a4ef-105">Questo articolo illustra come aggiornare un progetto ASP.NET Core 2.2 esistente a ASP.NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="2a4ef-106">Prerequisiti</span><span class="sxs-lookup"><span data-stu-id="2a4ef-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="2a4ef-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="2a4ef-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="2a4ef-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="2a4ef-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="2a4ef-109">Visual Studio per Mac</span><span class="sxs-lookup"><span data-stu-id="2a4ef-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="2a4ef-110">Aggiornare il file di progetto</span><span class="sxs-lookup"><span data-stu-id="2a4ef-110">Update the project file</span></span>

* <span data-ttu-id="2a4ef-111">Impostare il [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) a `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="2a4ef-112">Rimuovere eventuali `<PackageReference>` per il [Microsoft. aspnetcore](xref:fundamentals/metapackage) oppure [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapacchetto.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="2a4ef-113">Aggiornamento di `Version` attributo rimanenti `<PackageReference>` elementi per `Microsoft.AspNetCore.*` pacchetti per l'anteprima corrente (ad esempio, `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="2a4ef-114">Se è presente alcuna versione 3.0 di un pacchetto, il pacchetto potrebbe essere stato deprecato nella versione 3.0.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="2a4ef-115">Molti di questi pacchetti sono parte di `Microsoft.AspNetCore.App` e non deve essere specificato singolarmente.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="2a4ef-116">Per un elenco preliminare dei pacchetti non più prodotti in 3.0, vedere [arrestare la produzione di pacchetti per gli assembly di framework condiviso nella versione 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="2a4ef-117">Il *framework condiviso* è il set di assembly (*DLL* file) che vengono installati nel computer e fa riferimento `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="2a4ef-118">Per altre informazioni, vedere [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) (Il framework condiviso).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="2a4ef-119">Gli assembly per vari componenti rilevanti sono stati rimossi da `Microsoft.AspNetCore.App` nella versione 3.0.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="2a4ef-120">Aggiungere `<PackageReference>` elementi, se si usano le API da pacchetti elencati nel [gli assembly da rimuovere da 3.0 Microsoft.AspNetCore.App (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="2a4ef-121">Esempi di componenti rimossi:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="2a4ef-122">L'elenco degli assembly di spedizione `Microsoft.AspNetCore.App` non è ancora stata finalizzata e cambierà prima 3.0 RTM.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="2a4ef-123">Esaminare il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="2a4ef-124">Il `ReadAsAsync` metodo chiamato nel codice precedente è incluso in `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="2a4ef-125">Installare il [ASPNET](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) pacchetto NuGet per risolvere il problema di compilazione nella versione 3.0.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="2a4ef-126">Aggiungere [Json.NET supporto](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="2a4ef-127">Per impostazione predefinita i progetti di [modello di hosting in-process](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 o versione successiva.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="2a4ef-128">È eventualmente possibile rimuovere il `<AspNetCoreHostingModel>` proprietà nel file di progetto se il valore è `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="2a4ef-129">Supporto di Json.NET</span><span class="sxs-lookup"><span data-stu-id="2a4ef-129">Json.NET support</span></span>

<span data-ttu-id="2a4ef-130">Come parte del lavoro [migliorare il framework di ASP.NET Core condiviso](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) è stato rimosso dal framework condiviso ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="2a4ef-131">Per usare Json.NET in un progetto ASP.NET Core 3.0:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="2a4ef-132">Aggiungere un riferimento al pacchetto [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="2a4ef-133">Update `Startup.ConfigureServices` chiamare `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="2a4ef-134">`AddNewtonsoftJson` è compatibile con i nuovi metodi di registrazione del servizio MVC:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-134">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="2a4ef-135">Json.NET impostazioni possono essere impostate nella chiamata a `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-135">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="2a4ef-136">Registrazione del servizio MVC</span><span class="sxs-lookup"><span data-stu-id="2a4ef-136">MVC service registration</span></span>

<span data-ttu-id="2a4ef-137">ASP.NET Core 3.0 aggiunge nuove opzioni per la registrazione di scenari MVC all'interno di `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-137">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="2a4ef-138">Tre nuovi metodi di estensione di primo livello relativa agli scenari di MVC su `IServiceCollection` sono disponibili.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-138">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="2a4ef-139">I modelli usano questi nuovi metodi anziché `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-139">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="2a4ef-140">Tuttavia, `AddMvc` continua a comportarsi come ha nelle versioni precedenti.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-140">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="2a4ef-141">Nell'esempio seguente aggiunge il supporto per controller e funzionalità API, ma non a viste o le pagine.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-141">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="2a4ef-142">Il modello API Usa questo codice:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-142">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="2a4ef-143">Nell'esempio seguente aggiunge il supporto per controller, funzionalità correlate a API e le viste, ma non le pagine.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-143">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="2a4ef-144">Il modello di applicazione Web (MVC) Usa questo codice:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-144">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="2a4ef-145">Nell'esempio seguente aggiunge il supporto per le pagine Razor e supporto del controller minimo.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-145">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="2a4ef-146">Il modello di applicazione Web Usa questo codice:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-146">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="2a4ef-147">I nuovi metodi possono anche essere combinati.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-147">The new methods can also be combined.</span></span> <span data-ttu-id="2a4ef-148">L'esempio seguente è equivalente alla chiamata `AddMvc` in ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-148">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="2a4ef-149">Aggiornare il codice di avvio routing</span><span class="sxs-lookup"><span data-stu-id="2a4ef-149">Update routing startup code</span></span>

<span data-ttu-id="2a4ef-150">Se un'app per le chiamate `UseMvc` oppure `UseSignalR`, eseguire la migrazione all'app di [Endpoint di Routing](xref:fundamentals/routing) se possibile.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-150">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="2a4ef-151">Per migliorare la compatibilità di Routing a Endpoint con le versioni precedenti di MVC, è stata ripristinata alcune delle modifiche nella generazione di URL introdotta in ASP.NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-151">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="2a4ef-152">Se si sono verificati problemi con il Routing Endpoint 2.2, prevedono miglioramenti nella versione 3.0 di ASP.NET Core con le eccezioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-152">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="2a4ef-153">Se l'app implementi `IRouter` o eredita da `Route`, è possibile evitare di eseguire la migrazione in questo momento.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-153">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="2a4ef-154">Fornire commenti e suggerimenti alla [intende eseguire la migrazione di implementazioni IRouter basati su Endpoint di Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-154">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="2a4ef-155">Se l'app accede direttamente a `RouteData.Routers` all'interno di MVC, è possibile evitare la migrazione in questo momento.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-155">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="2a4ef-156">Fornire commenti e suggerimenti alla [materiale sussidiario di migrazione per l'uso di RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-156">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="2a4ef-157">Routing a endpoint supporta la stessa sintassi di modello di route e il modello di route come funzionalità per la creazione `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-157">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="2a4ef-158">Endpoint Routing supporta `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-158">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="2a4ef-159">Routing supporta l'endpoint `[Route]`, `[HttpGet]`e gli altri attributi di routing MVC.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-159">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="2a4ef-160">Per la maggior parte delle applicazioni, solo `Startup` saranno necessarie modifiche.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-160">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="2a4ef-161">Eseguire la migrazione di Startup. Configure</span><span class="sxs-lookup"><span data-stu-id="2a4ef-161">Migrate Startup.Configure</span></span>

<span data-ttu-id="2a4ef-162">Consiglio generico:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-162">General advice:</span></span>

* <span data-ttu-id="2a4ef-163">Aggiungere `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-163">Add `UseRouting`.</span></span> 
* <span data-ttu-id="2a4ef-164">Se l'app chiama `UseStaticFiles`, inserire `UseStaticFiles` **prima** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-164">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="2a4ef-165">Se l'app Usa le funzionalità di autenticazione/autorizzazione, ad esempio `AuthorizePage` oppure `[Authorize]`, inserire la chiamata a `UseAuthentication` e `UseAuthorization` **dopo** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-165">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="2a4ef-166">Se l'app Usa [CORS](xref:security/cors) funzionalità, ad esempio `[EnableCors]`, posizionare `UseCors` successivo.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-166">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="2a4ef-167">Sostituire `UseMvc` oppure `UseSignalR` con `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="2a4ef-168">Di seguito è riportato un esempio di `Startup.Configure` in un'app ASP.NET Core 2.2 tipica:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-168">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="2a4ef-169">Dopo l'aggiornamento precedente `Startup.Configure` codice:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-169">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="2a4ef-170">Materiale sussidiario di middleware di sicurezza</span><span class="sxs-lookup"><span data-stu-id="2a4ef-170">Security middleware guidance</span></span>

<span data-ttu-id="2a4ef-171">Supporto per CORS e l'autorizzazione è unificato tutto il [middleware](xref:fundamentals/middleware/index) approccio.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-171">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="2a4ef-172">Ciò consente l'uso delle stesso middleware e funzionalità in questi scenari.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-172">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="2a4ef-173">In questa versione viene fornito un middleware autorizzazione aggiornato e Middleware CORS è stato migliorato in modo che sia in grado di utilizzare gli attributi utilizzati dai controller MVC.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-173">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="2a4ef-174">CORS</span><span class="sxs-lookup"><span data-stu-id="2a4ef-174">CORS</span></span>

<span data-ttu-id="2a4ef-175">In precedenza, CORS può essere difficile da configurare.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-175">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="2a4ef-176">Middleware è stato specificato per l'uso in alcuni casi d'uso, ma filtri MVC sono stati possono essere utilizzati **senza** il middleware in altri casi d'uso.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-176">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="2a4ef-177">Con ASP.NET Core 3.0, è consigliabile che tutte le app che richiedono CORS usare il Middleware CORS in abbinamento con l'Endpoint di Routing.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-177">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="2a4ef-178">`UseCors` può essere fornito con un criterio predefinito, e `[EnableCors]` e `[DisableCors]` attributi possono essere utilizzati per sostituire i criteri predefiniti ove necessario.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-178">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="2a4ef-179">Nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-179">In the following example:</span></span>

* <span data-ttu-id="2a4ef-180">CORS è abilitato per tutti gli endpoint con il `default` denominato criterio.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-180">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="2a4ef-181">Il `MyController` classe Disabilita CORS con la `[DisableCors]` attributo.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-181">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="2a4ef-182">Authorization</span><span class="sxs-lookup"><span data-stu-id="2a4ef-182">Authorization</span></span>

<span data-ttu-id="2a4ef-183">Nelle versioni precedenti di ASP.NET Core, supporto per l'autorizzazione è stato fornito tramite il `[Authorize]` attributo.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-183">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="2a4ef-184">Middleware di autorizzazione non era disponibile.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-184">Authorization middleware wasn't available.</span></span> <span data-ttu-id="2a4ef-185">In ASP.NET Core 3.0, il middleware di autorizzazione è necessario.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-185">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="2a4ef-186">È consigliabile posizionare il Middleware di autorizzazione di ASP.NET Core (`UseAuthorization`) subito dopo `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-186">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="2a4ef-187">Il Middleware di autorizzazione può essere configurato anche con un criterio predefinito, che può essere sottoposto a override.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-187">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="2a4ef-188">In ASP.NET Core 3.0 o versione successiva `UseAuthorization` viene chiamato nel `Startup.Configure`e le seguenti `HomeController` richiede un oggetto firmato nell'utente:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-188">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="2a4ef-189">Se l'app Usa un' `AuthorizeFilter` come filtro globale in MVC, si consiglia di refactoring del codice per fornire un criterio per il `UseAuthorization` middleware.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-189">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="2a4ef-190">Nell'esempio seguente, un criterio personalizzato da applicare a tutte le richieste quando `UseAuthorization` viene chiamato e il `HomeController` consente l'accesso senza che l'utente l'accesso nell'app:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-190">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="2a4ef-191">L'autorizzazione dal middleware funziona senza il framework abbiano alcuna consapevolezza specifica di autorizzazione.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-191">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="2a4ef-192">Ad esempio, [controlli di integrità](xref:host-and-deploy/health-checks) non dispone di alcuna conoscenza diretta di autorizzazione, ma i controlli di integrità possono avere un criterio di autorizzazione configurabile dal middleware.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-192">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="2a4ef-193">Nell'esempio seguente `UseAuthorization` elabora l'autorizzazione senza un criterio predefinito, ma la `/healthz` endpoint di controllo di integrità richiede che l'utente sia nel `admin` ruolo:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-193">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="2a4ef-194">Protezione dati viene implementato per alcuni scenari.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-194">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="2a4ef-195">`UseEndpoint` middleware genera un'eccezione se un'autorizzazione o un criterio CORS viene ignorata a causa di un middleware mancano.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-195">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="2a4ef-196">Supporto di analizzatore per fornire commenti e suggerimenti aggiuntivi sull'errore di configurazione sono in corso.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-196">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="2a4ef-197">Eseguire la migrazione di SignalR</span><span class="sxs-lookup"><span data-stu-id="2a4ef-197">Migrate SignalR</span></span>

<span data-ttu-id="2a4ef-198">Mapping degli hub SignalR ora ha luogo all'interno `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-198">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="2a4ef-199">Eseguire il mapping di ogni hub con `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-199">Map each hub with `MapHub`.</span></span> <span data-ttu-id="2a4ef-200">Come nelle versioni precedenti, ogni hub è elencato in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-200">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="2a4ef-201">Nell'esempio seguente, il supporto per il `ChatHub` hub SignalR viene aggiunto:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-201">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="2a4ef-202">Eseguire la migrazione di controller MVC</span><span class="sxs-lookup"><span data-stu-id="2a4ef-202">Migrate MVC controllers</span></span>

<span data-ttu-id="2a4ef-203">Mapping dei controller ora ha luogo all'interno `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-203">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="2a4ef-204">Aggiungere `MapControllers` se l'app Usa il routing con attributi.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-204">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="2a4ef-205">Poiché routing include il supporto per molti framework di ASP.NET Core 3.0 o versione successiva, l'aggiunta di attributi di routing controller è fornire il consenso esplicito.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-205">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="2a4ef-206">Sostituire gli elementi seguenti:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-206">Replace the following:</span></span>

* <span data-ttu-id="2a4ef-207">`MapRoute` con `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="2a4ef-207">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="2a4ef-208">`MapAreaRoute` con `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="2a4ef-208">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="2a4ef-209">Poiché routing ora include il supporto per più di MVC, la terminologia è stato modificato affinché chiaramente indicato cosa fanno questi metodi.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-209">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="2a4ef-210">Le route convenzionali, ad esempio `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` vengono applicate nell'ordine che vengono aggiunti.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-210">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="2a4ef-211">Inserisci prima le route più specifiche (ad esempio, le route per un'area).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-211">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="2a4ef-212">Nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-212">In the following example:</span></span>

* <span data-ttu-id="2a4ef-213">`MapControllers` Aggiunge il supporto per i controller di attributi di routing.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-213">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="2a4ef-214">`MapAreaControllerRoute` Aggiunge una route convenzionale per i controller in un'area.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-214">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="2a4ef-215">`MapControllerRoute` Aggiunge una route convenzionale per i controller.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-215">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="2a4ef-216">Eseguire la migrazione di pagine Razor</span><span class="sxs-lookup"><span data-stu-id="2a4ef-216">Migrate Razor Pages</span></span>

<span data-ttu-id="2a4ef-217">Mapping di Razor Pages ora ha luogo all'interno `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-217">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="2a4ef-218">Aggiungere `MapRazorPages` se l'app Usa le pagine Razor.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-218">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="2a4ef-219">Poiché l'Endpoint di Routing include il supporto per diversi Framework, l'aggiunta di che Razor Pages è a questo punto fornire il consenso esplicito.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-219">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="2a4ef-220">Nell'esempio seguente, `MapRazorPages` aggiunge il supporto per le pagine Razor:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-220">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="2a4ef-221">Usare MVC senza l'Endpoint di Routing</span><span class="sxs-lookup"><span data-stu-id="2a4ef-221">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="2a4ef-222">Usa MVC tramite `UseMvc` oppure `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 richiede l'impostazione esplicita acconsentire esplicitamente all'interno di `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-222">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="2a4ef-223">Ciò è necessario perché MVC devono sapere che è possibile fare affidamento su richieste di autorizzazione e Middleware CORS durante l'inizializzazione.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-223">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="2a4ef-224">Un analizzatore viene specificato che visualizza un avviso se l'app prova a usare una configurazione non supportata.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-224">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="2a4ef-225">Se l'app richiede legacy `IRouter` supportare, disabilitare `EnableEndpointRouting` usando uno dei seguenti approcci `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-225">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="2a4ef-226">Eseguire la migrazione di controlli di integrità</span><span class="sxs-lookup"><span data-stu-id="2a4ef-226">Migrate health checks</span></span>

<span data-ttu-id="2a4ef-227">Controlli di integrità possono essere utilizzati come un *router-ware* con Endpoint di Routing.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-227">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="2a4ef-228">Aggiungere `MapHealthChecks` usare integrità verifica con l'Endpoint di Routing.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-228">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="2a4ef-229">Il `MapHealthChecks` metodo accetta argomenti simili a `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-229">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="2a4ef-230">Il vantaggio dell'utilizzo `MapHealthChecks` failover `UseHealthChecks` è la possibilità di applicare l'autorizzazione e per avere maggiore controllo con granularità fine tramite i criteri di corrispondenza.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-230">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="2a4ef-231">Nell'esempio riportato di seguito `MapHealthChecks` viene chiamato per un endpoint di controllo di integrità in `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-231">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="2a4ef-232">HostBuilder sostituisce WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="2a4ef-232">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="2a4ef-233">Usano i modelli ASP.NET Core 3.0 [Host generico](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-233">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="2a4ef-234">Le versioni precedenti utilizzate [Web Host](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-234">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="2a4ef-235">Il codice seguente illustra il modello di ASP.NET Core 3.0 generato `Program` classe:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-235">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="2a4ef-236">Il codice seguente illustra la 2.2 di ASP.NET Core generati da un modello `Program` classe:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-236">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="2a4ef-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> rimane nella versione 3.0 ed è il tipo del `webBuilder` illustrato nell'esempio di codice precedente.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="2a4ef-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> sarà deprecata in una versione futura e sostituito da `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="2a4ef-239">Il cambiamento più significativo dal `WebHostBuilder` a `HostBuilder` le novità [inserimento (dipendenze)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="2a4ef-239">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="2a4ef-240">Quando si usa `HostBuilder`, è possibile solo inserire <xref:Microsoft.Extensions.Configuration.IConfiguration> e <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> in `Startup`del costruttore.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-240">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="2a4ef-241">Il `HostBuilder` vincoli l'inserimento delle dipendenze:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-241">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="2a4ef-242">Abilitare il contenitore di inserimento delle dipendenze per la compilazione solo una volta.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-242">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="2a4ef-243">Consente di evitare i problemi di durata degli oggetti risultante, ad esempio la risoluzione di più istanze di singleton.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-243">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="2a4ef-244">Aggiornare il codice di SignalR</span><span class="sxs-lookup"><span data-stu-id="2a4ef-244">Update SignalR code</span></span>

<span data-ttu-id="2a4ef-245">`System.Text.Json` è ora il valore predefinito dell'Hub protocollo usato dal client e server.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-245">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="2a4ef-246">Nelle `Startup.ConfigureServices`, chiamare `AddJsonProtocol` per impostare opzioni del serializzatore.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-246">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="2a4ef-247">**Server:**</span><span class="sxs-lookup"><span data-stu-id="2a4ef-247">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="2a4ef-248">**Client:**</span><span class="sxs-lookup"><span data-stu-id="2a4ef-248">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="2a4ef-249">Passare a newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="2a4ef-249">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="2a4ef-250">Se si usano funzionalità del `Newtonsoft.Json` che non sono supportati in `System.Text.Json`, è possibile tornare alla `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-250">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="2a4ef-251">Installare il [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) pacchetto NuGet.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-251">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="2a4ef-252">Sul client, concatenare un' `AddNewtonsoftJsonProtocol` chiamata al metodo il `HubConnectionBuilder` istanza:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-252">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="2a4ef-253">Nel server, concatenare un' `AddNewtonsoftJsonProtocol` chiamata al metodo per il `AddSignalR` chiamata al metodo `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="2a4ef-253">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="2a4ef-254">Fornire il consenso esplicito per la compilazione di runtime</span><span class="sxs-lookup"><span data-stu-id="2a4ef-254">Opt in to runtime compilation</span></span>

<span data-ttu-id="2a4ef-255">In 3.0, la compilazione di runtime è uno scenario di consenso esplicito.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-255">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="2a4ef-256">Per abilitare la compilazione di runtime, vedere https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span><span class="sxs-lookup"><span data-stu-id="2a4ef-256">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>
