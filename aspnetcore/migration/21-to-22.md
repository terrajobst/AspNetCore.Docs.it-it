---
title: Eseguire la migrazione da ASP.NET Core 2,1 a 2,2
author: scottaddie
description: Questo articolo descrive i prerequisiti e i passaggi più comuni per la migrazione di un progetto di ASP.NET Core 2,1 ASP.NET Core 2,2.
ms.author: scaddie
ms.custom: mvc
ms.date: 05/28/2019
uid: migration/21-to-22
ms.openlocfilehash: ac87d97a259c230c01d236e84d6e42b28adad7e0
ms.sourcegitcommit: 9a129f5f3e31cc449742b164d5004894bfca90aa
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78667279"
---
# <a name="migrate-from-aspnet-core-21-to-22"></a><span data-ttu-id="120be-103">Eseguire la migrazione da ASP.NET Core 2,1 a 2,2</span><span class="sxs-lookup"><span data-stu-id="120be-103">Migrate from ASP.NET Core 2.1 to 2.2</span></span>

<span data-ttu-id="120be-104">Di [Scott Addie](https://github.com/scottaddie)</span><span class="sxs-lookup"><span data-stu-id="120be-104">By [Scott Addie](https://github.com/scottaddie)</span></span>

<span data-ttu-id="120be-105">Questo articolo illustra come aggiornare un progetto di ASP.NET Core 2,1 esistente a ASP.NET Core 2,2.</span><span class="sxs-lookup"><span data-stu-id="120be-105">This article explains how to update an existing ASP.NET Core 2.1 project to ASP.NET Core 2.2.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="120be-106">Prerequisites</span><span class="sxs-lookup"><span data-stu-id="120be-106">Prerequisites</span></span>

# <a name="visual-studio"></a>[<span data-ttu-id="120be-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="120be-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs2019-2.2.md)]

# <a name="visual-studio-code"></a>[<span data-ttu-id="120be-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="120be-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-2.2.md)]

# <a name="visual-studio-for-mac"></a>[<span data-ttu-id="120be-109">Visual Studio per Mac</span><span class="sxs-lookup"><span data-stu-id="120be-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-2.2.md)]

---

## <a name="update-target-framework-moniker-tfm"></a><span data-ttu-id="120be-110">Aggiornare Moniker della versione di .NET Framework di destinazione (TFM, Target Framework Moniker)</span><span class="sxs-lookup"><span data-stu-id="120be-110">Update Target Framework Moniker (TFM)</span></span>

<span data-ttu-id="120be-111">I progetti destinati a .NET Core devono usare il [TFM](/dotnet/standard/frameworks) di una versione maggiore o uguale a .net core 2,2.</span><span class="sxs-lookup"><span data-stu-id="120be-111">Projects targeting .NET Core should use the [TFM](/dotnet/standard/frameworks) of a version greater than or equal to .NET Core 2.2.</span></span> <span data-ttu-id="120be-112">Nel file di progetto aggiornare il testo interno del nodo `<TargetFramework>` con `netcoreapp2.2`:</span><span class="sxs-lookup"><span data-stu-id="120be-112">In the project file, update the `<TargetFramework>` node's inner text with `netcoreapp2.2`:</span></span>

```xml
<TargetFramework>netcoreapp2.2</TargetFramework>
```

<span data-ttu-id="120be-113">I progetti destinati a .NET Framework possono continuare a usare il TFM di una versione maggiore o uguale .NET Framework 4.6.1:</span><span class="sxs-lookup"><span data-stu-id="120be-113">Projects targeting .NET Framework may continue to use the TFM of a version greater than or equal to .NET Framework 4.6.1:</span></span>

```xml
<TargetFramework>net461</TargetFramework>
```

## <a name="adopt-the-iis-in-process-hosting-model"></a><span data-ttu-id="120be-114">Adottare il modello di hosting IIS in-process</span><span class="sxs-lookup"><span data-stu-id="120be-114">Adopt the IIS in-process hosting model</span></span>

<span data-ttu-id="120be-115">Per adottare il [modello di hosting in-process per IIS](xref:host-and-deploy/iis/index#in-process-hosting-model), aggiungere la proprietà `<AspNetCoreHostingModel>` con un valore di `InProcess` a un `<PropertyGroup>` nel file di progetto:</span><span class="sxs-lookup"><span data-stu-id="120be-115">To adopt the [in-process hosting model for IIS](xref:host-and-deploy/iis/index#in-process-hosting-model), add the `<AspNetCoreHostingModel>` property with a value of `InProcess` to a `<PropertyGroup>` in the project file:</span></span>

```xml
<AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
```

<span data-ttu-id="120be-116">Il modello di hosting in-process non è supportato per le app ASP.NET Core destinate a .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="120be-116">The in-process hosting model isn't supported for ASP.NET Core apps targeting .NET Framework.</span></span>

<span data-ttu-id="120be-117">Per altre informazioni, vedere <xref:host-and-deploy/aspnet-core-module#hosting-models>.</span><span class="sxs-lookup"><span data-stu-id="120be-117">For more information, see <xref:host-and-deploy/aspnet-core-module#hosting-models>.</span></span>

## <a name="update-a-custom-webconfig-file"></a><span data-ttu-id="120be-118">Aggiornare un file Web. config personalizzato</span><span class="sxs-lookup"><span data-stu-id="120be-118">Update a custom web.config file</span></span>

<span data-ttu-id="120be-119">Per i progetti che utilizzano un file *Web. config* personalizzato nella radice del progetto per generare il file *Web. config* pubblicato:</span><span class="sxs-lookup"><span data-stu-id="120be-119">For projects that use a custom *web.config* file in the project root to generate their published *web.config* file:</span></span>

* <span data-ttu-id="120be-120">Nella voce `<handlers>` che aggiunge il modulo di ASP.NET Core (`name="aspNetCore"`), modificare il valore dell'attributo `modules` da `AspNetCoreModule` a `AspNetCoreModuleV2`.</span><span class="sxs-lookup"><span data-stu-id="120be-120">In the `<handlers>` entry that adds the ASP.NET Core Module (`name="aspNetCore"`), change the `modules` attribute value from `AspNetCoreModule` to `AspNetCoreModuleV2`.</span></span>
* <span data-ttu-id="120be-121">Nell'elemento `<aspNetCore>` aggiungere l'attributo del modello di hosting (`hostingModel="InProcess"`).</span><span class="sxs-lookup"><span data-stu-id="120be-121">In the `<aspNetCore>` element, add the hosting model attribute (`hostingModel="InProcess"`).</span></span>

<span data-ttu-id="120be-122">Per ulteriori informazioni e per i file *Web. config* di esempio, vedere <xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>.</span><span class="sxs-lookup"><span data-stu-id="120be-122">For more information and example *web.config* files, see <xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>.</span></span>

## <a name="update-package-references"></a><span data-ttu-id="120be-123">Aggiornare i riferimenti del pacchetto</span><span class="sxs-lookup"><span data-stu-id="120be-123">Update package references</span></span>

<span data-ttu-id="120be-124">Se la destinazione è .NET Core, rimuovere l'attributo `Version` del riferimento al metapacchetto nel file di progetto.</span><span class="sxs-lookup"><span data-stu-id="120be-124">If targeting .NET Core, remove the metapackage reference's `Version` attribute in the project file.</span></span> <span data-ttu-id="120be-125">L'inclusione di un attributo `Version` genera l'avviso seguente:</span><span class="sxs-lookup"><span data-stu-id="120be-125">Inclusion of a `Version` attribute results in the following warning:</span></span>

```console
A PackageReference to 'Microsoft.AspNetCore.App' specified a Version of `2.2.0`. Specifying the version of this package is not recommended. For more information, see https://aka.ms/sdkimplicitrefs
```

<span data-ttu-id="120be-126">Per altre informazioni, vedere <xref:fundamentals/metapackage-app>.</span><span class="sxs-lookup"><span data-stu-id="120be-126">For more information, see <xref:fundamentals/metapackage-app>.</span></span>

<span data-ttu-id="120be-127">Il riferimento al metapacchetto dovrebbe essere simile al seguente `<PackageReference />` nodo:</span><span class="sxs-lookup"><span data-stu-id="120be-127">The metapackage reference should resemble the following `<PackageReference />` node:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore.App" />
</ItemGroup>
```

<span data-ttu-id="120be-128">Se la destinazione è .NET Framework, aggiornare l'attributo `Version` di ogni riferimento a un pacchetto in 2.2.0 o versione successiva.</span><span class="sxs-lookup"><span data-stu-id="120be-128">If targeting .NET Framework, update each package reference's `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="120be-129">Di seguito sono riportati i riferimenti ai pacchetti in un progetto di ASP.NET Core 2,2 tipico destinato .NET Framework:</span><span class="sxs-lookup"><span data-stu-id="120be-129">Here are the package references in a typical ASP.NET Core 2.2 project targeting .NET Framework:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.CookiePolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.HttpsPolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.StaticFiles" Version="2.2.0" />
</ItemGroup>
```

<span data-ttu-id="120be-130">Se si fa riferimento al pacchetto [Microsoft. AspNetCore. Razor. Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) , aggiornare il relativo attributo `Version` a 2.2.0 o versione successiva.</span><span class="sxs-lookup"><span data-stu-id="120be-130">If referencing the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package, update its `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="120be-131">In caso contrario, viene restituito l'errore seguente:</span><span class="sxs-lookup"><span data-stu-id="120be-131">Failure to do so results in the following error:</span></span>

```console
Detected package downgrade: Microsoft.AspNetCore.Razor.Design from 2.2.0 to 2.1.2. Reference the package directly from the project to select a different version.
```

## <a name="update-net-core-sdk-version-in-globaljson"></a><span data-ttu-id="120be-132">Aggiornare la versione di .NET Core SDK in global.json</span><span class="sxs-lookup"><span data-stu-id="120be-132">Update .NET Core SDK version in global.json</span></span>

<span data-ttu-id="120be-133">Se la soluzione si basa su un file [Global. JSON](/dotnet/core/tools/global-json) per fare riferimento a una versione specifica di .NET Core SDK, aggiornare la relativa proprietà `version` alla versione 2,2 installata nel computer:</span><span class="sxs-lookup"><span data-stu-id="120be-133">If your solution relies upon a [global.json](/dotnet/core/tools/global-json) file to target a specific .NET Core SDK version, update its `version` property to the 2.2 version installed on your machine:</span></span>

```json
{
  "sdk": {
    "version": "2.2.100"
  }
}
```

## <a name="update-launch-settings"></a><span data-ttu-id="120be-134">Aggiorna impostazioni di avvio</span><span class="sxs-lookup"><span data-stu-id="120be-134">Update launch settings</span></span>

<span data-ttu-id="120be-135">Se si usa Visual Studio Code, aggiornare il file delle impostazioni di avvio del progetto ( *. VSCODE/Launch. JSON*).</span><span class="sxs-lookup"><span data-stu-id="120be-135">If using Visual Studio Code, update the project's launch settings file (*.vscode/launch.json*).</span></span> <span data-ttu-id="120be-136">Il percorso di `program` deve fare riferimento al nuovo TFM:</span><span class="sxs-lookup"><span data-stu-id="120be-136">The `program` path should reference the new TFM:</span></span>

[!code-json[](21-to-22/samples/launch.json?highlight=9)]

## <a name="update-kestrel-configuration"></a><span data-ttu-id="120be-137">Aggiornare la configurazione di gheppio</span><span class="sxs-lookup"><span data-stu-id="120be-137">Update Kestrel configuration</span></span>

<span data-ttu-id="120be-138">Se l'app chiama <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> chiamando `CreateDefaultBuilder` nel [Metodo CreateWebHostBuilder](xref:fundamentals/host/web-host#set-up-a-host) della classe `Program`, chiamare `ConfigureKestrel` per configurare il server gheppio anziché `UseKestrel` per evitare conflitti con il [modello di hosting in-process di IIS](xref:host-and-deploy/iis/index#in-process-hosting-model):</span><span class="sxs-lookup"><span data-stu-id="120be-138">If the app calls <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> by calling `CreateDefaultBuilder` in the [CreateWebHostBuilder method](xref:fundamentals/host/web-host#set-up-a-host) of the `Program` class, call `ConfigureKestrel` to configure Kestrel server instead of `UseKestrel` in order to avoid conflicts with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model):</span></span>

```csharp
public static IWebHostBuilder CreateWebHostBuilder(string[] args) =>
    WebHost.CreateDefaultBuilder(args)
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        });
```

<span data-ttu-id="120be-139">Se l'app non chiama `CreateDefaultBuilder` e compila manualmente l'host nella classe `Program`, chiamare <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> **prima** di chiamare `ConfigureKestrel`:</span><span class="sxs-lookup"><span data-stu-id="120be-139">If the app doesn't call `CreateDefaultBuilder` and builds the host manually in the `Program` class, call <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> **before** calling `ConfigureKestrel`:</span></span>

```csharp
public static void Main(string[] args)
{
    var host = new WebHostBuilder()
        .UseContentRoot(Directory.GetCurrentDirectory())
        .UseKestrel()
        .UseIISIntegration()
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        })
        .Build();

    host.Run();
}
```

<span data-ttu-id="120be-140">Per altre informazioni, vedere <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span><span class="sxs-lookup"><span data-stu-id="120be-140">For more information, see <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span></span>

## <a name="update-compatibility-version"></a><span data-ttu-id="120be-141">Aggiorna versione compatibilità</span><span class="sxs-lookup"><span data-stu-id="120be-141">Update compatibility version</span></span>

<span data-ttu-id="120be-142">Aggiornare la versione di compatibilità in `Startup.ConfigureServices` `Version_2_2`:</span><span class="sxs-lookup"><span data-stu-id="120be-142">Update the compatibility version in `Startup.ConfigureServices` to `Version_2_2`:</span></span>

```csharp
services.AddMvc()
        .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

## <a name="update-cors-policy"></a><span data-ttu-id="120be-143">Aggiornare i criteri di CORS</span><span class="sxs-lookup"><span data-stu-id="120be-143">Update CORS policy</span></span>

<span data-ttu-id="120be-144">In ASP.NET Core 2,2, il middleware CORS risponde con un'origine con carattere jolly (`*`) se un criterio consente qualsiasi origine e consente le credenziali.</span><span class="sxs-lookup"><span data-stu-id="120be-144">In ASP.NET Core 2.2, the CORS middleware responds with a wildcard origin (`*`) if a policy allows any origin and allows credentials.</span></span> <span data-ttu-id="120be-145">Le credenziali non sono supportate quando si specifica un'origine con carattere jolly (`*`) e i browser non consentono la richiesta CORS.</span><span class="sxs-lookup"><span data-stu-id="120be-145">Credentials aren't supported when a wildcard origin (`*`) is specified, and browsers will disallow the CORS request.</span></span> <span data-ttu-id="120be-146">Per ulteriori informazioni, incluse le opzioni per la correzione del problema nel client, vedere la [documentazione Web MDN](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span><span class="sxs-lookup"><span data-stu-id="120be-146">For more information, including options for correcting the problem on the client, see the [MDN web docs](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span></span>

<span data-ttu-id="120be-147">Per risolvere il problema nel server, eseguire una delle operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="120be-147">To correct this problem on the server, take one of the following actions:</span></span>

* <span data-ttu-id="120be-148">Modificare il criterio CORS in modo da non consentire più le credenziali.</span><span class="sxs-lookup"><span data-stu-id="120be-148">Modify the CORS policy to no longer allow credentials.</span></span> <span data-ttu-id="120be-149">Ovvero rimuovere la chiamata a <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> quando si configurano i criteri.</span><span class="sxs-lookup"><span data-stu-id="120be-149">That is, remove the call to <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> when configuring the policy.</span></span>
* <span data-ttu-id="120be-150">Se le credenziali sono necessarie per la riuscita della richiesta CORS, modificare il criterio per specificare gli host consentiti.</span><span class="sxs-lookup"><span data-stu-id="120be-150">If credentials are required for the CORS request to succeed, modify the policy to specify allowed hosts.</span></span> <span data-ttu-id="120be-151">Usare, ad esempio, `builder.WithOrigins("https://api.example1.com", "https://example2.com")` invece di usare <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span><span class="sxs-lookup"><span data-stu-id="120be-151">For example, use `builder.WithOrigins("https://api.example1.com", "https://example2.com")` instead of using <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span></span>

## <a name="update-docker-images"></a><span data-ttu-id="120be-152">Aggiornare le immagini Docker</span><span class="sxs-lookup"><span data-stu-id="120be-152">Update Docker images</span></span>

<span data-ttu-id="120be-153">La tabella seguente illustra le modifiche apportate al tag dell'immagine docker:</span><span class="sxs-lookup"><span data-stu-id="120be-153">The following table shows the Docker image tag changes:</span></span>

| <span data-ttu-id="120be-154">2.1</span><span class="sxs-lookup"><span data-stu-id="120be-154">2.1</span></span>                                       | <span data-ttu-id="120be-155">2.2</span><span class="sxs-lookup"><span data-stu-id="120be-155">2.2</span></span>                                        |
| ----------------------------------------- | -----------------------------------------  |
| `microsoft/dotnet:2.1-aspnetcore-runtime` | `mcr.microsoft.com/dotnet/core/aspnet:2.2` |
| `microsoft/dotnet:2.1-sdk`                | `mcr.microsoft.com/dotnet/core/sdk:2.2`    |

<span data-ttu-id="120be-156">Modificare le righe `FROM` nel *Dockerfile* per usare i nuovi tag di immagine nella colonna 2,2 della tabella precedente.</span><span class="sxs-lookup"><span data-stu-id="120be-156">Change the `FROM` lines in your *Dockerfile* to use the new image tags in the preceding table's 2.2 column.</span></span>

## <a name="build-manually-in-visual-studio-when-using-iis-in-process-hosting"></a><span data-ttu-id="120be-157">Compilare manualmente in Visual Studio quando si usa l'hosting in-process di IIS</span><span class="sxs-lookup"><span data-stu-id="120be-157">Build manually in Visual Studio when using IIS in-process hosting</span></span>

<span data-ttu-id="120be-158">La **compilazione automatica** di Visual Studio sull'esperienza di richiesta del browser non funziona con il [modello di hosting in-process di IIS](xref:host-and-deploy/iis/index#in-process-hosting-model).</span><span class="sxs-lookup"><span data-stu-id="120be-158">Visual Studio's **Auto build on browser request** experience doesn't function with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model).</span></span> <span data-ttu-id="120be-159">Quando si utilizza l'hosting in-process, è necessario ricompilare manualmente il progetto.</span><span class="sxs-lookup"><span data-stu-id="120be-159">You must manually rebuild the project when using in-process hosting.</span></span> <span data-ttu-id="120be-160">I miglioramenti apportati a questa esperienza sono pianificati per una versione futura di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="120be-160">Improvements to this experience are planned for a future release of Visual Studio.</span></span>

## <a name="update-logging-code"></a><span data-ttu-id="120be-161">Aggiornare il codice di registrazione</span><span class="sxs-lookup"><span data-stu-id="120be-161">Update logging code</span></span>

<span data-ttu-id="120be-162">Il codice di configurazione della registrazione consigliato non è stato modificato da 2,1 a 2,2, ma alcuni modelli di codifica 1. x ancora funzionanti in 2,1 non funzionano più in 2,2.</span><span class="sxs-lookup"><span data-stu-id="120be-162">Recommended logging configuration code didn't change from 2.1 to 2.2, but some 1.x coding patterns that still worked in 2.1 no longer work in 2.2.</span></span>

<span data-ttu-id="120be-163">Se l'app esegue la registrazione dell'inizializzazione, del filtraggio e del caricamento della configurazione del provider nella classe `Startup`, spostare il codice in `Program.Main`:</span><span class="sxs-lookup"><span data-stu-id="120be-163">If your app does logging provider initialization, filtering, and configuration loading in the `Startup` class, move that code to `Program.Main`:</span></span>

* <span data-ttu-id="120be-164">Inizializzazione del provider:</span><span class="sxs-lookup"><span data-stu-id="120be-164">Provider initialization:</span></span>

  <span data-ttu-id="120be-165">esempio 1. x:</span><span class="sxs-lookup"><span data-stu-id="120be-165">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole();
  }
  ```

  <span data-ttu-id="120be-166">2,2 esempio:</span><span class="sxs-lookup"><span data-stu-id="120be-166">2.2 example:</span></span>

  ```csharp

  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole();
          })
          // ...
  }
  ```

* <span data-ttu-id="120be-167">Filtri:</span><span class="sxs-lookup"><span data-stu-id="120be-167">Filtering:</span></span>

  <span data-ttu-id="120be-168">esempio 1. x:</span><span class="sxs-lookup"><span data-stu-id="120be-168">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(LogLevel.Information);
      // or
      loggerFactory.AddConsole((category, level) => 
          category == "A" || level == LogLevel.Critical);
  }
  ```

  <span data-ttu-id="120be-169">2,2 esempio:</span><span class="sxs-lookup"><span data-stu-id="120be-169">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole()
                     .AddFilter<ConsoleLoggerProvider>
                         (category: null, level: LogLevel.Information)
                     // or
                     .AddFilter<ConsoleLoggerProvider>
                         ((category, level) => category == "A" ||
                             level == LogLevel.Critical)
              );
          })
          // ...
  }
  ```

* <span data-ttu-id="120be-170">Caricamento della configurazione:</span><span class="sxs-lookup"><span data-stu-id="120be-170">Configuration loading:</span></span>

  <span data-ttu-id="120be-171">esempio 1. x:</span><span class="sxs-lookup"><span data-stu-id="120be-171">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(Configuration);
  }
  ```

  <span data-ttu-id="120be-172">2,2 esempio:</span><span class="sxs-lookup"><span data-stu-id="120be-172">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConfiguration(hostingContext.Configuration.GetSection("Logging"));
              logging.AddConsole();
          })
          // ...
  }
  ```

<span data-ttu-id="120be-173">Per altre informazioni, vedere <xref:fundamentals/logging/index></span><span class="sxs-lookup"><span data-stu-id="120be-173">For more information, see <xref:fundamentals/logging/index></span></span>

## <a name="additional-resources"></a><span data-ttu-id="120be-174">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="120be-174">Additional resources</span></span>

* <xref:mvc/compatibility-version>
* <xref:fundamentals/metapackage-app>
* [<span data-ttu-id="120be-175">Riferimenti impliciti ai pacchetti</span><span class="sxs-lookup"><span data-stu-id="120be-175">Implicit package references</span></span>](/dotnet/core/tools/csproj#implicit-package-references)
