---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/intro-to-web-pages-programming
title: 'Introduzione a ASP.NET Web Pages: programmazione di concetti di base | Documenti Microsoft'
author: tfitzmac
description: 'In questa esercitazione fornisce una panoramica di come programma in ASP.NET Web Pages con sintassi Razor. Si apprenderà: la sintassi "Razor" di base utilizzato per pr...'
ms.author: aspnetcontent
manager: wpickett
ms.date: 06/17/2015
ms.topic: article
ms.assetid: 7526ed45-a97d-4e8a-8301-01324ef0eff9
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/intro-to-web-pages-programming
msc.type: authoredcontent
ms.openlocfilehash: 60115dd06a27bf856427953de29e993194afb991
ms.sourcegitcommit: 477d38e33530a305405eaf19faa29c6d805273aa
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 05/07/2018
---
<a name="introducing-aspnet-web-pages---programming-basics"></a><span data-ttu-id="f90ca-104">Introduzione a ASP.NET Web Pages - nozioni di base sulla programmazione</span><span class="sxs-lookup"><span data-stu-id="f90ca-104">Introducing ASP.NET Web Pages - Programming Basics</span></span>
====================
<span data-ttu-id="f90ca-105">da [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="f90ca-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="f90ca-106">In questa esercitazione fornisce una panoramica di come programma in ASP.NET Web Pages con sintassi Razor.</span><span class="sxs-lookup"><span data-stu-id="f90ca-106">This tutorial gives you an overview of how to program in ASP.NET Web Pages with Razor syntax.</span></span>
> 
> <span data-ttu-id="f90ca-107">Illustra quanto segue:</span><span class="sxs-lookup"><span data-stu-id="f90ca-107">What you'll learn:</span></span>
> 
> - <span data-ttu-id="f90ca-108">La sintassi "Razor" base utilizzate per la programmazione in ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="f90ca-108">The basic "Razor" syntax that you use for programming in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="f90ca-109">Alcuni c# di base, ovvero il linguaggio di programmazione da usare.</span><span class="sxs-lookup"><span data-stu-id="f90ca-109">Some basic C#, which is the programming language you'll use.</span></span>
> - <span data-ttu-id="f90ca-110">Alcuni concetti di programmazione fondamentali per le pagine Web.</span><span class="sxs-lookup"><span data-stu-id="f90ca-110">Some fundamental programming concepts for Web Pages.</span></span>
> - <span data-ttu-id="f90ca-111">Come installare i pacchetti (componenti che contengono codice predefiniti) da utilizzare con il sito.</span><span class="sxs-lookup"><span data-stu-id="f90ca-111">How to install packages (components that contain prebuilt code) to use with your site.</span></span>
> - <span data-ttu-id="f90ca-112">Come usare *helper* per eseguire attività comuni di programmazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-112">How to use *helpers* to perform common programming tasks.</span></span>
>   
> 
> <span data-ttu-id="f90ca-113">Funzionalità/tecnologie descritte:</span><span class="sxs-lookup"><span data-stu-id="f90ca-113">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="f90ca-114">NuGet e la gestione dei pacchetti.</span><span class="sxs-lookup"><span data-stu-id="f90ca-114">NuGet and the package manager.</span></span>
> - <span data-ttu-id="f90ca-115">Il `Gravatar` helper.</span><span class="sxs-lookup"><span data-stu-id="f90ca-115">The `Gravatar` helper.</span></span>


<span data-ttu-id="f90ca-116">In questa esercitazione è principalmente un esercizio per una presentazione introduttiva è la sintassi di programmazione che si userà per ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="f90ca-116">This tutorial is primarily an exercise in introducing you to the programming syntax that you'll use for ASP.NET Web Pages.</span></span> <span data-ttu-id="f90ca-117">Si apprenderà sulle *sintassi Razor* e linguaggio di programmazione codice scritto in c#.</span><span class="sxs-lookup"><span data-stu-id="f90ca-117">You'll learn about *Razor syntax* and code that's written in the C# programming language.</span></span> <span data-ttu-id="f90ca-118">Una panoramica di questa sintassi ottenuti nell'esercitazione precedente; in questa esercitazione verrà illustrato la sintassi più.</span><span class="sxs-lookup"><span data-stu-id="f90ca-118">You got a glimpse of this syntax in the previous tutorial; in this tutorial we'll explain the syntax more.</span></span>

<span data-ttu-id="f90ca-119">Ti assicuriamo che questa esercitazione prevede più di programmazione che verrà visualizzato in un'esercitazione singola e che tratta dell'esercitazione solo è *solo* sulla programmazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-119">We promise that this tutorial involves the most programming that you'll see in a single tutorial, and that it's the only tutorial that is *only* about programming.</span></span> <span data-ttu-id="f90ca-120">Nelle esercitazioni rimanenti in questo set, si creeranno effettivamente pagine in cui eseguire operazioni interessanti.</span><span class="sxs-lookup"><span data-stu-id="f90ca-120">In the remaining tutorials in this set, you'll actually create pages that do interesting things.</span></span>

<span data-ttu-id="f90ca-121">Apprenderà anche *helper*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-121">You'll also learn about *helpers*.</span></span> <span data-ttu-id="f90ca-122">Un helper è un componente, ovvero una parte di backup inclusi nel pacchetto di codice, che è possibile aggiungere a una pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-122">A helper is a component — a packaged-up piece of code — that you can add to a page.</span></span> <span data-ttu-id="f90ca-123">L'helper esegue operazioni che in caso contrario potrebbe essere difficile o complessa da svolgere manualmente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-123">The helper performs work for you that otherwise might be tedious or complex to do by hand.</span></span>

## <a name="creating-a-page-to-play-with-razor"></a><span data-ttu-id="f90ca-124">Creazione di una pagina per la riproduzione con Razor</span><span class="sxs-lookup"><span data-stu-id="f90ca-124">Creating a Page to Play with Razor</span></span>

<span data-ttu-id="f90ca-125">In questa sezione si proveranno un bit con Razor in modo è possibile farsi un'idea della sintassi di base.</span><span class="sxs-lookup"><span data-stu-id="f90ca-125">In this section you'll play a bit with Razor so you can get a sense of the basic syntax.</span></span>

<span data-ttu-id="f90ca-126">Se non è già in esecuzione, avviare WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="f90ca-126">Start WebMatrix if it's not already running.</span></span> <span data-ttu-id="f90ca-127">Si utilizzerà il sito Web è stato creato nell'esercitazione precedente ([recupero avviato con le pagine Web](https://go.microsoft.com/fwlink/?LinkId=251578)).</span><span class="sxs-lookup"><span data-stu-id="f90ca-127">You'll use the website you created in the previous tutorial ([Getting Started With Web Pages](https://go.microsoft.com/fwlink/?LinkId=251578)).</span></span> <span data-ttu-id="f90ca-128">Per riaprirla, fare clic su **siti personali** e scegliere **WebPageMovies**:</span><span class="sxs-lookup"><span data-stu-id="f90ca-128">To reopen it, click **My Sites** and choose **WebPageMovies**:</span></span>

![Schermata iniziale di WebMatrix che illustra le opzioni Apri sito e siti personali evidenziato](intro-to-web-pages-programming/_static/image1.png)

<span data-ttu-id="f90ca-130">Selezionare il **file** dell'area di lavoro.</span><span class="sxs-lookup"><span data-stu-id="f90ca-130">Select the **Files** workspace.</span></span>

<span data-ttu-id="f90ca-131">Nella barra multifunzione, fare clic su **New** per creare una pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-131">In the ribbon, click **New** to create a page.</span></span> <span data-ttu-id="f90ca-132">Selezionare **CSHTML** e denominare la nuova pagina *TestRazor.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-132">Select **CSHTML** and name the new page *TestRazor.cshtml*.</span></span>

<span data-ttu-id="f90ca-133">Fare clic su **OK**.</span><span class="sxs-lookup"><span data-stu-id="f90ca-133">Click **OK**.</span></span>

<span data-ttu-id="f90ca-134">Copiare quanto segue nel file di sostituzione completa cosa esiste già.</span><span class="sxs-lookup"><span data-stu-id="f90ca-134">Copy the following into the file, completely replacing what's there already.</span></span>

> [!NOTE]
> <span data-ttu-id="f90ca-135">Quando si copia markup o codice degli esempi in una pagina, il rientro e allineamento potrebbero non essere uguale a quello dell'esercitazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-135">When you copy code or markup from the examples into a page, the indentation and alignment might not be the same as in the tutorial.</span></span> <span data-ttu-id="f90ca-136">Rientro e allineamento non influiscono sulla modalità di esecuzione del codice, tuttavia.</span><span class="sxs-lookup"><span data-stu-id="f90ca-136">Indentation and alignment don't affect how the code runs, though.</span></span>


[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample1.cshtml)]

## <a name="examining-the-example-page"></a><span data-ttu-id="f90ca-137">Esaminare la pagina di esempio</span><span class="sxs-lookup"><span data-stu-id="f90ca-137">Examining the Example Page</span></span>

<span data-ttu-id="f90ca-138">La maggior parte di ciò che viene visualizzato è HTML normale.</span><span class="sxs-lookup"><span data-stu-id="f90ca-138">Most of what you see is ordinary HTML.</span></span> <span data-ttu-id="f90ca-139">Tuttavia, nella parte superiore è il blocco di codice:</span><span class="sxs-lookup"><span data-stu-id="f90ca-139">However, at the top there's this code block:</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample2.cshtml)]

<span data-ttu-id="f90ca-140">Si noti quanto segue su questo blocco di codice:</span><span class="sxs-lookup"><span data-stu-id="f90ca-140">Notice the following things about this code block:</span></span>

- <span data-ttu-id="f90ca-141">Il carattere @ indica ad ASP.NET che quello che segue è codice Razor, non HTML.</span><span class="sxs-lookup"><span data-stu-id="f90ca-141">The @ character tells ASP.NET that what follows is Razor code, not HTML.</span></span> <span data-ttu-id="f90ca-142">ASP.NET considererà tutti gli elementi dopo il @ carattere come codice finché non viene eseguito nuovamente in HTML.</span><span class="sxs-lookup"><span data-stu-id="f90ca-142">ASP.NET will treat everything after the @ character as code until it runs into some HTML again.</span></span> <span data-ttu-id="f90ca-143">(In questo caso, che il &lt;! DOCTYPE&gt; elemento.</span><span class="sxs-lookup"><span data-stu-id="f90ca-143">(In this case, that's the &lt;!DOCTYPE&gt; element.</span></span>
- <span data-ttu-id="f90ca-144">Le parentesi graffe ({e}), racchiudere un blocco di codice Razor se il codice ha più di una riga.</span><span class="sxs-lookup"><span data-stu-id="f90ca-144">The braces ( { and } ) enclose a block of Razor code if the code has more than one line.</span></span> <span data-ttu-id="f90ca-145">Le parentesi graffe indicano ASP.NET in cui il codice per tale blocco inizia e termina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-145">The braces tell ASP.NET where the code for that block starts and ends.</span></span>
- <span data-ttu-id="f90ca-146">Il / / caratteri contrassegnare un commento, vale a dire, una parte del codice non verrà eseguite.</span><span class="sxs-lookup"><span data-stu-id="f90ca-146">The // characters mark a comment — that is, a part of the code that won't execute.</span></span>
- <span data-ttu-id="f90ca-147">Ogni istruzione deve terminare con un punto e virgola (;).</span><span class="sxs-lookup"><span data-stu-id="f90ca-147">Each statement has to end with a semicolon (;).</span></span> <span data-ttu-id="f90ca-148">(Non i commenti, tuttavia.)</span><span class="sxs-lookup"><span data-stu-id="f90ca-148">(Not comments, though.)</span></span>
- <span data-ttu-id="f90ca-149">È possibile archiviare valori in *variabili*, che creano (*dichiarare*) con la funzione VAR (parola chiave).</span><span class="sxs-lookup"><span data-stu-id="f90ca-149">You can store values in *variables*, which you create (*declare*) with the keyword var.</span></span> <span data-ttu-id="f90ca-150">Quando si crea una variabile, assegnargli un nome, che può includere lettere, numeri e caratteri di sottolineatura (\_).</span><span class="sxs-lookup"><span data-stu-id="f90ca-150">When you create a variable, you give it a name, which can include letters, numbers, and underscore (\_).</span></span> <span data-ttu-id="f90ca-151">I nomi delle variabili non può iniziare con un numero e non è possibile utilizzare il nome di una parola chiave di programmazione (ad esempio var).</span><span class="sxs-lookup"><span data-stu-id="f90ca-151">Variable names can't start with a number and can't use the name of a programming keyword (like var).</span></span>
- <span data-ttu-id="f90ca-152">Stringhe di caratteri (ad esempio, "ASP.NET" e "Pagine") racchiudere tra virgolette.</span><span class="sxs-lookup"><span data-stu-id="f90ca-152">You enclose character strings (like "ASP.NET" and "Web Pages") in quotation marks.</span></span> <span data-ttu-id="f90ca-153">(Devono essere le virgolette doppie). I numeri non sono inclusi tra virgolette.</span><span class="sxs-lookup"><span data-stu-id="f90ca-153">(They must be double quotation marks.) Numbers are not in quotation marks.</span></span>
- <span data-ttu-id="f90ca-154">Gli spazi vuoti all'esterno di virgolette non ha importanza.</span><span class="sxs-lookup"><span data-stu-id="f90ca-154">Whitespace outside of quotation marks doesn't matter.</span></span> <span data-ttu-id="f90ca-155">Non importante principalmente le interruzioni di riga. l'eccezione è che non è possibile dividere una stringa tra virgolette in più righe.</span><span class="sxs-lookup"><span data-stu-id="f90ca-155">Line breaks mostly don't matter; the exception is that you can't split a string in quotation marks across lines.</span></span> <span data-ttu-id="f90ca-156">Rientro e allineamento non è rilevante.</span><span class="sxs-lookup"><span data-stu-id="f90ca-156">Indentation and alignment don't matter.</span></span>

<span data-ttu-id="f90ca-157">Un elemento che non sia evidente in questo esempio è che tutto il codice tra maiuscole e minuscole.</span><span class="sxs-lookup"><span data-stu-id="f90ca-157">Something that's not obvious from this example is that all code is case sensitive.</span></span> <span data-ttu-id="f90ca-158">Ciò significa che il somma di variabile è una variabile diversa rispetto alle variabili che potrebbe essere denominata la somma o la somma.</span><span class="sxs-lookup"><span data-stu-id="f90ca-158">This means that the variable TheSum is a different variable than variables that might be named theSum or thesum.</span></span> <span data-ttu-id="f90ca-159">Analogamente, var è una parola chiave, ma non è Var.</span><span class="sxs-lookup"><span data-stu-id="f90ca-159">Similarly, var is a keyword, but Var is not.</span></span>

### <a name="objects-and-properties-and-methods"></a><span data-ttu-id="f90ca-160">Gli oggetti, proprietà e metodi</span><span class="sxs-lookup"><span data-stu-id="f90ca-160">Objects and properties and methods</span></span>

<span data-ttu-id="f90ca-161">Non vi è l'espressione DateTime. Now.</span><span class="sxs-lookup"><span data-stu-id="f90ca-161">Then there's the expression DateTime.Now.</span></span> <span data-ttu-id="f90ca-162">In altre parole, DateTime è un *oggetto*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-162">In simple terms, DateTime is an *object*.</span></span> <span data-ttu-id="f90ca-163">Un oggetto è un elemento che è possibile programmare con, ovvero una pagina di una casella di testo, un file, un'immagine, una richiesta web, un messaggio di posta elettronica, un record del cliente, e così via. Gli oggetti dispongono di uno o più *proprietà* che descrivono le caratteristiche.</span><span class="sxs-lookup"><span data-stu-id="f90ca-163">An object is a thing that you can program with—a page, a text box, a file, an image, a web request, an email message, a customer record, etc. Objects have one or more *properties* that describe their characteristics.</span></span> <span data-ttu-id="f90ca-164">Oggetto di una casella di testo ha una proprietà di testo (tra gli altri), un oggetto di richiesta ha una proprietà Url (e altri), un messaggio di posta elettronica è una proprietà From e una proprietà a, e così via.</span><span class="sxs-lookup"><span data-stu-id="f90ca-164">A text box object has a Text property (among others), a request object has a Url property (and others), an email message has a From property and a To property, and so on.</span></span> <span data-ttu-id="f90ca-165">Gli oggetti hanno anche *metodi* che sono "verbi" possono eseguire.</span><span class="sxs-lookup"><span data-stu-id="f90ca-165">Objects also have *methods* that are the "verbs" they can perform.</span></span> <span data-ttu-id="f90ca-166">Verrà utilizzato con gli oggetti molto.</span><span class="sxs-lookup"><span data-stu-id="f90ca-166">You'll be working with objects a lot.</span></span>

<span data-ttu-id="f90ca-167">Come si può notare nell'esempio, DateTime è un oggetto che consente di programma date e ore.</span><span class="sxs-lookup"><span data-stu-id="f90ca-167">As you can see from the example, DateTime is an object that lets you program dates and times.</span></span> <span data-ttu-id="f90ca-168">Include una proprietà denominata ora che restituisce la data e ora correnti.</span><span class="sxs-lookup"><span data-stu-id="f90ca-168">It has a property named Now that returns the current date and time.</span></span>

### <a name="using-code-to-render-markup-in-the-page"></a><span data-ttu-id="f90ca-169">Utilizzo di codice per il rendering del markup nella pagina</span><span class="sxs-lookup"><span data-stu-id="f90ca-169">Using code to render markup in the page</span></span>

<span data-ttu-id="f90ca-170">Nel corpo della pagina, tenere presente quanto segue:</span><span class="sxs-lookup"><span data-stu-id="f90ca-170">In the body of the page, notice the following:</span></span>

[!code-html[Main](intro-to-web-pages-programming/samples/sample3.html)]

<span data-ttu-id="f90ca-171">Nuovamente, il carattere @ indica ad ASP.NET che quello che segue è il codice, non HTML.</span><span class="sxs-lookup"><span data-stu-id="f90ca-171">Again, the @ character tells ASP.NET that what follows is code, not HTML.</span></span> <span data-ttu-id="f90ca-172">Nel codice è possibile aggiungere seguita da un'espressione di codice e ASP.NET restituirà il valore di tale diritto espressione a quel punto.</span><span class="sxs-lookup"><span data-stu-id="f90ca-172">In the markup you can add @ followed by a code expression, and ASP.NET will render the value of that expression right at that point.</span></span> <span data-ttu-id="f90ca-173">Nell'esempio @a verrà eseguito il rendering l'elemento è il valore della variabile denominata, @product esegue il rendering di qualunque sia il prodotto denominato variabile e così via.</span><span class="sxs-lookup"><span data-stu-id="f90ca-173">In the example, @a will render whatever the value is of the variable named a, @product renders whatever is in the variable named product, and so on.</span></span>

<span data-ttu-id="f90ca-174">Non si è limitato alle variabili, tuttavia.</span><span class="sxs-lookup"><span data-stu-id="f90ca-174">You're not limited to variables, though.</span></span> <span data-ttu-id="f90ca-175">In alcuni casi, il carattere @ precede un'espressione:</span><span class="sxs-lookup"><span data-stu-id="f90ca-175">In a few instances here, the @ character precedes an expression:</span></span>

- <span data-ttu-id="f90ca-176">@(un\*b) esegue il rendering del prodotto delle informazioni presenti nelle variabili di un a e b.</span><span class="sxs-lookup"><span data-stu-id="f90ca-176">@(a\*b) renders the product of whatever is in the variables a and b.</span></span> <span data-ttu-id="f90ca-177">(Il \* operatore: moltiplicazione.)</span><span class="sxs-lookup"><span data-stu-id="f90ca-177">(The \* operator means multiplication.)</span></span>
- <span data-ttu-id="f90ca-178">@(tecnologia + "" + prodotto) Visualizza i valori in una tecnologia di variabili e il prodotto dopo concatenarle e aggiunta di uno spazio tra.</span><span class="sxs-lookup"><span data-stu-id="f90ca-178">@(technology + " " + product) renders the values in the variables technology and product after concatenating them and adding a space in between.</span></span> <span data-ttu-id="f90ca-179">L'operatore (+) per la concatenazione di stringhe è lo stesso come operatore per l'aggiunta di numeri.</span><span class="sxs-lookup"><span data-stu-id="f90ca-179">The operator (+) for concatenating strings is the same as the operator for adding numbers.</span></span> <span data-ttu-id="f90ca-180">ASP.NET in genere è possibile indicare se si sta lavorando con numeri o stringhe e consente di eseguire l'operazione di destra con l'operatore +.</span><span class="sxs-lookup"><span data-stu-id="f90ca-180">ASP.NET can usually tell whether you're working with numbers or with strings and does the right thing with the + operator.</span></span>
- <span data-ttu-id="f90ca-181">@Request.Url esegue il rendering la proprietà Url dell'oggetto della richiesta.</span><span class="sxs-lookup"><span data-stu-id="f90ca-181">@Request.Url renders the Url property of the Request object.</span></span> <span data-ttu-id="f90ca-182">L'oggetto richiesta contiene informazioni sulla richiesta corrente dal browser e naturalmente la proprietà Url contiene l'URL della richiesta corrente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-182">The Request object contains information about the current request from the browser, and of course the Url property contains the URL of that current request.</span></span>

<span data-ttu-id="f90ca-183">L'esempio è inoltre progettato per mostrare che è possibile eseguire il lavoro in modi diversi.</span><span class="sxs-lookup"><span data-stu-id="f90ca-183">The example is also designed to show you that you can do work in different ways.</span></span> <span data-ttu-id="f90ca-184">È possibile eseguire calcoli nel blocco di codice nella parte superiore, inserire i risultati in una variabile e quindi eseguire il rendering la variabile nel markup.</span><span class="sxs-lookup"><span data-stu-id="f90ca-184">You can do calculations in the code block at the top, put the results into a variable, and then render the variable in markup.</span></span> <span data-ttu-id="f90ca-185">Oppure è possibile eseguire calcoli in un diritto di espressione nel markup.</span><span class="sxs-lookup"><span data-stu-id="f90ca-185">Or you can do calculations in an expression right in the markup.</span></span> <span data-ttu-id="f90ca-186">L'approccio adottato dipende dalle operazioni eseguite e, in alcuni casi, delle proprie preferenze.</span><span class="sxs-lookup"><span data-stu-id="f90ca-186">The approach you use depends on what you're doing and, to some extent, on your own preference.</span></span>

### <a name="seeing-the-code-in-action"></a><span data-ttu-id="f90ca-187">Visualizzare i codici di azione</span><span class="sxs-lookup"><span data-stu-id="f90ca-187">Seeing the code in action</span></span>

<span data-ttu-id="f90ca-188">Il nome del file e quindi **Avvia nel browser**.</span><span class="sxs-lookup"><span data-stu-id="f90ca-188">Right-click the name of the file and then choose **Launch in browser**.</span></span> <span data-ttu-id="f90ca-189">Viene visualizzata la pagina nel browser con tutti i valori e le espressioni risolte nella pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-189">You see the page in the browser with all the values and expressions resolved in the page.</span></span>

![Pagina 'TestRazor' in esecuzione nel browser](intro-to-web-pages-programming/_static/image2.png)

<span data-ttu-id="f90ca-191">Esaminare l'origine nel browser.</span><span class="sxs-lookup"><span data-stu-id="f90ca-191">Look at the source in the browser.</span></span>

!['Test Razor' origine della pagina nel browser](intro-to-web-pages-programming/_static/image3.png)

<span data-ttu-id="f90ca-193">Come si prevede che l'esperienza nell'esercitazione precedente, nessuna parte del codice Razor è nella pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-193">As you expect from your experience in the previous tutorial, none of the Razor code is in the page.</span></span> <span data-ttu-id="f90ca-194">Siano che visibili solo i valori di visualizzazione effettivo.</span><span class="sxs-lookup"><span data-stu-id="f90ca-194">All you see are the actual display values.</span></span> <span data-ttu-id="f90ca-195">Quando si esegue una pagina, si apportano effettivamente una richiesta al server web che viene compilato in WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="f90ca-195">When you run a page, you're actually making a request to the web server that's built into WebMatrix.</span></span> <span data-ttu-id="f90ca-196">Quando viene ricevuta la richiesta, ASP.NET risolve tutti i valori e le espressioni e viene eseguito il rendering di valori nella pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-196">When the request is received, ASP.NET resolves all the values and expressions and renders their values into the page.</span></span> <span data-ttu-id="f90ca-197">Invia quindi la pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="f90ca-197">It then sends the page to the browser.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="f90ca-198">**Razor e c#**</span><span class="sxs-lookup"><span data-stu-id="f90ca-198">**Razor and C#**</span></span>
> 
> <span data-ttu-id="f90ca-199">Fino a ora abbiamo abbiamo detto che si lavora con sintassi Razor.</span><span class="sxs-lookup"><span data-stu-id="f90ca-199">Up to now we've said that you're working with Razor syntax.</span></span> <span data-ttu-id="f90ca-200">Che è true, ma non la storia completa.</span><span class="sxs-lookup"><span data-stu-id="f90ca-200">That's true, but it's not the complete story.</span></span> <span data-ttu-id="f90ca-201">Il linguaggio di programmazione effettivo in uso viene chiamato *c#*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-201">The actual programming language you're using is called *C#*.</span></span> <span data-ttu-id="f90ca-202">In c# è stato creato da Microsoft oltre un decennio fa ed è diventato uno dei linguaggi di programmazione primari per la creazione di App di Windows.</span><span class="sxs-lookup"><span data-stu-id="f90ca-202">C# was created by Microsoft over a decade ago and has become one of the primary programming languages for creating Windows apps.</span></span> <span data-ttu-id="f90ca-203">Tutte le regole di cui che si è visto su come assegnare un nome di una variabile e come creare le istruzioni e così via sono effettivamente tutte le regole del linguaggio c#.</span><span class="sxs-lookup"><span data-stu-id="f90ca-203">All the rules you've seen about how to name a variable and how to create statements and so on are actually all rules of the C# language.</span></span>
> 
> <span data-ttu-id="f90ca-204">Razor fa riferimento in modo più specifico per il piccolo set di convenzioni per come incorporare il codice in una pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-204">Razor refers more specifically to the small set of conventions for how you embed this code into a page.</span></span> <span data-ttu-id="f90ca-205">Ad esempio, la convenzione di usando per contrassegnare il codice della pagina e @ {} per incorporare un blocco di codice è l'aspetto Razor di una pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-205">For example, the convention of using @ to mark code in the page and using @{ } to embed a code block is the Razor aspect of a page.</span></span> <span data-ttu-id="f90ca-206">Gli helper sono considerati come parte del Razor.</span><span class="sxs-lookup"><span data-stu-id="f90ca-206">Helpers are also considered to be part of Razor.</span></span> <span data-ttu-id="f90ca-207">Sintassi Razor è utilizzata in altre posizioni di solo in ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="f90ca-207">Razor syntax is used in more places than just in ASP.NET Web Pages.</span></span> <span data-ttu-id="f90ca-208">(Ad esempio, utilizzato nelle visualizzazioni di ASP.NET MVC nonché.)</span><span class="sxs-lookup"><span data-stu-id="f90ca-208">(For example, it's used in ASP.NET MVC views as well.)</span></span>
> 
> <span data-ttu-id="f90ca-209">È importante tenere presente questo perché se si cerca di informazioni sulla programmazione ASP.NET Web Pages, è disponibile un numero elevato di riferimenti a Razor.</span><span class="sxs-lookup"><span data-stu-id="f90ca-209">We mention this because if you look for information about programming ASP.NET Web Pages, you'll find lots of references to Razor.</span></span> <span data-ttu-id="f90ca-210">Tuttavia, molte di tali riferimenti non si applicano a quanto è associata a tale operazione e pertanto potrebbe risultare poco chiaro.</span><span class="sxs-lookup"><span data-stu-id="f90ca-210">However, a lot of those references don't apply to what you're doing and might therefore be confusing.</span></span> <span data-ttu-id="f90ca-211">E, in realtà, molte delle domande programmazione effettivamente devono essere relative a lavorare con c# o l'utilizzo con ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="f90ca-211">And in fact, many of your programming questions are really going to be about either working with C# or working with ASP.NET.</span></span> <span data-ttu-id="f90ca-212">Pertanto, se si esamina in modo specifico per informazioni su Razor, potrebbe non trovare le risposte che necessarie.</span><span class="sxs-lookup"><span data-stu-id="f90ca-212">So if you look specifically for information about Razor, you might not find the answers you need.</span></span>


## <a name="adding-some-conditional-logic"></a><span data-ttu-id="f90ca-213">Aggiunta di logica condizionale</span><span class="sxs-lookup"><span data-stu-id="f90ca-213">Adding Some Conditional Logic</span></span>

<span data-ttu-id="f90ca-214">Una delle eccezionali funzionalità sull'utilizzo di codice in una pagina è che è possibile modificare cosa accade in base a diverse condizioni.</span><span class="sxs-lookup"><span data-stu-id="f90ca-214">One of the great features about using code in a page is that you can change what happens based on various conditions.</span></span> <span data-ttu-id="f90ca-215">In questa parte dell'esercitazione, sarà sperimentare alcuni modi per modificare la visualizzazione della pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-215">In this part of the tutorial, you'll play around with some ways to change what's displayed in the page.</span></span>

<span data-ttu-id="f90ca-216">Nell'esempio sarà semplice e un certo studiato appositamente in modo che è possibile concentrarsi sulla logica condizionale.</span><span class="sxs-lookup"><span data-stu-id="f90ca-216">The example will be simple and somewhat contrived so that we can concentrate on the conditional logic.</span></span> <span data-ttu-id="f90ca-217">La pagina che si creeranno eseguirà questo:</span><span class="sxs-lookup"><span data-stu-id="f90ca-217">The page you'll create will do this:</span></span>

- <span data-ttu-id="f90ca-218">Visualizzare il testo diverso nella pagina, a seconda se è la prima volta che viene visualizzata la pagina o se si è fatto clic di un pulsante per l'invio della pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-218">Show different text on the page depending on whether it's the first time the page is displayed or whether you've clicked a button to submit the page.</span></span> <span data-ttu-id="f90ca-219">Che sarà il primo test condizionale.</span><span class="sxs-lookup"><span data-stu-id="f90ca-219">That will be the first conditional test.</span></span>
- <span data-ttu-id="f90ca-220">Il messaggio viene visualizzato solo se un determinato valore viene passato nella stringa di query dell'URL (http://...?show=true).</span><span class="sxs-lookup"><span data-stu-id="f90ca-220">Display the message only if a certain value is passed in the query string of the URL (http://...?show=true).</span></span> <span data-ttu-id="f90ca-221">Che sarà il secondo test condizionale.</span><span class="sxs-lookup"><span data-stu-id="f90ca-221">That will be the second conditional test.</span></span>

<span data-ttu-id="f90ca-222">In WebMatrix, creare una pagina e il nome *TestRazorPart2.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-222">In WebMatrix, create a page and name it *TestRazorPart2.cshtml*.</span></span> <span data-ttu-id="f90ca-223">(Nella barra multifunzione, fare clic su **New**, scegliere **CSHTML**, denominare il file e quindi fare clic su **OK**.)</span><span class="sxs-lookup"><span data-stu-id="f90ca-223">(In the ribbon, click **New**, choose **CSHTML**, name the file, and then click **OK**.)</span></span>

<span data-ttu-id="f90ca-224">Sostituire il contenuto di tale pagina con le operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="f90ca-224">Replace the contents of that page with the following:</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample4.cshtml)]

<span data-ttu-id="f90ca-225">Il blocco di codice nella parte superiore consente di inizializzare una variabile denominata messaggio con del testo.</span><span class="sxs-lookup"><span data-stu-id="f90ca-225">The code block at the top initializes a variable named message with some text.</span></span> <span data-ttu-id="f90ca-226">Nel corpo della pagina, il contenuto della variabile di messaggio viene visualizzato all'interno di un &lt;p&gt; elemento.</span><span class="sxs-lookup"><span data-stu-id="f90ca-226">In the body of the page, the contents of the message variable are displayed inside a &lt;p&gt; element.</span></span> <span data-ttu-id="f90ca-227">Il codice contiene inoltre un &lt;input&gt; elemento per creare un **Invia** pulsante.</span><span class="sxs-lookup"><span data-stu-id="f90ca-227">The markup also contains an &lt;input&gt; element to create a **Submit** button.</span></span>

<span data-ttu-id="f90ca-228">Eseguire la pagina per vedere come funziona ora.</span><span class="sxs-lookup"><span data-stu-id="f90ca-228">Run the page to see how it works now.</span></span> <span data-ttu-id="f90ca-229">Per il momento, si tratta in sostanza una pagina statica, anche se si sceglie il **Invia** pulsante.</span><span class="sxs-lookup"><span data-stu-id="f90ca-229">For now, it's basically a static page, even if you click the **Submit** button.</span></span>

<span data-ttu-id="f90ca-230">Tornare a WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="f90ca-230">Go back to WebMatrix.</span></span> <span data-ttu-id="f90ca-231">All'interno del blocco di codice, aggiungere il seguente codice evidenziato *dopo* la riga che inizializza messaggio:</span><span class="sxs-lookup"><span data-stu-id="f90ca-231">Inside the code block, add the following highlighted code *after* the line that initializes message:</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample5.cshtml?highlight=4-6)]

### <a name="the-if---block"></a><span data-ttu-id="f90ca-232">Se il blocco di {}</span><span class="sxs-lookup"><span data-stu-id="f90ca-232">The if { } block</span></span>

<span data-ttu-id="f90ca-233">Appena aggiunto è stato un se condizione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-233">What you just added was an if condition.</span></span> <span data-ttu-id="f90ca-234">Nel codice, il se condizione dispone di una struttura simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="f90ca-234">In code, the if condition has a structure like this:</span></span>

[!code-csharp[Main](intro-to-web-pages-programming/samples/sample6.cs)]

<span data-ttu-id="f90ca-235">La condizione da testare è racchiuso tra parentesi.</span><span class="sxs-lookup"><span data-stu-id="f90ca-235">The condition to test is in parentheses.</span></span> <span data-ttu-id="f90ca-236">Deve essere un valore o un'espressione che restituisce true o false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-236">It has to be a value or an expression that returns true or false.</span></span> <span data-ttu-id="f90ca-237">Se la condizione è true, ASP.NET esegue le istruzioni che si trovano all'interno delle parentesi graffe.</span><span class="sxs-lookup"><span data-stu-id="f90ca-237">If the condition is true, ASP.NET runs the statement or statements that are inside the braces.</span></span> <span data-ttu-id="f90ca-238">(Sono il *quindi* in parte il *if-then* logica.) Se la condizione è false, il blocco di codice viene ignorato.</span><span class="sxs-lookup"><span data-stu-id="f90ca-238">(Those are the *then* part of the *if-then* logic.) If the condition is false, the block of code is skipped.</span></span>

<span data-ttu-id="f90ca-239">Ecco alcuni esempi di condizioni, è possibile verificare in un'istruzione:</span><span class="sxs-lookup"><span data-stu-id="f90ca-239">Here are a few examples of conditions you can test in an if statement:</span></span>

[!code-csharp[Main](intro-to-web-pages-programming/samples/sample7.cs)]

<span data-ttu-id="f90ca-240">È possibile testare le variabili in base a valori o espressioni utilizzando un <em>operatore logico</em> o <em>operatore di confronto</em>: uguale a (= =), maggiore di (&gt;), minore di (&lt;), maggiore o uguale a (&gt;=) e minore o uguale a (&lt;=).</span><span class="sxs-lookup"><span data-stu-id="f90ca-240">You can test variables against values or against expressions by using a <em>logical operator</em> or <em>comparison operator</em>: equal to (==), greater than (&gt;), less than (&lt;), greater than or equal to (&gt;=), and less than or equal to (&lt;=).</span></span> <span data-ttu-id="f90ca-241">Il! = operatore significa non è uguale a, ad esempio, se (un! = 0) significa <em>se</em> <em>un</em><em>non è uguale a 0</em>.</span><span class="sxs-lookup"><span data-stu-id="f90ca-241">The != operator means not equal to — for example, if(a != 0) means <em>if</em> <em>a</em><em>is not equal to 0</em>.</span></span>

> [!NOTE]
> <span data-ttu-id="f90ca-242">Verificare che si nota che l'operatore di confronto uguale a (= =) non è identico a quello =.</span><span class="sxs-lookup"><span data-stu-id="f90ca-242">Make sure you notice that the comparison operator for equals to (==) is not the same as =.</span></span> <span data-ttu-id="f90ca-243">I = operatore viene usato solo per assegnare valori (var un = 2).</span><span class="sxs-lookup"><span data-stu-id="f90ca-243">The = operator is used only to assign values (var a=2).</span></span> <span data-ttu-id="f90ca-244">Se si combinano questi operatori di, si ottiene un errore o si otterrà alcuni risultati imprevisti.</span><span class="sxs-lookup"><span data-stu-id="f90ca-244">If you mix these operators up, you'll either get an error or you'll get some strange results.</span></span>


<span data-ttu-id="f90ca-245">Per verificare se un elemento è true, la sintassi completa è if(IsDone == true).</span><span class="sxs-lookup"><span data-stu-id="f90ca-245">To test whether something is true, the complete syntax is if(IsDone == true).</span></span> <span data-ttu-id="f90ca-246">Ma è anche possibile usare il if(IsDone) di scelta rapida.</span><span class="sxs-lookup"><span data-stu-id="f90ca-246">But you can also use the shortcut if(IsDone).</span></span> <span data-ttu-id="f90ca-247">Se è presente nessun operatore di confronto, ASP.NET si presuppone che si sta testando per true.</span><span class="sxs-lookup"><span data-stu-id="f90ca-247">If there's no comparison operator, ASP.NET assumes that you're testing for true.</span></span>

<span data-ttu-id="f90ca-248">Il carattere!</span><span class="sxs-lookup"><span data-stu-id="f90ca-248">The !</span></span> <span data-ttu-id="f90ca-249">operatore di per sé significa NOT logico.</span><span class="sxs-lookup"><span data-stu-id="f90ca-249">operator by itself means a logical NOT.</span></span> <span data-ttu-id="f90ca-250">Ad esempio, la condizione if (! Istruzione IsPost) significa *se l'istruzione IsPost non è vero*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-250">For example, the condition if(!IsPost) means *if IsPost is not true*.</span></span>

<span data-ttu-id="f90ca-251">È possibile combinare condizioni con un operatore logico AND (&amp; &amp; operator) o OR logico (| | (operatore)).</span><span class="sxs-lookup"><span data-stu-id="f90ca-251">You can combine conditions by using a logical AND (&amp;&amp; operator) or logical OR (|| operator).</span></span> <span data-ttu-id="f90ca-252">Ad esempio, l'ultima del se le condizioni nel precedente si esempi *se FileProcessingIsDone non è impostata su true e displayMessage è impostata su false*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-252">For example, the last of the if conditions in the preceding examples means *if FileProcessingIsDone is not set to true AND displayMessage is set to false*.</span></span>

### <a name="the-else-block"></a><span data-ttu-id="f90ca-253">Il blocco else</span><span class="sxs-lookup"><span data-stu-id="f90ca-253">The else block</span></span>

<span data-ttu-id="f90ca-254">L'ultimo elemento sul se blocchi: un se il blocco può essere seguito da un blocco else.</span><span class="sxs-lookup"><span data-stu-id="f90ca-254">One final thing about if blocks: an if block can be followed by an else block.</span></span> <span data-ttu-id="f90ca-255">Un blocco else è utile è necessario eseguire codice diverso quando la condizione è false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-255">An else block is useful is you have to execute different code when the condition is false.</span></span> <span data-ttu-id="f90ca-256">Di seguito è riportato un esempio:</span><span class="sxs-lookup"><span data-stu-id="f90ca-256">Here's a simple example:</span></span>

[!code-csharp[Main](intro-to-web-pages-programming/samples/sample8.cs)]

<span data-ttu-id="f90ca-257">Nelle esercitazioni successive di questa serie in cui l'utilizzo di un blocco else è utile, si noterà alcuni esempi.</span><span class="sxs-lookup"><span data-stu-id="f90ca-257">You'll see some examples in later tutorials in this series where using an else block is useful.</span></span>

### <a name="testing-whether-the-request-is-a-submit-post"></a><span data-ttu-id="f90ca-258">Verificare se la richiesta è un invio (post)</span><span class="sxs-lookup"><span data-stu-id="f90ca-258">Testing whether the request is a submit (post)</span></span>

<span data-ttu-id="f90ca-259">Altre, ma tornando all'esempio, che ha il if(IsPost) condizione {…}.</span><span class="sxs-lookup"><span data-stu-id="f90ca-259">There's more, but let's get back to the example, which has the condition if(IsPost){ ... }.</span></span> <span data-ttu-id="f90ca-260">Istruzione IsPost è effettivamente una proprietà della pagina corrente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-260">IsPost is actually a property of the current page.</span></span> <span data-ttu-id="f90ca-261">La prima volta che viene richiesta la pagina, istruzione IsPost restituisce false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-261">The first time the page is requested, IsPost returns false.</span></span> <span data-ttu-id="f90ca-262">Tuttavia, se si sceglie un pulsante o in caso contrario inviare la pagina, vale a dire, registrando: istruzione IsPost restituisce true.</span><span class="sxs-lookup"><span data-stu-id="f90ca-262">However, if you click a button or otherwise submit the page — that is, you post it — IsPost returns true.</span></span> <span data-ttu-id="f90ca-263">Pertanto, istruzione IsPost consente di determinare se si vuole eseguire l'invio di un form.</span><span class="sxs-lookup"><span data-stu-id="f90ca-263">So IsPost lets you determine whether you're dealing with a form submission.</span></span> <span data-ttu-id="f90ca-264">(In termini di verbi HTTP, se la richiesta è un'operazione GET, istruzione IsPost restituisce false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-264">(In terms of HTTP verbs, if the request is a GET operation, IsPost returns false.</span></span> <span data-ttu-id="f90ca-265">Se la richiesta è un'operazione POST, istruzione IsPost restituisce true.) In un'esercitazione successiva si utilizzerà moduli di input, in cui questo test condizione risulta particolarmente utile.</span><span class="sxs-lookup"><span data-stu-id="f90ca-265">If the request is a POST operation, IsPost returns true.) In a later tutorial you'll work with input forms, where this test becomes particularly useful.</span></span>

<span data-ttu-id="f90ca-266">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-266">Run the page.</span></span> <span data-ttu-id="f90ca-267">Poiché questa è la prima volta è richiesta la pagina, vedere "È la prima volta che hai richiesto la pagina".</span><span class="sxs-lookup"><span data-stu-id="f90ca-267">Because this is the first time you're requested the page, you see "This is the first time you've requested the page".</span></span> <span data-ttu-id="f90ca-268">Tale stringa è il valore che è stato inizializzato per la variabile del messaggio.</span><span class="sxs-lookup"><span data-stu-id="f90ca-268">That string is the value that you initialized the message variable to.</span></span> <span data-ttu-id="f90ca-269">Si tratta di un test di if(IsPost), ma che restituisce false al momento, in modo da bloccare il codice all'interno di se non viene eseguito.</span><span class="sxs-lookup"><span data-stu-id="f90ca-269">There's an if(IsPost) test, but that returns false at the moment, so the code inside the if block doesn't run.</span></span>

<span data-ttu-id="f90ca-270">Fare clic su di **Invia** pulsante.</span><span class="sxs-lookup"><span data-stu-id="f90ca-270">Click the **Submit** button.</span></span> <span data-ttu-id="f90ca-271">La pagina viene richiesta nuovamente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-271">The page is requested again.</span></span> <span data-ttu-id="f90ca-272">Come prima, la variabile di messaggio è impostata su "È la prima volta...".</span><span class="sxs-lookup"><span data-stu-id="f90ca-272">As before, the message variable is set to "This is the first time ...".</span></span> <span data-ttu-id="f90ca-273">Ma questa volta if(IsPost) il test restituisce true, il codice all'interno di se bloccare l'esecuzione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-273">But this time, the test if(IsPost) returns true, so the code inside the if block runs.</span></span> <span data-ttu-id="f90ca-274">Il codice modifica il valore della variabile di messaggio a un valore diverso, viene eseguito il rendering nel markup.</span><span class="sxs-lookup"><span data-stu-id="f90ca-274">The code changes the value of the message variable to a different value, which is what's rendered in the markup.</span></span>

<span data-ttu-id="f90ca-275">A questo punto aggiungere un se condizione nel markup.</span><span class="sxs-lookup"><span data-stu-id="f90ca-275">Now add an if condition in the markup.</span></span> <span data-ttu-id="f90ca-276">Di sotto di &lt;p&gt; elemento che contiene il **Invia** pulsante, aggiungere il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="f90ca-276">Below the &lt;p&gt; element that contains the **Submit** button, add the following markup:</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample9.cshtml)]

<span data-ttu-id="f90ca-277">Si sta aggiungendo codice all'interno del markup, pertanto è necessario iniziare con @.</span><span class="sxs-lookup"><span data-stu-id="f90ca-277">You're adding code inside the markup, so you have to start with @.</span></span> <span data-ttu-id="f90ca-278">Non vi è un se test simile a quello aggiunto in precedenza nel blocco di codice.</span><span class="sxs-lookup"><span data-stu-id="f90ca-278">Then there's an if test similar to the one you added earlier up in the code block.</span></span> <span data-ttu-id="f90ca-279">All'interno delle parentesi, tuttavia, si aggiungono HTML ordinario, almeno, è normale fino ad arrivare a @DateTime.Now.</span><span class="sxs-lookup"><span data-stu-id="f90ca-279">Inside the braces, though, you're adding ordinary HTML — at least, it's ordinary until it gets to @DateTime.Now.</span></span> <span data-ttu-id="f90ca-280">Si tratta di un altro poco di codice Razor, dunque è necessario aggiungere trovano davanti.</span><span class="sxs-lookup"><span data-stu-id="f90ca-280">This is another little bit of Razor code, so again you have to add @ in front of it.</span></span>

<span data-ttu-id="f90ca-281">Il punto è che se è possibile aggiungere condizioni sia il codice blocca nella parte superiore e nel markup.</span><span class="sxs-lookup"><span data-stu-id="f90ca-281">The point here is that you can add if conditions in both the code block at the top and in the markup.</span></span> <span data-ttu-id="f90ca-282">Se si utilizza un se condizione nel corpo della pagina, le righe all'interno del blocco può essere markup o codice.</span><span class="sxs-lookup"><span data-stu-id="f90ca-282">If you use an if condition in the body of the page, the lines inside the block can be markup or code.</span></span> <span data-ttu-id="f90ca-283">In tal caso, e come è true ogni volta che si combinano markup e codice, è necessario utilizzare per rendere più chiare di ASP.NET in cui il codice è.</span><span class="sxs-lookup"><span data-stu-id="f90ca-283">In that case, and as is true anytime you mix markup and code, you have to use @ to make it clear to ASP.NET where the code is.</span></span>

<span data-ttu-id="f90ca-284">Eseguire la pagina e fare clic su **Invia**.</span><span class="sxs-lookup"><span data-stu-id="f90ca-284">Run the page and click **Submit**.</span></span> <span data-ttu-id="f90ca-285">Questa volta non visualizzare solo un messaggio diverso quando si invia ("dopo avere inviato..."), ma viene visualizzato un nuovo messaggio che indica la data e ora.</span><span class="sxs-lookup"><span data-stu-id="f90ca-285">This time you not only see a different message when you submit ("Now you've submitted ..."), but you see a new message that lists the date and time.</span></span>

![Pagina 'Test Razor 2' in esecuzione nel browser con un timestamp di visualizzazione dopo l'invio](intro-to-web-pages-programming/_static/image4.png)

### <a name="testing-the-value-of-a-query-string"></a><span data-ttu-id="f90ca-287">Verifica del valore di una stringa di query</span><span class="sxs-lookup"><span data-stu-id="f90ca-287">Testing the value of a query string</span></span>

<span data-ttu-id="f90ca-288">Un test di altre.</span><span class="sxs-lookup"><span data-stu-id="f90ca-288">One more test.</span></span> <span data-ttu-id="f90ca-289">Questa volta, si aggiungerà un se blocco che consente di verificare un valore denominato show che possono essere passati nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="f90ca-289">This time, you'll add an if block that tests a value named show that might be passed in the query string.</span></span> <span data-ttu-id="f90ca-290">(Come segue: `http://localhost:43097/TestRazorPart2.cshtml?show=true`) si modificherà la pagina in modo che il messaggio è stato visualizzati ("This is la prima volta..." e così via) viene visualizzata solo se il valore di show è true.</span><span class="sxs-lookup"><span data-stu-id="f90ca-290">(Like this: `http://localhost:43097/TestRazorPart2.cshtml?show=true`) You'll change the page so that the message you've been displaying ("This is the first time ...", etc.) is only displayed if the value of show is true.</span></span>

<span data-ttu-id="f90ca-291">Nella parte inferiore (ma interno), il blocco di codice nella parte superiore della pagina, aggiungere quanto segue:</span><span class="sxs-lookup"><span data-stu-id="f90ca-291">At the bottom (but inside) the code block at the top of the page, add the following:</span></span>

[!code-csharp[Main](intro-to-web-pages-programming/samples/sample10.cs)]

<span data-ttu-id="f90ca-292">Il codice completo blocco ora simile a nell'esempio seguente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-292">The complete code block now look like the following example.</span></span> <span data-ttu-id="f90ca-293">(Tenere presente che quando si copia il codice in una pagina, il rientro potrebbe essere diverso.</span><span class="sxs-lookup"><span data-stu-id="f90ca-293">(Remember that when you copy the code into your page, the indentation might look different.</span></span> <span data-ttu-id="f90ca-294">Ma che non influisce sul modo in cui viene eseguito il codice).</span><span class="sxs-lookup"><span data-stu-id="f90ca-294">But that doesn't affect how the code runs.)</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample11.cshtml)]

<span data-ttu-id="f90ca-295">Il nuovo codice nel blocco consente di inizializzare una variabile denominata showMessage su false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-295">The new code in the block initializes a variable named showMessage to false.</span></span> <span data-ttu-id="f90ca-296">Esegue quindi un se test per cercare un valore nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="f90ca-296">It then does an if test to look for a value in the query string.</span></span> <span data-ttu-id="f90ca-297">Quando si richiede innanzitutto la pagina, dispone di un URL simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="f90ca-297">When you first request the page, it has a URL like this one:</span></span>

`http://localhost:43097/TestRazorPart2.cshtml`

<span data-ttu-id="f90ca-298">Il codice determina se l'URL contiene una variabile denominata Mostra nella stringa di query, ad esempio di questa versione dell'URL:</span><span class="sxs-lookup"><span data-stu-id="f90ca-298">The code determines whether the URL contains a variable named show in the query string, like this version of the URL:</span></span>

<span data-ttu-id="f90ca-299">`http://localhost:43097/TestRazorPart2.cshtml`? Mostra = true</span><span class="sxs-lookup"><span data-stu-id="f90ca-299">`http://localhost:43097/TestRazorPart2.cshtml`?show=true</span></span>

<span data-ttu-id="f90ca-300">Il test di esamina la proprietà di stringa di query dell'oggetto della richiesta.</span><span class="sxs-lookup"><span data-stu-id="f90ca-300">The test itself looks at the QueryString property of the Request object.</span></span> <span data-ttu-id="f90ca-301">Se la stringa di query contiene un'elemento denominata presentazione e se tale elemento è impostato su true, se blocco viene eseguito e imposta la variabile showMessage su true.</span><span class="sxs-lookup"><span data-stu-id="f90ca-301">If the query string contains an item named show, and if that item is set to true, the if block runs and sets the showMessage variable to true.</span></span>

<span data-ttu-id="f90ca-302">È uno stratagemma in questo caso, è possibile visualizzare.</span><span class="sxs-lookup"><span data-stu-id="f90ca-302">There's a trick here, as you can see.</span></span> <span data-ttu-id="f90ca-303">Come indicato il nome, la stringa di query è una stringa.</span><span class="sxs-lookup"><span data-stu-id="f90ca-303">Like the name says, the query string is a string.</span></span> <span data-ttu-id="f90ca-304">Tuttavia, è possibile testare solo per true e false se il valore di cui che si sta testando è valore booleano (true/false).</span><span class="sxs-lookup"><span data-stu-id="f90ca-304">However, you can only test for true and false if the value you're testing is a Boolean (true/false) value.</span></span> <span data-ttu-id="f90ca-305">Prima di testare il valore della variabile Mostra nella stringa di query, è necessario convertirlo in un valore booleano.</span><span class="sxs-lookup"><span data-stu-id="f90ca-305">Before you can test the value of the show variable in the query string, you have to convert it to a Boolean value.</span></span> <span data-ttu-id="f90ca-306">Che è quello che il metodo AsBool: accetta una stringa come input e lo converte in un valore booleano.</span><span class="sxs-lookup"><span data-stu-id="f90ca-306">That's what the AsBool method does — it takes a string as input and converts it to a Boolean value.</span></span> <span data-ttu-id="f90ca-307">Chiaramente, se la stringa è "true", il metodo AsBool converte il valore su true.</span><span class="sxs-lookup"><span data-stu-id="f90ca-307">Clearly, if the string is "true", the AsBool method converts that value to true.</span></span> <span data-ttu-id="f90ca-308">Se il valore della stringa è diversa, AsBool restituisce false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-308">If the value of the string is anything else, AsBool returns false.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="f90ca-309">**Tipi di dati e i metodi As()**</span><span class="sxs-lookup"><span data-stu-id="f90ca-309">**Data Types and As() Methods**</span></span>
> 
> <span data-ttu-id="f90ca-310">È stato detto solo fino a questo punto quando si crea una variabile, utilizzare la funzione VAR (parola chiave).</span><span class="sxs-lookup"><span data-stu-id="f90ca-310">We've only said so far that when you create a variable, you use the keyword var.</span></span> <span data-ttu-id="f90ca-311">Che non però l'intera sequenza.</span><span class="sxs-lookup"><span data-stu-id="f90ca-311">That's not the entire story, though.</span></span> <span data-ttu-id="f90ca-312">Per modificare i valori, per aggiungere i numeri, o concatenare stringhe, o confrontare le date o di test per true o false, c# deve utilizzare a una rappresentazione interna appropriata del valore.</span><span class="sxs-lookup"><span data-stu-id="f90ca-312">In order to manipulate values — to add numbers, or concatenate strings, or compare dates, or test for true/false — C# has to work with an appropriate internal representation of the value.</span></span> <span data-ttu-id="f90ca-313">C# può *in genere* scoprire quale deve essere tale rappresentazione (vale a dire, cosa *tipo* i dati sono) in base alle operazioni eseguite con i valori.</span><span class="sxs-lookup"><span data-stu-id="f90ca-313">C# can *usually* figure out what that representation should be (that is, what *type* the data is) based on what you're doing with the values.</span></span> <span data-ttu-id="f90ca-314">E quindi, tuttavia, non può che.</span><span class="sxs-lookup"><span data-stu-id="f90ca-314">Now and then, though, it can't do that.</span></span> <span data-ttu-id="f90ca-315">In caso contrario, è necessario aiutare indicando come c# devono rappresentare i dati in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="f90ca-315">If not, you have to help out by explicitly indicating how C# should represent the data.</span></span> <span data-ttu-id="f90ca-316">Esegue il metodo AsBool, indica in c# che un valore stringa "true" o "false" deve essere considerato come un valore booleano.</span><span class="sxs-lookup"><span data-stu-id="f90ca-316">The AsBool method does that — it tells C# that a string value of "true" or "false" should be treated as a Boolean value.</span></span> <span data-ttu-id="f90ca-317">Sono disponibili metodi simili per rappresentare le stringhe come altri tipi, nonché come AsInt (considera come un numero intero), AsDateTime (considera come una data/ora), AsFloat (considera come numero a virgola mobile) e così via.</span><span class="sxs-lookup"><span data-stu-id="f90ca-317">Similar methods exist to represent strings as other types as well, like AsInt (treat as an integer), AsDateTime (treat as a date/time), AsFloat (treat as a floating-point number), and so on.</span></span> <span data-ttu-id="f90ca-318">Quando si utilizza queste informazioni come metodi (), c# non può rappresentare il valore di stringa come richiesto, verrà visualizzato un errore.</span><span class="sxs-lookup"><span data-stu-id="f90ca-318">When you use these As( ) methods, if C# can't represent the string value as requested, you'll see an error.</span></span>


<span data-ttu-id="f90ca-319">Nel markup della pagina, rimuovere o impostare come commento l'elemento (qui viene visualizzato come commento):</span><span class="sxs-lookup"><span data-stu-id="f90ca-319">In the markup of the page, remove or comment out this element (here it's shown commented out):</span></span>

[!code-html[Main](intro-to-web-pages-programming/samples/sample12.html)]

<span data-ttu-id="f90ca-320">Destra in cui è stato rimosso o impostate come commento il testo, aggiungere quanto segue:</span><span class="sxs-lookup"><span data-stu-id="f90ca-320">Right where you removed or commented out that text, add the following:</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample13.cshtml)]

<span data-ttu-id="f90ca-321">Il se test indica che se la variabile showMessage è true, il rendering di un &lt;p&gt; elemento con il valore della variabile di messaggio.</span><span class="sxs-lookup"><span data-stu-id="f90ca-321">The if test says that if the showMessage variable is true, render a &lt;p&gt; element with the value of the message variable.</span></span>

### <a name="summary-of-your-conditional-logic"></a><span data-ttu-id="f90ca-322">Riepilogo della logica condizionale</span><span class="sxs-lookup"><span data-stu-id="f90ca-322">Summary of your conditional logic</span></span>

<span data-ttu-id="f90ca-323">Nel caso in cui non si è certi interamente di le modifiche appena apportate, di seguito è riportato un riepilogo.</span><span class="sxs-lookup"><span data-stu-id="f90ca-323">In case you're not entirely sure of what you've just done, here's a summary.</span></span>

- <span data-ttu-id="f90ca-324">La variabile di messaggio viene inizializzata su una stringa predefinita ("This is la prima volta...").</span><span class="sxs-lookup"><span data-stu-id="f90ca-324">The message variable is initialized to a default string ("This is the first time ...").</span></span>
- <span data-ttu-id="f90ca-325">Se la richiesta della pagina è il risultato dell'inoltro (post), il valore del messaggio viene modificato in "dopo avere inviato..."</span><span class="sxs-lookup"><span data-stu-id="f90ca-325">If the page request is the result of a submit (post), the value of message is changed to "Now you've submitted ..."</span></span>
- <span data-ttu-id="f90ca-326">La variabile showMessage viene inizializzata su false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-326">The showMessage variable is initialized to false.</span></span>
- <span data-ttu-id="f90ca-327">Se la stringa di query contiene? Mostra = true, la variabile showMessage è impostata su true.</span><span class="sxs-lookup"><span data-stu-id="f90ca-327">If the query string contains ?show=true, the showMessage variable is set to true.</span></span>
- <span data-ttu-id="f90ca-328">Nel codice, se è true, showMessage un &lt;p&gt; elemento viene eseguito il rendering che mostra il valore del messaggio.</span><span class="sxs-lookup"><span data-stu-id="f90ca-328">In the markup, if showMessage is true, a &lt;p&gt; element is rendered that shows the value of message.</span></span> <span data-ttu-id="f90ca-329">(Se showMessage è false, non viene eseguito il rendering a questo punto nel codice.)</span><span class="sxs-lookup"><span data-stu-id="f90ca-329">(If showMessage is false, nothing is rendered at that point in the markup.)</span></span>
- <span data-ttu-id="f90ca-330">Nel codice, se la richiesta è una richiesta post, un &lt;p&gt; elemento viene eseguito il rendering che visualizza la data e ora.</span><span class="sxs-lookup"><span data-stu-id="f90ca-330">In the markup, if the request is a post, a &lt;p&gt; element is rendered that displays the date and time.</span></span>

<span data-ttu-id="f90ca-331">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-331">Run the page.</span></span> <span data-ttu-id="f90ca-332">Nessun messaggio, infatti showMessage è false, nel markup if(showMessage) test restituisce false.</span><span class="sxs-lookup"><span data-stu-id="f90ca-332">There's no message, because showMessage is false, so in the markup the if(showMessage) test returns false.</span></span>

<span data-ttu-id="f90ca-333">Fare clic su **Invia**.</span><span class="sxs-lookup"><span data-stu-id="f90ca-333">Click **Submit**.</span></span> <span data-ttu-id="f90ca-334">Viene visualizzato, la data e ora, ma ancora alcun messaggio.</span><span class="sxs-lookup"><span data-stu-id="f90ca-334">You see the date and time, but still no message.</span></span>

<span data-ttu-id="f90ca-335">Nel browser, passare alla casella URL e aggiungere quanto segue alla fine dell'URL:? Mostra = true e quindi premere INVIO.</span><span class="sxs-lookup"><span data-stu-id="f90ca-335">In your browser, go to the URL box and add the following to the end of the URL: ?show=true and then press Enter.</span></span>

![Pagina 'Test Razor 2' nel browser con una stringa di query](intro-to-web-pages-programming/_static/image5.png)

<span data-ttu-id="f90ca-337">La pagina viene visualizzata nuovamente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-337">The page is displayed again.</span></span> <span data-ttu-id="f90ca-338">(Perché l'URL è stata modificata, questa è una nuova richiesta, non un invio). Fare clic su **Invia** nuovamente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-338">(Because you changed the URL, this is a new request, not a submit.) Click **Submit** again.</span></span> <span data-ttu-id="f90ca-339">Il messaggio viene visualizzato di nuovo come data e ora.</span><span class="sxs-lookup"><span data-stu-id="f90ca-339">The message is displayed again, as is the date and time.</span></span>

![Pagina 'Test 2 Razor' dopo l'invio quando vi è una stringa di query](intro-to-web-pages-programming/_static/image6.png)

<span data-ttu-id="f90ca-341">Nell'URL, modificare? Mostra = true per? Mostra = false e premere INVIO.</span><span class="sxs-lookup"><span data-stu-id="f90ca-341">In the URL, change ?show=true to ?show=false and press Enter.</span></span> <span data-ttu-id="f90ca-342">Inviare di nuovo la pagina.</span><span class="sxs-lookup"><span data-stu-id="f90ca-342">Submit the page again.</span></span> <span data-ttu-id="f90ca-343">La pagina è di nuovo l'avvio, ovvero nessun messaggio.</span><span class="sxs-lookup"><span data-stu-id="f90ca-343">The page is back to how you started — no message.</span></span>

<span data-ttu-id="f90ca-344">Come notato in precedenza, la logica di questo esempio è un complicato.</span><span class="sxs-lookup"><span data-stu-id="f90ca-344">As noted earlier, the logic of this example is a little contrived.</span></span> <span data-ttu-id="f90ca-345">Tuttavia, se viene visualizzata con molte delle pagine e richiederà uno o più moduli di cui è stato illustrato di seguito.</span><span class="sxs-lookup"><span data-stu-id="f90ca-345">However, if is going to come up in many of your pages, and it will take one or more of the forms you've seen here.</span></span>

## <a name="installing-a-helper-displaying-a-gravatar-image"></a><span data-ttu-id="f90ca-346">L'installazione di un Helper (visualizzazione di un'immagine Gravatar)</span><span class="sxs-lookup"><span data-stu-id="f90ca-346">Installing a Helper (Displaying a Gravatar Image)</span></span>

<span data-ttu-id="f90ca-347">Alcune attività che gli utenti desiderano spesso nelle pagine web richiedono una grande quantità di codice o richiedono la conoscenza aggiuntivo.</span><span class="sxs-lookup"><span data-stu-id="f90ca-347">Some tasks that people often want to do on web pages require a lot of code or require extra knowledge.</span></span> <span data-ttu-id="f90ca-348">Esempi: visualizzazione di un grafico per i dati. Inserisce un Facebook "pulsante mi piace" in una pagina. Invio messaggio di posta elettronica dal sito Web; il ritaglio e ridimensionamento di immagini. utilizzo di PayPal per il sito.</span><span class="sxs-lookup"><span data-stu-id="f90ca-348">Examples: displaying a chart for data; putting a Facebook "Like" button on a page; sending email from your website; cropping or resizing images; using PayPal for your site.</span></span> <span data-ttu-id="f90ca-349">Per consentire un facile eseguire questi tipi di elementi, le pagine Web ASP.NET consente di utilizzare *helper*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-349">To make it easy to do these kinds of things, ASP.NET Web Pages lets you use *helpers*.</span></span> <span data-ttu-id="f90ca-350">Gli helper sono componenti installabili per un sito e che consentono di eseguire attività comuni con poche righe di codice Razor.</span><span class="sxs-lookup"><span data-stu-id="f90ca-350">Helpers are components that you install for a site and that let you perform typical tasks by using just a few lines of Razor code.</span></span>

<span data-ttu-id="f90ca-351">Le pagine Web ASP.NET dispone di alcuni helper compilato.</span><span class="sxs-lookup"><span data-stu-id="f90ca-351">ASP.NET Web Pages has a few helpers built in.</span></span> <span data-ttu-id="f90ca-352">Tuttavia, molte funzioni di supporto sono disponibili nei pacchetti che sono forniti usando Gestione pacchetti NuGet (componenti aggiuntivi).</span><span class="sxs-lookup"><span data-stu-id="f90ca-352">However, many helpers are available in packages (add-ins) that are provided using the NuGet package manager.</span></span> <span data-ttu-id="f90ca-353">NuGet consente di selezionare un pacchetto da installare e quindi si occupa di tutti i dettagli dell'installazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-353">NuGet lets you select a package to install and then it takes care of all the details of the installation.</span></span>

<span data-ttu-id="f90ca-354">In questa parte dell'esercitazione, verrà installato un helper che consente di visualizzare l'immagine Gravatar ("avatar riconosciuto a livello globale").</span><span class="sxs-lookup"><span data-stu-id="f90ca-354">In this part of the tutorial, you'll install a helper that lets you display a Gravatar ("globally recognized avatar") image.</span></span> <span data-ttu-id="f90ca-355">Si apprenderà due operazioni.</span><span class="sxs-lookup"><span data-stu-id="f90ca-355">You'll learn two things.</span></span> <span data-ttu-id="f90ca-356">Uno viene illustrato come trovare e installare un file di supporto.</span><span class="sxs-lookup"><span data-stu-id="f90ca-356">One is how to find and install a helper.</span></span> <span data-ttu-id="f90ca-357">Si apprenderà inoltre come un helper semplifica eseguire un'operazione in caso contrario è necessario eseguire con una grande quantità di codice che è necessario scrivere manualmente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-357">You'll also learn how a helper makes it easy to do something you'd otherwise need to do by using a lot of code you'd have to write yourself.</span></span>

<span data-ttu-id="f90ca-358">È possibile registrare la propria Gravatar nel sito Web Gravatar al [ http://www.gravatar.com/ ](http://www.gravatar.com/), ma non è essenziale per creare un account Gravatar per eseguire questa parte dell'esercitazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-358">You can register your own Gravatar at the Gravatar website at [http://www.gravatar.com/](http://www.gravatar.com/), but it is not essential to create a Gravatar account to perform this part of the tutorial.</span></span>

<span data-ttu-id="f90ca-359">In WebMatrix, fare clic su di **NuGet** pulsante.</span><span class="sxs-lookup"><span data-stu-id="f90ca-359">In WebMatrix, click the **NuGet** button.</span></span>

![Nella finestra di dialogo Raccolta NuGet WebMatrix](intro-to-web-pages-programming/_static/image7.png)

<span data-ttu-id="f90ca-361">Verrà avviato Gestione pacchetti NuGet e visualizza i pacchetti disponibili.</span><span class="sxs-lookup"><span data-stu-id="f90ca-361">This launches the NuGet package manager and displays available packages.</span></span> <span data-ttu-id="f90ca-362">(Non tutti i pacchetti sono helper; alcuni aggiungere funzionalità a WebMatrix, alcuni modelli aggiuntivi e così via.) È possibile ottenere un messaggio di errore relativo a un'incompatibilità di versione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-362">(Not all of the packages are helpers; some add functionality to WebMatrix itself, some are additional templates, and so on.) You may get an error message about version incompatibility.</span></span> <span data-ttu-id="f90ca-363">È possibile ignorare questo messaggio di errore, fare clic su **OK** e procedere con questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-363">You can ignore this error message by clicking **OK** and proceeding with this tutorial.</span></span>

![Nella finestra di dialogo Raccolta NuGet WebMatrix](intro-to-web-pages-programming/_static/image8.png)

<span data-ttu-id="f90ca-365">Nella casella di ricerca, immettere "helper di asp.net".</span><span class="sxs-lookup"><span data-stu-id="f90ca-365">In the search box, enter "asp.net helpers".</span></span> <span data-ttu-id="f90ca-366">NuGet Mostra i pacchetti che corrispondono ai criteri di ricerca.</span><span class="sxs-lookup"><span data-stu-id="f90ca-366">NuGet shows the packages that match the search terms.</span></span>

![Raccolta NuGet in WebMatrix che mostra i pacchetti](intro-to-web-pages-programming/_static/image9.png)

<span data-ttu-id="f90ca-368">ASP.NET Web Helpers Library contiene il codice per semplificare molte attività comuni, inclusi l'utilizzo di immagini Gravatar.</span><span class="sxs-lookup"><span data-stu-id="f90ca-368">The ASP.NET Web Helpers Library contains code to simplify many common tasks, including the use of Gravatar images.</span></span> <span data-ttu-id="f90ca-369">Selezionare il **ASP.NET Web Helpers Library** pacchetto e quindi fare clic su **installare** per avviare il programma di installazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-369">Select the **ASP.NET Web Helpers Library** package and then click **Install** to launch the installer.</span></span> <span data-ttu-id="f90ca-370">Selezionare **Sì** quando viene richiesto se si desidera installare il pacchetto e accettare le condizioni per completare l'installazione.</span><span class="sxs-lookup"><span data-stu-id="f90ca-370">Select **Yes** when asked if you want to install the package, and accept the terms to complete the installation.</span></span>

<span data-ttu-id="f90ca-371">È tutto.</span><span class="sxs-lookup"><span data-stu-id="f90ca-371">That's it.</span></span> <span data-ttu-id="f90ca-372">NuGet Scarica e installa tutti gli elementi, inclusi tutti i componenti aggiuntivi che potrebbero essere necessari (*dipendenze*).</span><span class="sxs-lookup"><span data-stu-id="f90ca-372">NuGet downloads and installs everything, including any additional components that might be required (*dependencies*).</span></span>

<span data-ttu-id="f90ca-373">Se per qualche motivo, è necessario disinstallare un helper, il processo è molto simile.</span><span class="sxs-lookup"><span data-stu-id="f90ca-373">If for some reason you have to uninstall a helper, the process is very similar.</span></span> <span data-ttu-id="f90ca-374">Fare clic su di **NuGet** fare clic su di **installato** scheda e selezionare il pacchetto che si desidera disinstallare.</span><span class="sxs-lookup"><span data-stu-id="f90ca-374">Click the **NuGet** button, click the **Installed** tab, and pick the package you want to uninstall.</span></span>

## <a name="using-a-helper-in-a-page"></a><span data-ttu-id="f90ca-375">Utilizzo di un Helper in una pagina</span><span class="sxs-lookup"><span data-stu-id="f90ca-375">Using a Helper in a Page</span></span>

<span data-ttu-id="f90ca-376">Ora si userà l'helper appena installata.</span><span class="sxs-lookup"><span data-stu-id="f90ca-376">Now you'll use the helper that you just installed.</span></span> <span data-ttu-id="f90ca-377">Il processo per l'aggiunta di un helper a una pagina è simile per la maggior parte delle funzioni di supporto.</span><span class="sxs-lookup"><span data-stu-id="f90ca-377">The process for adding a helper to a page is similar for most helpers.</span></span>

<span data-ttu-id="f90ca-378">In WebMatrix, creare una pagina e il nome *GravatarTest.cshml*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-378">In WebMatrix, create a page and name it *GravatarTest.cshml*.</span></span> <span data-ttu-id="f90ca-379">(Si sta creando una pagina speciale per l'helper di test, ma è possibile utilizzare l'helper in qualsiasi pagina del sito).</span><span class="sxs-lookup"><span data-stu-id="f90ca-379">(You're creating a special page to test the helper, but you can use helpers in any page in your site.)</span></span>

<span data-ttu-id="f90ca-380">All'interno di &lt;corpo&gt; elemento, aggiungere un &lt;div&gt; elemento.</span><span class="sxs-lookup"><span data-stu-id="f90ca-380">Inside the &lt;body&gt; element, add a &lt;div&gt; element.</span></span> <span data-ttu-id="f90ca-381">All'interno di &lt;div&gt; elemento, digitare quanto segue:</span><span class="sxs-lookup"><span data-stu-id="f90ca-381">Inside the &lt;div&gt; element, type this:</span></span>

<span data-ttu-id="f90ca-382">@Gravatar.</span><span class="sxs-lookup"><span data-stu-id="f90ca-382">@Gravatar.</span></span>

<span data-ttu-id="f90ca-383">Il carattere @ è lo stesso carattere che hai usato per contrassegnare il codice Razor.</span><span class="sxs-lookup"><span data-stu-id="f90ca-383">The @ character is the same character you've been using to mark Razor code.</span></span> <span data-ttu-id="f90ca-384">**Gravatar** è l'oggetto helper che si sta lavorando.</span><span class="sxs-lookup"><span data-stu-id="f90ca-384">**Gravatar** is the helper object that you're working with.</span></span>

<span data-ttu-id="f90ca-385">Non appena si digita il punto (.), WebMatrix consente di visualizzare un elenco di *metodi* (funzioni), che rende disponibile il supporto di Gravatar:</span><span class="sxs-lookup"><span data-stu-id="f90ca-385">As soon as you type the period (.), WebMatrix displays a list of *methods* (functions) that the Gravatar helper makes available:</span></span>

![Elenco di helper elenco a discesa IntelliSense Gravatar](intro-to-web-pages-programming/_static/image10.png)

<span data-ttu-id="f90ca-387">Questa funzionalità è nota come *IntelliSense*.</span><span class="sxs-lookup"><span data-stu-id="f90ca-387">This feature is known as *IntelliSense*.</span></span> <span data-ttu-id="f90ca-388">È utile che il codice, fornendo scelte appropriato al contesto.</span><span class="sxs-lookup"><span data-stu-id="f90ca-388">It helps you code by providing context-appropriate choices.</span></span> <span data-ttu-id="f90ca-389">IntelliSense funziona con HTML, CSS, codice ASP.NET, JavaScript e altri linguaggi supportati in WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="f90ca-389">IntelliSense works with HTML, CSS, ASP.NET code, JavaScript, and other languages that are supported in WebMatrix.</span></span> <span data-ttu-id="f90ca-390">Si tratta di un'altra funzionalità che semplifica lo sviluppo di pagine web in WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="f90ca-390">It's another feature that makes it easier to develop web pages in WebMatrix.</span></span>

<span data-ttu-id="f90ca-391">Premere G sulla tastiera e vedere IntelliSense consente di trovare il metodo GetHtml.</span><span class="sxs-lookup"><span data-stu-id="f90ca-391">Press G on the keyboard, and you see that IntelliSense finds the GetHtml method.</span></span> <span data-ttu-id="f90ca-392">Premere Tab. IntelliSense consente di inserire il metodo selezionato (GetHtml) per l'utente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-392">Press Tab. IntelliSense inserts the selected method (GetHtml) for you.</span></span> <span data-ttu-id="f90ca-393">Digitare una parentesi aperta e notare che la parentesi di chiusura viene aggiunto automaticamente.</span><span class="sxs-lookup"><span data-stu-id="f90ca-393">Type an open parenthesis, and notice that the closing parenthesis is automatically added.</span></span> <span data-ttu-id="f90ca-394">Digitare l'indirizzo di posta elettronica tra virgolette tra due parentesi.</span><span class="sxs-lookup"><span data-stu-id="f90ca-394">Type your email address in quotation marks between the two parenthesis.</span></span> <span data-ttu-id="f90ca-395">Se si dispone di un account Gravatar, verrà restituito l'immagine del profilo.</span><span class="sxs-lookup"><span data-stu-id="f90ca-395">If you have a Gravatar account, your profile picture will be returned.</span></span> <span data-ttu-id="f90ca-396">Se non si dispone di un account Gravatar, un'immagine predefinita.</span><span class="sxs-lookup"><span data-stu-id="f90ca-396">If you do not have a Gravatar account, a default image is returned.</span></span> <span data-ttu-id="f90ca-397">Al termine, la riga è simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="f90ca-397">When you're done, the line looks like this:</span></span>

[!code-css[Main](intro-to-web-pages-programming/samples/sample14.css)]

<span data-ttu-id="f90ca-398">Ora è possibile visualizzare la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="f90ca-398">Now view the page in a browser.</span></span> <span data-ttu-id="f90ca-399">Verrà visualizzata l'immagine o l'immagine predefinita, a seconda se si dispone di un account Gravatar.</span><span class="sxs-lookup"><span data-stu-id="f90ca-399">Either your picture or the default image is displayed, depending on whether you have a Gravatar account.</span></span>

![Gravatar](intro-to-web-pages-programming/_static/image11.png) ![immagine predefinita](intro-to-web-pages-programming/_static/image12.png)

<span data-ttu-id="f90ca-402">Per ottenere un'idea di cosa sta effettuando l'helper per l'utente, visualizzare l'origine della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="f90ca-402">To get an idea of what the helper is doing for you, view the source of the page in the browser.</span></span> <span data-ttu-id="f90ca-403">Insieme a HTML presenti nella pagina, vedrai un elemento immagine che include un identificatore.</span><span class="sxs-lookup"><span data-stu-id="f90ca-403">Along with the HTML that you had in your page, you see an image element that includes an identifier.</span></span> <span data-ttu-id="f90ca-404">Si tratta di codice che il supporto di cui è stato eseguito il rendering nella pagina nella posizione in cui era @Gravatar.GetHtml.</span><span class="sxs-lookup"><span data-stu-id="f90ca-404">This is code that the helper rendered into the page at the place where you had @Gravatar.GetHtml.</span></span> <span data-ttu-id="f90ca-405">Il supporto ha acquisito le informazioni fornite e ha generato il codice che comunica direttamente con Gravatar per ottenere nuovamente l'immagine corretta per l'account specificato.</span><span class="sxs-lookup"><span data-stu-id="f90ca-405">The helper took the information you provided and generated the code that talks directly to Gravatar in order to get back the correct image for supplied account.</span></span>

<span data-ttu-id="f90ca-406">Il metodo GetHtml consente inoltre di personalizzare l'immagine fornendo altri parametri.</span><span class="sxs-lookup"><span data-stu-id="f90ca-406">The GetHtml method also enables you to customize the image by providing other parameters.</span></span> <span data-ttu-id="f90ca-407">Il codice seguente viene illustrato come richiedere un'immagine con una larghezza e l'altezza di 40 pixel e che usa un'immagine predefinita specificata denominata **wavatar** se l'account specificato non esiste.</span><span class="sxs-lookup"><span data-stu-id="f90ca-407">The following code shows how to request an image has a width and height of 40 pixels, and uses a specified default image named **wavatar** if the specified account does not exist.</span></span>

[!code-javascript[Main](intro-to-web-pages-programming/samples/sample15.js)]

<span data-ttu-id="f90ca-408">Questo codice produce un risultato simile il risultato seguente (l'immagine predefinita in modo casuale potrà variare).</span><span class="sxs-lookup"><span data-stu-id="f90ca-408">This code produces something like the following result (the default image will randomly vary).</span></span>

![](intro-to-web-pages-programming/_static/image13.png)

## <a name="coming-up-next"></a><span data-ttu-id="f90ca-409">Esercitazione seguente</span><span class="sxs-lookup"><span data-stu-id="f90ca-409">Coming Up Next</span></span>

<span data-ttu-id="f90ca-410">Per mantenere questa breve esercitazione, era necessario concentrarsi solo alcune nozioni di base.</span><span class="sxs-lookup"><span data-stu-id="f90ca-410">To keep this tutorial short, we had to focus on only a few basics.</span></span> <span data-ttu-id="f90ca-411">Ovviamente, è presente un *molti* ulteriori Razor e c#.</span><span class="sxs-lookup"><span data-stu-id="f90ca-411">Naturally, there's a *lot* more to Razor and C#.</span></span> <span data-ttu-id="f90ca-412">Si apprenderà come esaminare le esercitazioni.</span><span class="sxs-lookup"><span data-stu-id="f90ca-412">You'll learn more as you go through these tutorials.</span></span> <span data-ttu-id="f90ca-413">Se si è interessati a ottenere ulteriori informazioni su aspetti della programmazione relativi Razor e c# ora, è possibile leggere qui l'introduzione approfondita: [Introduzione alla programmazione Web ASP.NET utilizzando la sintassi Razor](https://go.microsoft.com/fwlink/?LinkID=202890).</span><span class="sxs-lookup"><span data-stu-id="f90ca-413">If you're interested in learning more about the programming aspects of Razor and C# right now, you can read a more thorough introduction here: [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkID=202890).</span></span>

<span data-ttu-id="f90ca-414">L'esercitazione successiva presentato l'utilizzo di un database.</span><span class="sxs-lookup"><span data-stu-id="f90ca-414">The next tutorial introduces you to working with a database.</span></span> <span data-ttu-id="f90ca-415">In tale esercitazione innanzitutto creare l'applicazione di esempio che consente di elencare i film preferiti.</span><span class="sxs-lookup"><span data-stu-id="f90ca-415">In that tutorial, you'll begin creating the sample application that lets you list your favorite movies.</span></span>

## <a name="complete-listing-for-testrazor-page"></a><span data-ttu-id="f90ca-416">Elenco completo per la pagina TestRazor</span><span class="sxs-lookup"><span data-stu-id="f90ca-416">Complete Listing for TestRazor Page</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample16.cshtml)]

## <a name="complete-listing-for-testrazorpart2-page"></a><span data-ttu-id="f90ca-417">Elenco completo per la pagina TestRazorPart2</span><span class="sxs-lookup"><span data-stu-id="f90ca-417">Complete Listing for TestRazorPart2 Page</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample17.cshtml)]

## <a name="complete-listing-for-gravatartest-page"></a><span data-ttu-id="f90ca-418">Elenco completo per la pagina GravatarTest</span><span class="sxs-lookup"><span data-stu-id="f90ca-418">Complete Listing for GravatarTest Page</span></span>

[!code-cshtml[Main](intro-to-web-pages-programming/samples/sample18.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="f90ca-419">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="f90ca-419">Additional Resources</span></span>

- [<span data-ttu-id="f90ca-420">Introduzione alla programmazione Web ASP.NET con sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="f90ca-420">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- [<span data-ttu-id="f90ca-421">Helper di Twitter</span><span class="sxs-lookup"><span data-stu-id="f90ca-421">Twitter helper</span></span>](../../ui-layouts-and-themes/twitter-helper.md)

> [!div class="step-by-step"]
> <span data-ttu-id="f90ca-422">[Precedente](getting-started.md)
> [Successivo](displaying-data.md)</span><span class="sxs-lookup"><span data-stu-id="f90ca-422">[Previous](getting-started.md)
[Next](displaying-data.md)</span></span>
