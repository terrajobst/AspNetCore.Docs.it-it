---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: 'Introduzione a ASP.NET Web Pages: aggiornamento dei dati di Database | Documenti Microsoft'
author: tfitzmac
description: In questa esercitazione viene illustrato come aggiornare la voce di un database esistente (modifica) quando si utilizzano pagine Web ASP.NET (Razor). Si presuppone di aver completato la serie th...
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: e889cd27e2267a08f7b6ea708c92e35edbdd7a1a
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/06/2018
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="2140d-104">Introduzione a ASP.NET Web Pages: aggiornamento dei dati di Database</span><span class="sxs-lookup"><span data-stu-id="2140d-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="2140d-105">da [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="2140d-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="2140d-106">In questa esercitazione viene illustrato come aggiornare la voce di un database esistente (modifica) quando si utilizzano pagine Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="2140d-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="2140d-107">Si presuppone di aver completato la serie tramite [immissione di dati da utilizzare form utilizzando ASP.NET Web Pages](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="2140d-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="2140d-108">Illustra quanto segue:</span><span class="sxs-lookup"><span data-stu-id="2140d-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="2140d-109">Come selezionare un singolo record di `WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="2140d-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="2140d-110">Come leggere un singolo record da un database.</span><span class="sxs-lookup"><span data-stu-id="2140d-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="2140d-111">Modalità di precaricamento di un form con i valori del record del database.</span><span class="sxs-lookup"><span data-stu-id="2140d-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="2140d-112">Come aggiornare un record esistente in un database.</span><span class="sxs-lookup"><span data-stu-id="2140d-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="2140d-113">Come archiviare le informazioni nella pagina senza visualizzarla.</span><span class="sxs-lookup"><span data-stu-id="2140d-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="2140d-114">Come usare un campo nascosto per archiviare le informazioni.</span><span class="sxs-lookup"><span data-stu-id="2140d-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="2140d-115">Funzionalità/tecnologie descritte:</span><span class="sxs-lookup"><span data-stu-id="2140d-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="2140d-116">Il `WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="2140d-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="2140d-117">L'istruzione SQL `Update` comando.</span><span class="sxs-lookup"><span data-stu-id="2140d-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="2140d-118">Metodo `Database.Execute`.</span><span class="sxs-lookup"><span data-stu-id="2140d-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="2140d-119">Campi nascosti (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="2140d-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="2140d-120">Cosa Compilerai</span><span class="sxs-lookup"><span data-stu-id="2140d-120">What You'll Build</span></span>

<span data-ttu-id="2140d-121">Nell'esercitazione precedente, è stato descritto come aggiungere un record a un database.</span><span class="sxs-lookup"><span data-stu-id="2140d-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="2140d-122">In questo caso, si apprenderà come visualizzare un record per la modifica.</span><span class="sxs-lookup"><span data-stu-id="2140d-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="2140d-123">Nel *filmati* pagina verrà aggiornata la `WebGrid` helper in modo che venga visualizzato un **modifica** collegamento accanto a ogni filmato:</span><span class="sxs-lookup"><span data-stu-id="2140d-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid visualizzare contenente un collegamento di 'Modifica' per ogni film](updating-data/_static/image1.png)

<span data-ttu-id="2140d-125">Quando si sceglie il **modifica** collegamento, consente di visualizzare una pagina diversa, in cui le informazioni di film sono già in un formato:</span><span class="sxs-lookup"><span data-stu-id="2140d-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Modifica pagina filmato che mostra film da modificare](updating-data/_static/image2.png)

<span data-ttu-id="2140d-127">È possibile modificare uno dei valori.</span><span class="sxs-lookup"><span data-stu-id="2140d-127">You can change any of the values.</span></span> <span data-ttu-id="2140d-128">Quando si inviano le modifiche, il codice nella pagina Aggiorna il database e consente di tornare all'elenco di film.</span><span class="sxs-lookup"><span data-stu-id="2140d-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="2140d-129">Questa parte del processo funziona quasi esattamente come il *AddMovie.cshtml* pagina creata nell'esercitazione precedente, pertanto la maggior parte di questa esercitazione risulteranno familiare.</span><span class="sxs-lookup"><span data-stu-id="2140d-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="2140d-130">Esistono diversi modi, è possibile implementare un modo per modificare un filmato singoli.</span><span class="sxs-lookup"><span data-stu-id="2140d-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="2140d-131">L'approccio indicato è stato scelto perché è facile da implementare e facile da comprendere.</span><span class="sxs-lookup"><span data-stu-id="2140d-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="2140d-132">Aggiunta di un collegamento di modifica per l'elenco di film</span><span class="sxs-lookup"><span data-stu-id="2140d-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="2140d-133">Per iniziare, si aggiornerà il *filmati* pagina in modo che ogni film anche l'elenco contiene un **modifica** collegamento.</span><span class="sxs-lookup"><span data-stu-id="2140d-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="2140d-134">Aprire il *Movies.cshtml* file.</span><span class="sxs-lookup"><span data-stu-id="2140d-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="2140d-135">Nel corpo della pagina, modificare il `WebGrid` markup aggiungendo una colonna.</span><span class="sxs-lookup"><span data-stu-id="2140d-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="2140d-136">Di seguito è riportato il codice modificato:</span><span class="sxs-lookup"><span data-stu-id="2140d-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="2140d-137">Questa è la nuova colonna:</span><span class="sxs-lookup"><span data-stu-id="2140d-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="2140d-138">Il punto di questa colonna è di visualizzare un collegamento (`<a>` elemento) con il testo "Modifica".</span><span class="sxs-lookup"><span data-stu-id="2140d-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="2140d-139">Siamo dopo consiste nel creare un collegamento che è simile al seguente quando si esegue la pagina, con la `id` valore diverso per ogni film:</span><span class="sxs-lookup"><span data-stu-id="2140d-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="2140d-140">Questo collegamento verrà richiamato una pagina denominata *EditMovie*, e passa la stringa di query `?id=7` a tale pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="2140d-141">La sintassi per la nuova colonna potrebbe essere un po' complessa, ma solo perché riunisce diversi elementi.</span><span class="sxs-lookup"><span data-stu-id="2140d-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="2140d-142">Ogni singolo elemento è semplice.</span><span class="sxs-lookup"><span data-stu-id="2140d-142">Each individual element is straightforward.</span></span> <span data-ttu-id="2140d-143">Se concentrarsi solo il `<a>` elemento, viene visualizzato questo codice:</span><span class="sxs-lookup"><span data-stu-id="2140d-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="2140d-144">Alcune informazioni generali sul funzionamento della griglia: nella griglia vengono visualizzate le righe, una per ogni record di database, e visualizza le colonne per ogni campo nel record del database.</span><span class="sxs-lookup"><span data-stu-id="2140d-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="2140d-145">Mentre viene creato ogni riga della griglia, il `item` oggetto contiene i record del database (elemento) per la riga.</span><span class="sxs-lookup"><span data-stu-id="2140d-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="2140d-146">Questa disposizione consente nel codice per ottenere i dati per la riga.</span><span class="sxs-lookup"><span data-stu-id="2140d-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="2140d-147">È ciò che viene visualizzato di seguito: l'espressione `item.ID` Ottiene il valore ID dell'elemento del database corrente.</span><span class="sxs-lookup"><span data-stu-id="2140d-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="2140d-148">È possibile ottenere uno dei valori del database (titolo, genere o anno) allo stesso modo usando `item.Title`, `item.Genre`, o `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="2140d-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="2140d-149">L'espressione `"~/EditMovie?id=@item.ID` combina la parte hardcoded dell'URL di destinazione (`~/EditMovie?id=`) con questo ID derivata in modo dinamico.</span><span class="sxs-lookup"><span data-stu-id="2140d-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="2140d-150">(Si è visto il `~` operatore nell'esercitazione precedente; è un operatore di ASP.NET che rappresenta la radice del sito Web corrente.)</span><span class="sxs-lookup"><span data-stu-id="2140d-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="2140d-151">Il risultato è che questa parte del markup nella colonna semplicemente genera un risultato come il seguente codice in fase di esecuzione:</span><span class="sxs-lookup"><span data-stu-id="2140d-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="2140d-152">Ovviamente, il valore effettivo del `id` sarà diverso per ogni riga.</span><span class="sxs-lookup"><span data-stu-id="2140d-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="2140d-153">Creazione di una visualizzazione personalizzata per una colonna della griglia</span><span class="sxs-lookup"><span data-stu-id="2140d-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="2140d-154">Eseguire il backup ora per la colonna della griglia.</span><span class="sxs-lookup"><span data-stu-id="2140d-154">Now back to the grid column.</span></span> <span data-ttu-id="2140d-155">Le tre colonne in origine era la griglia visualizzata solo i valori dei dati (titolo, genre e l'anno).</span><span class="sxs-lookup"><span data-stu-id="2140d-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="2140d-156">Questa visualizzazione è specificato, passando il nome della colonna di database &mdash; , ad esempio, `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="2140d-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="2140d-157">Questa nuova **modifica** colonna di collegamento è diverso.</span><span class="sxs-lookup"><span data-stu-id="2140d-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="2140d-158">Anziché specificare un nome di colonna, si passa un `format` parametro.</span><span class="sxs-lookup"><span data-stu-id="2140d-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="2140d-159">Questo parametro consente di definire markup che il `WebGrid` helper verrà eseguito il rendering con il `item` valore per visualizzare i dati della colonna in grassetto o verde o nel formato che si desidera.</span><span class="sxs-lookup"><span data-stu-id="2140d-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="2140d-160">Ad esempio, se si desidera che il titolo da visualizzare in grassetto, è possibile creare una colonna simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="2140d-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="2140d-161">(I vari `@` caratteri presenti il `format` proprietà contrassegnare la transizione tra markup e un valore di codice.)</span><span class="sxs-lookup"><span data-stu-id="2140d-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="2140d-162">Una volta che si conoscono il `format` proprietà, è più facile da comprendere come il nuovo **modifica** viene sommata colonna collegamento:</span><span class="sxs-lookup"><span data-stu-id="2140d-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="2140d-163">La colonna include *solo* di markup che esegue il rendering del collegamento, nonché alcune informazioni (ID) che è stato estratto dal record del database per la riga.</span><span class="sxs-lookup"><span data-stu-id="2140d-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP]
> 
> <span data-ttu-id="2140d-164">**I parametri denominati e i parametri posizionali per un metodo**</span><span class="sxs-lookup"><span data-stu-id="2140d-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="2140d-165">Numero di volte dopo aver chiamato un metodo e passare i parametri, semplicemente elencati i valori dei parametri separati da virgole.</span><span class="sxs-lookup"><span data-stu-id="2140d-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="2140d-166">Di seguito sono riportati alcuni esempi:</span><span class="sxs-lookup"><span data-stu-id="2140d-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="2140d-167">È non segnalare il problema quando si è visto prima di tutto il codice, ma in ogni caso, si sta passando i parametri ai metodi in un ordine specifico &mdash; in particolare, l'ordine in cui i parametri sono definiti in tale metodo.</span><span class="sxs-lookup"><span data-stu-id="2140d-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="2140d-168">Per `db.Execute` e `Validation.RequireFields`, se confuse l'ordine dei valori passati, verrà visualizzato un messaggio di errore quando la pagina viene eseguita, o almeno alcuni risultati imprevisti.</span><span class="sxs-lookup"><span data-stu-id="2140d-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="2140d-169">Chiaramente, è necessario conoscere l'ordine per passare i parametri in.</span><span class="sxs-lookup"><span data-stu-id="2140d-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="2140d-170">(In WebMatrix, IntelliSense consente di individuare il nome, tipo e ordine dei parametri di informazioni.)</span><span class="sxs-lookup"><span data-stu-id="2140d-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="2140d-171">In alternativa al passaggio di valori in ordine, è possibile utilizzare *parametri denominati*.</span><span class="sxs-lookup"><span data-stu-id="2140d-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="2140d-172">(Il passaggio di parametri nell'ordine è noto come utilizzando *parametri posizionali*.) Per i parametri denominati, includere in modo esplicito il nome del parametro quando si passa il relativo valore.</span><span class="sxs-lookup"><span data-stu-id="2140d-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="2140d-173">Utilizzati parametri denominati già un numero di volte in queste esercitazioni.</span><span class="sxs-lookup"><span data-stu-id="2140d-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="2140d-174">Ad esempio:</span><span class="sxs-lookup"><span data-stu-id="2140d-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="2140d-175">e</span><span class="sxs-lookup"><span data-stu-id="2140d-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="2140d-176">I parametri denominati sono utili per un paio di situazioni, soprattutto quando un metodo accetta un numero di parametri.</span><span class="sxs-lookup"><span data-stu-id="2140d-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="2140d-177">Uno è quando si desidera passare solo uno o due parametri, ma non i valori che si desidera passare tra le posizioni prima nell'elenco di parametri.</span><span class="sxs-lookup"><span data-stu-id="2140d-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="2140d-178">Anche quando si desidera rendere il codice più leggibile, passando i parametri nell'ordine in cui più appropriato per l'utente.</span><span class="sxs-lookup"><span data-stu-id="2140d-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="2140d-179">Per utilizzare parametri denominati, ovviamente, è necessario conoscere i nomi dei parametri.</span><span class="sxs-lookup"><span data-stu-id="2140d-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="2140d-180">WebMatrix IntelliSense può *Mostra* per i nomi, ma non è attualmente compilare i campi per l'utente.</span><span class="sxs-lookup"><span data-stu-id="2140d-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="2140d-181">Creazione pagina di modifica</span><span class="sxs-lookup"><span data-stu-id="2140d-181">Creating the Edit Page</span></span>

<span data-ttu-id="2140d-182">Ora è possibile creare il *EditMovie* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="2140d-183">Quando gli utenti fanno clic il **modifica** collegamento, essi termine, verrà visualizzata in questa pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="2140d-184">Creare una pagina denominata *EditMovie.cshtml* e sostituire quello presente nel file con il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="2140d-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="2140d-185">In questo markup e il codice è simile a quanto è presente nel *AddMovie* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="2140d-186">È una piccola differenza nel testo del pulsante Invia.</span><span class="sxs-lookup"><span data-stu-id="2140d-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="2140d-187">Come con la *AddMovie* pagina, è presente un `Html.ValidationSummary` chiamata che consente di visualizzare se sono presenti errori di convalida.</span><span class="sxs-lookup"><span data-stu-id="2140d-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="2140d-188">Questa fase ci stiamo escludendo le chiamate a `Validation.Message`, dal momento che gli errori verranno visualizzati nel riepilogo di convalida.</span><span class="sxs-lookup"><span data-stu-id="2140d-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="2140d-189">Come indicato nell'esercitazione precedente, è possibile utilizzare il riepilogo di convalida e i singoli messaggi di errore in varie combinazioni.</span><span class="sxs-lookup"><span data-stu-id="2140d-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="2140d-190">Nuovo notare che il `method` attributo del `<form>` è impostato su `post`.</span><span class="sxs-lookup"><span data-stu-id="2140d-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="2140d-191">Come con la *AddMovie.cshtml* pagina, questa pagina apporta modifiche al database.</span><span class="sxs-lookup"><span data-stu-id="2140d-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="2140d-192">Pertanto, questo modulo è necessario eseguire un `POST` operazione.</span><span class="sxs-lookup"><span data-stu-id="2140d-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="2140d-193">(Per ulteriori informazioni sulla differenza tra `GET` e `POST` operazioni, vedere il [sicurezza verbo HTTP GET e POST](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) barra laterale nell'esercitazione di form HTML.)</span><span class="sxs-lookup"><span data-stu-id="2140d-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="2140d-194">Come osservato in un'esercitazione precedente, il `value` vengono impostati gli attributi delle caselle di testo con codice Razor per precaricare li.</span><span class="sxs-lookup"><span data-stu-id="2140d-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="2140d-195">Questa volta, tuttavia, si usa variabili come `title` e `genre` per l'attività anziché `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="2140d-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="2140d-196">Come prima, in questo markup verrà precaricare i valori della casella di testo con i valori di film.</span><span class="sxs-lookup"><span data-stu-id="2140d-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="2140d-197">Verrà visualizzato in un momento perché è utile usare le variabili di questo momento anziché il `Request` oggetto.</span><span class="sxs-lookup"><span data-stu-id="2140d-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="2140d-198">È inoltre disponibile un `<input type="hidden">` elemento in questa pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="2140d-199">Questo elemento archivia l'ID film senza renderlo visibile nella pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="2140d-200">L'ID viene inizialmente passato alla pagina, utilizzando un valore di stringa di query (`?id=7` o simili nell'URL).</span><span class="sxs-lookup"><span data-stu-id="2140d-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="2140d-201">Inserisce il valore ID in un campo nascosto, è possibile assicurarsi che sia disponibile quando il form viene inviato, anche se non è più necessario l'accesso all'URL originale è stata richiamata con la pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="2140d-202">A differenza di *AddMovie* pagina, il codice per il *EditMovie* pagina contiene due funzioni distinte.</span><span class="sxs-lookup"><span data-stu-id="2140d-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="2140d-203">La prima funzione è che quando viene visualizzata la pagina per la prima volta (e *solo* quindi), il codice ottiene l'ID film dalla stringa di query.</span><span class="sxs-lookup"><span data-stu-id="2140d-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="2140d-204">Il codice quindi utilizza l'ID per leggere il film corrispondente all'esterno del database e visualizzare (Precarica), nelle caselle di testo.</span><span class="sxs-lookup"><span data-stu-id="2140d-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="2140d-205">La seconda funzione è che quando l'utente sceglie il **Invia modifiche** pulsante, il codice deve leggere i valori delle caselle di testo e convalidarli.</span><span class="sxs-lookup"><span data-stu-id="2140d-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="2140d-206">Il codice è inoltre necessario aggiornare l'elemento di database con i nuovi valori.</span><span class="sxs-lookup"><span data-stu-id="2140d-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="2140d-207">Questa tecnica è analoga all'aggiunta di un record, come specificato nella *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="2140d-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="2140d-208">Aggiungere il codice per leggere un singolo filmato</span><span class="sxs-lookup"><span data-stu-id="2140d-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="2140d-209">Per eseguire la prima funzione, aggiungere questo codice nella parte superiore della pagina:</span><span class="sxs-lookup"><span data-stu-id="2140d-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="2140d-210">La maggior parte di questo codice si trova all'interno di un blocco che inizia `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="2140d-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="2140d-211">Il `!` operatore significa "not", che significa che l'espressione *se la richiesta non è un inoltro post*, ovvero un modo indiretto indicando *se la richiesta è la prima volta che questa pagina è stata eseguita*.</span><span class="sxs-lookup"><span data-stu-id="2140d-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="2140d-212">Come notato in precedenza, questo codice deve essere eseguito *solo* alla prima esecuzione della pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="2140d-213">Se si non racchiudere il codice in `if(!IsPost)`, verrà eseguito ogni volta che la pagina viene richiamata, se la prima volta oppure in risposta a un pulsante fare clic su.</span><span class="sxs-lookup"><span data-stu-id="2140d-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="2140d-214">Si noti che il codice include un `else` bloccare questo momento.</span><span class="sxs-lookup"><span data-stu-id="2140d-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="2140d-215">Come indicato quando è stata introdotta `if` blocchi, a volte si desidera eseguire codice alternativo se la condizione che si sta testando non è true.</span><span class="sxs-lookup"><span data-stu-id="2140d-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="2140d-216">Ovvero in questo caso.</span><span class="sxs-lookup"><span data-stu-id="2140d-216">That's the case here.</span></span> <span data-ttu-id="2140d-217">Se la condizione supera (ovvero, se l'ID passato alla pagina è ok), leggere una riga dal database.</span><span class="sxs-lookup"><span data-stu-id="2140d-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="2140d-218">Tuttavia, se non soddisfa la condizione, il `else` blocco viene eseguito e il codice imposta un messaggio di errore.</span><span class="sxs-lookup"><span data-stu-id="2140d-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="2140d-219">Convalida di un valore passato alla pagina</span><span class="sxs-lookup"><span data-stu-id="2140d-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="2140d-220">Il codice utilizza `Request.QueryString["id"]` per ottenere l'ID che viene passato alla pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="2140d-221">Il codice garantisce che in realtà è stato passato un valore per l'ID.</span><span class="sxs-lookup"><span data-stu-id="2140d-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="2140d-222">Se è stato passato alcun valore, il codice imposta un errore di convalida.</span><span class="sxs-lookup"><span data-stu-id="2140d-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="2140d-223">Questo codice viene illustrato un modo diverso per convalidare le informazioni.</span><span class="sxs-lookup"><span data-stu-id="2140d-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="2140d-224">Nell'esercitazione precedente, si è lavorato con il `Validation` helper.</span><span class="sxs-lookup"><span data-stu-id="2140d-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="2140d-225">La registrazione di campi da convalidare, ASP.NET automaticamente e la convalida ha visualizzati errori tramite `Html.ValidationMessage` e `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="2140d-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="2140d-226">In questo caso, tuttavia, si effettua non effettivamente la convalida dell'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="2140d-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="2140d-227">Al contrario, si effettua la convalida di un valore che è stato passato alla pagina da un' posizione.</span><span class="sxs-lookup"><span data-stu-id="2140d-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="2140d-228">Il `Validation` helper che non esegue automaticamente.</span><span class="sxs-lookup"><span data-stu-id="2140d-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="2140d-229">Pertanto, per verificare il valore manualmente, con `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="2140d-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="2140d-230">Se si verifica un problema, è possibile visualizzare l'errore utilizzando `Html.ValidationSummary`, come è stato eseguito con il `Validation` helper.</span><span class="sxs-lookup"><span data-stu-id="2140d-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="2140d-231">A tale scopo, si chiama `Validation.AddFormError` e passarlo a un messaggio da visualizzare.</span><span class="sxs-lookup"><span data-stu-id="2140d-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="2140d-232">`Validation.AddFormError` è un metodo incorporato che consente di definire messaggi personalizzati che strettamente si ha già familiarità con il sistema di convalida.</span><span class="sxs-lookup"><span data-stu-id="2140d-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="2140d-233">(Più avanti in questa esercitazione verranno fornite informazioni rendere un po' più affidabile questo processo di convalida.)</span><span class="sxs-lookup"><span data-stu-id="2140d-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="2140d-234">Dopo aver apportato che vi sia un ID per il film, il codice legge il database, cercando solo un elemento singolo database.</span><span class="sxs-lookup"><span data-stu-id="2140d-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="2140d-235">(Si probabilmente notato il modello generale per le operazioni di database: aprire il database, definire un'istruzione SQL ed eseguire l'istruzione.) Questa volta, l'istruzione SQL `Select` istruzione include `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="2140d-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="2140d-236">Poiché l'ID è univoco, può essere restituito un solo record.</span><span class="sxs-lookup"><span data-stu-id="2140d-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="2140d-237">La query viene eseguita tramite `db.QuerySingle` (non `db.Query`, come è stato utilizzato per l'elenco di film), e il codice inserisce il risultato nella `row` variabile.</span><span class="sxs-lookup"><span data-stu-id="2140d-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="2140d-238">Il nome `row` è arbitrario; puoi le variabili di qualsiasi nome desiderato.</span><span class="sxs-lookup"><span data-stu-id="2140d-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="2140d-239">Le variabili inizializzate nella parte superiore vengono compilate con i dettagli del film in modo che questi valori possono essere visualizzati nelle caselle di testo.</span><span class="sxs-lookup"><span data-stu-id="2140d-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="2140d-240">Test della pagina di modifica (finora)</span><span class="sxs-lookup"><span data-stu-id="2140d-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="2140d-241">Se si desidera verificare la pagina, eseguire il *filmati* pagina ora e fare clic su un **modifica** collegamento accanto a qualsiasi film.</span><span class="sxs-lookup"><span data-stu-id="2140d-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="2140d-242">Verrà visualizzato il *EditMovie* pagina con i dettagli specificati per il film selezionato:</span><span class="sxs-lookup"><span data-stu-id="2140d-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Modifica pagina filmato che mostra film da modificare](updating-data/_static/image3.png)

<span data-ttu-id="2140d-244">Si noti che l'URL della pagina include un output simile `?id=10` (o un altro numero).</span><span class="sxs-lookup"><span data-stu-id="2140d-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="2140d-245">Finora è stato testato che **modifica** di collegamenti nel *film* pagina lavoro, che la pagina viene letta l'ID dalla stringa di query, e che il database di eseguire una query per ottenere un record singolo filmato sia funzionante.</span><span class="sxs-lookup"><span data-stu-id="2140d-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="2140d-246">È possibile modificare le informazioni di film, ma non accade nulla quando si fa clic **Invia modifiche**.</span><span class="sxs-lookup"><span data-stu-id="2140d-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="2140d-247">Aggiungere il codice per aggiornare il film con le modifiche dell'utente</span><span class="sxs-lookup"><span data-stu-id="2140d-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="2140d-248">Nel *EditMovie.cshtml* file, per implementare la seconda funzione (salvataggio), aggiungere il codice seguente all'interno di parentesi graffa di chiusura di `@` blocco.</span><span class="sxs-lookup"><span data-stu-id="2140d-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="2140d-249">(Se non si è certi esattamente in cui inserire il codice, è possibile esaminare il [completo listato di codice per la pagina Modifica film](#Complete_Page_Listing_for_EditMovie) che viene visualizzato alla fine di questa esercitazione.)</span><span class="sxs-lookup"><span data-stu-id="2140d-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="2140d-250">Nuovamente, questo markup e il codice è simile al codice in *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="2140d-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="2140d-251">Il codice è in un `if(IsPost)` blocco, perché questo codice viene eseguito solo quando l'utente sceglie il **Invia modifiche** pulsante &mdash; , ovvero quando (e solo quando) ha inviato il form.</span><span class="sxs-lookup"><span data-stu-id="2140d-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="2140d-252">In questo caso, non si usa un test come `if(IsPost && Validation.IsValid())`, vale a dire da non combinare entrambi i test tramite and.</span><span class="sxs-lookup"><span data-stu-id="2140d-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="2140d-253">In questa pagina, innanzitutto determinare se è presente un invio del modulo (`if(IsPost)`) e registrare solo i campi per la convalida.</span><span class="sxs-lookup"><span data-stu-id="2140d-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="2140d-254">Quindi è possibile testare i risultati della convalida (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="2140d-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="2140d-255">Il flusso è leggermente diverso da quello di *AddMovie.cshtml* pagina, ma l'effetto è lo stesso.</span><span class="sxs-lookup"><span data-stu-id="2140d-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="2140d-256">Per ottenere i valori delle caselle di testo utilizzare `Request.Form["title"]` e codice analogo per gli altri `<input>` elementi.</span><span class="sxs-lookup"><span data-stu-id="2140d-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="2140d-257">Si noti che questo periodo, il codice di Ottiene l'ID film fuori dal campo nascosto (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="2140d-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="2140d-258">Quando la pagina è stata eseguita la prima volta, il codice ha ricevuto l'ID la stringa di query.</span><span class="sxs-lookup"><span data-stu-id="2140d-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="2140d-259">Ottenere il valore del campo nascosto per assicurarsi di ottenere l'ID del filmato che originariamente è stato visualizzato, nel caso in cui la stringa di query è stata modificata in qualche modo da allora.</span><span class="sxs-lookup"><span data-stu-id="2140d-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="2140d-260">L'importante differenza tra il *AddMovie* codice e il codice è che in questo codice di utilizzare l'istruzione SQL `Update` istruzione anziché il `Insert Into` istruzione.</span><span class="sxs-lookup"><span data-stu-id="2140d-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="2140d-261">L'esempio seguente mostra la sintassi dell'istruzione SQL `Update` istruzione:</span><span class="sxs-lookup"><span data-stu-id="2140d-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="2140d-262">È possibile specificare le colonne in qualsiasi ordine, e non è necessario aggiornare ogni colonna durante un `Update` operazione.</span><span class="sxs-lookup"><span data-stu-id="2140d-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="2140d-263">(Non è possibile aggiornare l'ID, in quanto che sarebbe attivo salvare il record come un nuovo record, e che non è consentita per un `Update` operazione.)</span><span class="sxs-lookup"><span data-stu-id="2140d-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="2140d-264">**Importante** il `Where` clausola con l'ID è molto importante, perché si tratta come database sappia quale database record che si desidera aggiornare.</span><span class="sxs-lookup"><span data-stu-id="2140d-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="2140d-265">Se si è interrotto il `Where` clausola, il database Aggiorna *ogni* record nel database.</span><span class="sxs-lookup"><span data-stu-id="2140d-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="2140d-266">Nella maggior parte dei casi, sarebbe un'emergenza.</span><span class="sxs-lookup"><span data-stu-id="2140d-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="2140d-267">Nel codice, aggiornare i valori vengono passati all'istruzione SQL utilizzando segnaposto.</span><span class="sxs-lookup"><span data-stu-id="2140d-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="2140d-268">Per ripetere quanto abbiamo detto prima: per motivi di sicurezza *solo* utilizzare segnaposto per passare valori a un'istruzione SQL.</span><span class="sxs-lookup"><span data-stu-id="2140d-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="2140d-269">Dopo il codice Usa `db.Execute` per eseguire il `Update` istruzione, viene reindirizzato alla pagina di elenco, in cui è possibile visualizzare le modifiche.</span><span class="sxs-lookup"><span data-stu-id="2140d-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="2140d-270">**Istruzioni SQL diverso, metodi diversi**</span><span class="sxs-lookup"><span data-stu-id="2140d-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="2140d-271">È possibile notare l'uso di metodi leggermente diversi per eseguire istruzioni SQL diverse.</span><span class="sxs-lookup"><span data-stu-id="2140d-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="2140d-272">Per eseguire un `Select` query che potrebbero restituisce più record, si utilizza il `Query` metodo.</span><span class="sxs-lookup"><span data-stu-id="2140d-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="2140d-273">Per eseguire un `Select` che verranno restituiti solo un elemento di database, utilizzare il `QuerySingle` metodo.</span><span class="sxs-lookup"><span data-stu-id="2140d-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="2140d-274">Per eseguire i comandi che apportano modifiche, ma che non restituiscono gli elementi di database, utilizzare il `Execute` metodo.</span><span class="sxs-lookup"><span data-stu-id="2140d-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="2140d-275">È necessario disporre di diversi metodi in quanto ognuno di essi restituisce risultati diversi, come già specificato nella differenza tra `Query` e `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="2140d-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="2140d-276">(Il `Execute` metodo restituisce un valore anche &mdash; , ovvero il numero di righe di database che sono interessate dal comando &mdash; ma si è stati ignorando che finora.)</span><span class="sxs-lookup"><span data-stu-id="2140d-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="2140d-277">Naturalmente, la `Query` metodo può restituire solo una riga di database.</span><span class="sxs-lookup"><span data-stu-id="2140d-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="2140d-278">Tuttavia, ASP.NET considera sempre i risultati del `Query` metodo come una raccolta.</span><span class="sxs-lookup"><span data-stu-id="2140d-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="2140d-279">Anche se il metodo restituisce una sola riga, è necessario estrarre la singola riga dalla raccolta.</span><span class="sxs-lookup"><span data-stu-id="2140d-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="2140d-280">Pertanto, in situazioni in cui si *conoscere* si otterrà solo una riga, è un po' più comodo usare `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="2140d-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="2140d-281">Esistono alcuni altri metodi che eseguono tipi specifici di operazioni di database.</span><span class="sxs-lookup"><span data-stu-id="2140d-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="2140d-282">È possibile trovare un elenco di metodi di database di [riferimento rapido di ASP.NET Web Pages API](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="2140d-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="2140d-283">Effettua la convalida per più ID affidabile</span><span class="sxs-lookup"><span data-stu-id="2140d-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="2140d-284">La prima volta che la pagina viene eseguita, si ottiene l'ID film dalla stringa di query in modo da poter accedere ottenere tale film dal database.</span><span class="sxs-lookup"><span data-stu-id="2140d-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="2140d-285">Verificato che in realtà era presente un valore da passare cercare, che è stato eseguito con questo codice:</span><span class="sxs-lookup"><span data-stu-id="2140d-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="2140d-286">Questo codice usato per fare in modo che se un utente ottiene il *EditMovies* pagina senza selezionare un filmato nel *filmati* pagina, la pagina Visualizza un messaggio di errore descrittivo.</span><span class="sxs-lookup"><span data-stu-id="2140d-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="2140d-287">(In caso contrario, verrà visualizzato un errore che sarebbe probabilmente confondere.)</span><span class="sxs-lookup"><span data-stu-id="2140d-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="2140d-288">Tuttavia, la convalida non molto affidabile.</span><span class="sxs-lookup"><span data-stu-id="2140d-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="2140d-289">La pagina potrebbe essere richiamata anche con questi errori:</span><span class="sxs-lookup"><span data-stu-id="2140d-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="2140d-290">L'ID non è un numero.</span><span class="sxs-lookup"><span data-stu-id="2140d-290">The ID isn't a number.</span></span> <span data-ttu-id="2140d-291">Ad esempio, la pagina potrebbe essere richiamata con un URL come `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="2140d-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="2140d-292">L'ID è un numero, ma fa riferimento a un filmato che non esiste (ad esempio, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="2140d-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="2140d-293">Se si desidera visualizzare gli errori risultanti da tali URL, eseguire il *filmati* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="2140d-294">Selezionare un filmato per modificare e quindi modificare l'URL del *EditMovie* pagina a un URL che contiene un alfabetico ID o l'ID di un filmato inesistente.</span><span class="sxs-lookup"><span data-stu-id="2140d-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="2140d-295">Pertanto, deve procedere?</span><span class="sxs-lookup"><span data-stu-id="2140d-295">So what should you do?</span></span> <span data-ttu-id="2140d-296">La correzione prima consiste nel verificare che non solo è un ID passato alla pagina, ma che l'ID è un numero intero.</span><span class="sxs-lookup"><span data-stu-id="2140d-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="2140d-297">Modificare il codice per il `!IsPost` test aspetto simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="2140d-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="2140d-298">Aver aggiunto una seconda condizione per il `IsEmpty` test, collegate con `&&` (AND logico):</span><span class="sxs-lookup"><span data-stu-id="2140d-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="2140d-299">Ricorderà dal [Introduzione alla programmazione di ASP.NET Web Pages](../introducing-razor-syntax-c.md) esercitazione che metodi come `AsBool` un `AsInt` convertire una stringa di caratteri in un altro tipo di dati.</span><span class="sxs-lookup"><span data-stu-id="2140d-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="2140d-300">Il `IsInt` metodo (e altri, ad esempio `IsBool` e `IsDateTime`) sono simili.</span><span class="sxs-lookup"><span data-stu-id="2140d-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="2140d-301">Tuttavia, eseguono il test solo se si *possibile* convertire la stringa, senza eseguire effettivamente la conversione.</span><span class="sxs-lookup"><span data-stu-id="2140d-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="2140d-302">Questo significa fondamentalmente *se il valore di stringa di query può essere convertito in un intero...* .</span><span class="sxs-lookup"><span data-stu-id="2140d-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="2140d-303">Potenziale problema sta cercando un filmato che non esiste.</span><span class="sxs-lookup"><span data-stu-id="2140d-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="2140d-304">Il codice per ottenere un filmato simile al codice:</span><span class="sxs-lookup"><span data-stu-id="2140d-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="2140d-305">Se si passa un `movieId` valore per il `QuerySingle` metodo che non corrisponde a un filmato effettivo, non viene restituito alcun risultato e le istruzioni che seguono (ad esempio, `title=row.Title`) provocare errori.</span><span class="sxs-lookup"><span data-stu-id="2140d-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="2140d-306">È nuovamente un facile da risolvere.</span><span class="sxs-lookup"><span data-stu-id="2140d-306">Again there's an easy fix.</span></span> <span data-ttu-id="2140d-307">Se il `db.QuerySingle` metodo non restituisce alcun risultato, il `row` variabile sarà null.</span><span class="sxs-lookup"><span data-stu-id="2140d-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="2140d-308">È possibile controllare se il `row` variabile è null prima di tentare di recuperare i valori da esso.</span><span class="sxs-lookup"><span data-stu-id="2140d-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="2140d-309">Il codice seguente aggiunge un `if` blocco intorno alle istruzioni che ottengono i valori di timeout di `row` oggetto:</span><span class="sxs-lookup"><span data-stu-id="2140d-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="2140d-310">Con questi due test di convalida aggiuntivo, la pagina diventa più valide.</span><span class="sxs-lookup"><span data-stu-id="2140d-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="2140d-311">Il codice completo per il `!IsPost` ramo avrà ora un aspetto simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="2140d-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="2140d-312">Si noterà che ancora una volta che questa operazione è consigliabile utilizzare per un `else` blocco.</span><span class="sxs-lookup"><span data-stu-id="2140d-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="2140d-313">Se il test non superati, il `else` set di blocchi di messaggi di errore.</span><span class="sxs-lookup"><span data-stu-id="2140d-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="2140d-314">Aggiunta di un collegamento per tornare alla pagina di filmati</span><span class="sxs-lookup"><span data-stu-id="2140d-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="2140d-315">È un dettaglio finale e utile per aggiungere un collegamento al *filmati* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="2140d-316">Nel flusso normale di eventi, gli utenti inizieranno al *filmati* pagina e fare clic su un **modifica** collegamento.</span><span class="sxs-lookup"><span data-stu-id="2140d-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="2140d-317">Che li include il *EditMovie* pagina, in cui è possibile modificare il filmato e fare clic sul pulsante.</span><span class="sxs-lookup"><span data-stu-id="2140d-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="2140d-318">Dopo che il codice ha elaborato la modifica, reindirizza il *filmati* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="2140d-319">Tuttavia:</span><span class="sxs-lookup"><span data-stu-id="2140d-319">However:</span></span>

- <span data-ttu-id="2140d-320">L'utente potrebbe decidere di non apporta alcuna modifica.</span><span class="sxs-lookup"><span data-stu-id="2140d-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="2140d-321">L'utente potrebbe essere stato ottenuto a questa pagina senza prima avere scelto un **modifica** collegare il *filmati* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="2140d-322">In entrambi i casi, si desidera semplificare tornare all'elenco principale.</span><span class="sxs-lookup"><span data-stu-id="2140d-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="2140d-323">Si tratta di una facile correzione &mdash; aggiungere il markup seguente subito dopo la chiusura `</form>` tag nel codice:</span><span class="sxs-lookup"><span data-stu-id="2140d-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="2140d-324">Questo codice Usa la stessa sintassi per un `<a>` elemento che si è visto altrove.</span><span class="sxs-lookup"><span data-stu-id="2140d-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="2140d-325">L'URL include `~` per indicare "radice del sito Web".</span><span class="sxs-lookup"><span data-stu-id="2140d-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="2140d-326">Test del processo di aggiornamento del film</span><span class="sxs-lookup"><span data-stu-id="2140d-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="2140d-327">Ora è possibile eseguire il test.</span><span class="sxs-lookup"><span data-stu-id="2140d-327">Now you can test.</span></span> <span data-ttu-id="2140d-328">Eseguire il *filmati* pagina e fare clic su **modifica** accanto a un filmato.</span><span class="sxs-lookup"><span data-stu-id="2140d-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="2140d-329">Quando il *EditMovie* verrà visualizzata la pagina, apportare modifiche per il film e fare clic su **Invia modifiche**.</span><span class="sxs-lookup"><span data-stu-id="2140d-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="2140d-330">Quando viene visualizzato l'elenco di film, assicurarsi che le modifiche vengono visualizzate.</span><span class="sxs-lookup"><span data-stu-id="2140d-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="2140d-331">Per assicurarsi che funzioni correttamente la convalida, fare clic su **modifica** per un altro film.</span><span class="sxs-lookup"><span data-stu-id="2140d-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="2140d-332">Quando si accede al *EditMovie* pagina, deseleziona il **Genre** campo (o **anno** campo o entrambi) e si tenta di inviare le modifiche.</span><span class="sxs-lookup"><span data-stu-id="2140d-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="2140d-333">Verrà visualizzato un errore, come previsto:</span><span class="sxs-lookup"><span data-stu-id="2140d-333">You'll see an error, as you'd expect:</span></span>

![Modifica pagina filmato che mostri gli errori di convalida](updating-data/_static/image4.png)

<span data-ttu-id="2140d-335">Fare clic su di **tornare all'elenco di film** collegamento per annullare le modifiche e tornare al *filmati* pagina.</span><span class="sxs-lookup"><span data-stu-id="2140d-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="2140d-336">Esercitazione seguente</span><span class="sxs-lookup"><span data-stu-id="2140d-336">Coming Up Next</span></span>

<span data-ttu-id="2140d-337">Nella prossima esercitazione, si noterà come eliminare un record di film.</span><span class="sxs-lookup"><span data-stu-id="2140d-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="2140d-338">Elenco completo per la pagina di film (aggiornata con i collegamenti di modifica)</span><span class="sxs-lookup"><span data-stu-id="2140d-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="2140d-339">Completare l'elenco di pagina per pagina film modifica</span><span class="sxs-lookup"><span data-stu-id="2140d-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="2140d-340">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="2140d-340">Additional Resources</span></span>

- [<span data-ttu-id="2140d-341">Introduzione alla programmazione Web ASP.NET utilizzando la sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="2140d-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../../getting-started/introducing-razor-syntax-c.md)
- <span data-ttu-id="2140d-342">[Istruzione SQL UPDATE](http://www.w3schools.com/sql/sql_update.asp) nel sito W3Schools</span><span class="sxs-lookup"><span data-stu-id="2140d-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="2140d-343">[Precedente](entering-data.md)
> [Successivo](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="2140d-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
