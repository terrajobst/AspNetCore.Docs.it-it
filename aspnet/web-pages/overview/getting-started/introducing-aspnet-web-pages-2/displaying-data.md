---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Introduzione a ASP.NET Web Pages - visualizzazione di dati | Documenti Microsoft
author: tfitzmac
description: In questa esercitazione viene illustrato come creare un database in WebMatrix e come visualizzare i dati del database in una pagina quando si utilizzano pagine Web ASP.NET (Razor). Si presuppone di y...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 6c66e5fb0a1a49da411286e19c7954f83055c3fd
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/06/2018
ms.locfileid: "30898457"
---
<a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="db235-104">Introduzione a ASP.NET Web Pages - visualizzazione di dati</span><span class="sxs-lookup"><span data-stu-id="db235-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>
====================
<span data-ttu-id="db235-105">da [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="db235-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="db235-106">In questa esercitazione viene illustrato come creare un database in WebMatrix e come visualizzare i dati del database in una pagina quando si utilizzano pagine Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="db235-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="db235-107">Si presuppone di aver completato la serie tramite [Introduzione alla programmazione di pagine Web ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="db235-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="db235-108">Illustra quanto segue:</span><span class="sxs-lookup"><span data-stu-id="db235-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="db235-109">Come utilizzare gli strumenti di WebMatrix per creare un database e tabelle di database.</span><span class="sxs-lookup"><span data-stu-id="db235-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="db235-110">Come utilizzare gli strumenti di WebMatrix per aggiungere dati a un database.</span><span class="sxs-lookup"><span data-stu-id="db235-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="db235-111">Come visualizzare i dati dal database in una pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="db235-112">Come eseguire comandi SQL in ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="db235-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="db235-113">Come personalizzare il `WebGrid` helper per modificare la visualizzazione dei dati e aggiungere l'ordinamento e paging.</span><span class="sxs-lookup"><span data-stu-id="db235-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="db235-114">Funzionalità/tecnologie descritte:</span><span class="sxs-lookup"><span data-stu-id="db235-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="db235-115">Strumenti di database di WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="db235-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="db235-116">`WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="db235-116">`WebGrid` helper.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="db235-117">Cosa Compilerai</span><span class="sxs-lookup"><span data-stu-id="db235-117">What You'll Build</span></span>

<span data-ttu-id="db235-118">Nell'esercitazione precedente, sono stati introdotti per ASP.NET Web Pages (*. cshtml* file), per le nozioni di base della sintassi Razor e per gli helper.</span><span class="sxs-lookup"><span data-stu-id="db235-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="db235-119">In questa esercitazione, innanzitutto creare l'applicazione web che verrà utilizzato per il resto della serie.</span><span class="sxs-lookup"><span data-stu-id="db235-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="db235-120">L'applicazione è un'applicazione semplice filmato che consente di visualizzare, aggiungere, modificare ed eliminare le informazioni sui film.</span><span class="sxs-lookup"><span data-stu-id="db235-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="db235-121">Una volta terminato con questa esercitazione, sarà in grado di visualizzare un elenco di film simile a questa pagina:</span><span class="sxs-lookup"><span data-stu-id="db235-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Visualizzazione WebGrid che include i parametri impostati per i nomi delle classi CSS](displaying-data/_static/image1.png)

<span data-ttu-id="db235-123">Tuttavia, per iniziare, è necessario creare un database.</span><span class="sxs-lookup"><span data-stu-id="db235-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="db235-124">Una breve introduzione ai database</span><span class="sxs-lookup"><span data-stu-id="db235-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="db235-125">In questa esercitazione verrà fornite solo briefest Introduzione ai database.</span><span class="sxs-lookup"><span data-stu-id="db235-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="db235-126">Se si ha esperienza di database, è possibile ignorare questa sezione breve.</span><span class="sxs-lookup"><span data-stu-id="db235-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="db235-127">Un database contiene uno o più tabelle che contengono informazioni &mdash; tabelle, ad esempio, per i clienti, ordini e fornitori o per studenti, docenti, classi e livelli.</span><span class="sxs-lookup"><span data-stu-id="db235-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="db235-128">Livello di struttura, una tabella di database è un foglio di lavoro.</span><span class="sxs-lookup"><span data-stu-id="db235-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="db235-129">Si supponga una tipico di Rubrica.</span><span class="sxs-lookup"><span data-stu-id="db235-129">Imagine a typical address book.</span></span> <span data-ttu-id="db235-130">Per ogni voce della Rubrica (ovvero, per ogni persona) si dispone di diverse parti di informazioni quali nome, cognome, indirizzo, indirizzo di posta elettronica e numero di telefono.</span><span class="sxs-lookup"><span data-stu-id="db235-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Tabella di database di esempio come griglia semplice](displaying-data/_static/image2.png)

<span data-ttu-id="db235-132">(Righe vengono dette *record*, e le colonne sono dette *campi*.)</span><span class="sxs-lookup"><span data-stu-id="db235-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="db235-133">Per la maggior parte delle tabelle di database, la tabella deve disporre di una colonna che contiene un valore univoco, ad esempio un numero cliente, il numero di conto e così via.</span><span class="sxs-lookup"><span data-stu-id="db235-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="db235-134">Questo valore è noto come la tabella *chiave primaria*, e usarlo per identificare ogni riga nella tabella.</span><span class="sxs-lookup"><span data-stu-id="db235-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="db235-135">Nell'esempio, la colonna ID è la chiave primaria per la Rubrica illustrata nell'esempio precedente.</span><span class="sxs-lookup"><span data-stu-id="db235-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="db235-136">Gran parte delle operazioni eseguite nelle applicazioni web è costituito da informazioni dal database di lettura e visualizzarli in una pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="db235-137">È inoltre spesso sarà raccogliere informazioni dagli utenti e aggiungerlo a un database o che si desidera modificare i record che sono già presenti nel database.</span><span class="sxs-lookup"><span data-stu-id="db235-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="db235-138">(Tratteremo tutte queste operazioni nel corso di questa esercitazione set.)</span><span class="sxs-lookup"><span data-stu-id="db235-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="db235-139">Operazioni di database possono essere particolarmente complesso e possono richiedere conoscenze specializzate.</span><span class="sxs-lookup"><span data-stu-id="db235-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="db235-140">Per set di questa esercitazione, tuttavia, è necessario comprendere solo concetti di base, che verranno tutti spiegati mentre Prosegui.</span><span class="sxs-lookup"><span data-stu-id="db235-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="db235-141">Creazione di un Database</span><span class="sxs-lookup"><span data-stu-id="db235-141">Creating a Database</span></span>

<span data-ttu-id="db235-142">WebMatrix include strumenti che semplificano la creazione di un database e per creare tabelle nel database.</span><span class="sxs-lookup"><span data-stu-id="db235-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="db235-143">(La struttura di un database viene definita il database *schema*.) Per set di questa esercitazione, si creerà un database che contiene una sola tabella &mdash; film.</span><span class="sxs-lookup"><span data-stu-id="db235-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="db235-144">Se non è già fatto, aprire WebMatrix e aprire il sito WebPagesMovies creato nell'esercitazione precedente.</span><span class="sxs-lookup"><span data-stu-id="db235-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="db235-145">Nel riquadro a sinistra, fare clic su di **Database** dell'area di lavoro.</span><span class="sxs-lookup"><span data-stu-id="db235-145">In the left pane, click the **Database** workspace.</span></span>

![Scheda di WebMatrix Database dell'area di lavoro](displaying-data/_static/image3.png)

<span data-ttu-id="db235-147">La barra multifunzione cambia per mostrare le attività correlate ai database.</span><span class="sxs-lookup"><span data-stu-id="db235-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="db235-148">Nella barra multifunzione, fare clic su **Nuovo Database**.</span><span class="sxs-lookup"><span data-stu-id="db235-148">In the ribbon, click **New Database**.</span></span>

![Pulsante "Nuovo Database" nella barra multifunzione di WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="db235-150">WebMatrix consente di creare un database di SQL Server CE (un *sdf* file) che ha lo stesso nome del sito &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="db235-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="db235-151">(Si non esegue questa operazione qui, ma è possibile rinominare il file su qualsiasi elemento che si desidera, purché abbia un *sdf* estensione.)</span><span class="sxs-lookup"><span data-stu-id="db235-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="db235-152">Creazione di una tabella</span><span class="sxs-lookup"><span data-stu-id="db235-152">Creating a Table</span></span>

<span data-ttu-id="db235-153">Nella barra multifunzione, fare clic su **nuova tabella**.</span><span class="sxs-lookup"><span data-stu-id="db235-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="db235-154">WebMatrix consente di aprire Progettazione tabelle in una nuova scheda. (Se l'opzione nuova tabella non è disponibile, assicurarsi che il nuovo database sia selezionato nella visualizzazione albero a sinistra.)</span><span class="sxs-lookup"><span data-stu-id="db235-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

!['Nella tabella nuovo' pulsante nella barra multifunzione di WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="db235-156">Nella casella di testo nella parte superiore (in cui il limite è indicato "Immettere il nome di tabella"), immettere "Filmati".</span><span class="sxs-lookup"><span data-stu-id="db235-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Immettere un nome di tabella nella finestra di progettazione di database di WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="db235-158">Riquadro di sotto il nome della tabella viene usata per definire singole colonne.</span><span class="sxs-lookup"><span data-stu-id="db235-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="db235-159">Per il *filmati* tabella in questa esercitazione verrà creato solo alcune colonne: *ID*, *titolo*, *Genre*, e *anno*.</span><span class="sxs-lookup"><span data-stu-id="db235-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="db235-160">Nel **nome** , immettere "ID".</span><span class="sxs-lookup"><span data-stu-id="db235-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="db235-161">Immettere un valore attiva tutti i controlli per la nuova colonna.</span><span class="sxs-lookup"><span data-stu-id="db235-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="db235-162">Premere TAB per passare il **tipo di dati** elenco e scegliere **int**. Questo valore specifica che la colonna ID conterrà dati integer (numero).</span><span class="sxs-lookup"><span data-stu-id="db235-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="db235-163">È non chiamarlo un dettaglio di seguito (molto), ma è possibile utilizzare i movimenti di tastiera Windows standard per spostarsi all'interno di questa griglia.</span><span class="sxs-lookup"><span data-stu-id="db235-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="db235-164">Ad esempio, può spostarsi tra i campi, è solo possibile iniziare a digitare per selezionare un elemento in un elenco e così via.</span><span class="sxs-lookup"><span data-stu-id="db235-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>


<span data-ttu-id="db235-165">Scheda oltre il **Default Value** casella (ossia, lasciare vuoto il campo).</span><span class="sxs-lookup"><span data-stu-id="db235-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="db235-166">Premere TAB per passare il **chiave primaria è** casella di controllo e selezionarlo.</span><span class="sxs-lookup"><span data-stu-id="db235-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="db235-167">Questa opzione indica il database di *ID* colonna conterrà i dati che identifica singole righe.</span><span class="sxs-lookup"><span data-stu-id="db235-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="db235-168">(Ovvero, ogni riga avrà un valore univoco nella colonna ID che è possibile utilizzare per trovare tale riga.)</span><span class="sxs-lookup"><span data-stu-id="db235-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="db235-169">Scegliere il **identità** opzione.</span><span class="sxs-lookup"><span data-stu-id="db235-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="db235-170">Questa opzione indica il database che dovrà generare automaticamente il successivo numero sequenziale per ogni nuova riga.</span><span class="sxs-lookup"><span data-stu-id="db235-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="db235-171">(Il **identità** opzione funziona solo se è selezionata anche il **chiave primaria è** opzione.)</span><span class="sxs-lookup"><span data-stu-id="db235-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="db235-172">Fare clic sulla riga successiva della griglia, o premere il tasto Tab due volte per terminare la riga corrente.</span><span class="sxs-lookup"><span data-stu-id="db235-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="db235-173">L'azione consente di salvare la riga corrente e avvia quello successivo.</span><span class="sxs-lookup"><span data-stu-id="db235-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="db235-174">Si noti che il **Default Value** colonna viene visualizzato ora **Null**.</span><span class="sxs-lookup"><span data-stu-id="db235-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="db235-175">(Il valore predefinito è null per il valore predefinito, pertanto per dire.)</span><span class="sxs-lookup"><span data-stu-id="db235-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="db235-176">Al termine della definizione di nuovo **ID** colonna, la finestra di progettazione sarà simile a questa illustrazione:</span><span class="sxs-lookup"><span data-stu-id="db235-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Progettazione database WebMatrix dopo la definizione della colonna di ID per la tabella di filmati](displaying-data/_static/image7.png)

<span data-ttu-id="db235-178">Per creare la colonna successiva, fare clic nella casella di **nome** colonna.</span><span class="sxs-lookup"><span data-stu-id="db235-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="db235-179">Immettere "Title" per la colonna e quindi selezionare **nvarchar** per il **tipo di dati** valore.</span><span class="sxs-lookup"><span data-stu-id="db235-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="db235-180">La parte "var" **nvarchar** indica il database che i dati per questa colonna sarà una stringa il cui dimensioni possono variare da un record a altro.</span><span class="sxs-lookup"><span data-stu-id="db235-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="db235-181">(Il prefisso "n" rappresenta "national", che indica che il campo può contenere qualsiasi carattere alfabetico o sistema di scrittura dati di tipo carattere, vale a dire, il campo contiene i dati Unicode.)</span><span class="sxs-lookup"><span data-stu-id="db235-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="db235-182">Quando si sceglie **nvarchar**, un'altra casella viene visualizzata in cui è possibile immettere la lunghezza massima per il campo.</span><span class="sxs-lookup"><span data-stu-id="db235-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="db235-183">Immettere 50, presupponendo che nessun titolo del film che verranno utilizzate in questa esercitazione sarà più di 50 caratteri.</span><span class="sxs-lookup"><span data-stu-id="db235-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="db235-184">Ignora **Default Value** e deselezionare il **Ammetti** opzione.</span><span class="sxs-lookup"><span data-stu-id="db235-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="db235-185">Per evitare che il database per consentire qualsiasi filmati essere immessi nel database che non dispongono di un titolo.</span><span class="sxs-lookup"><span data-stu-id="db235-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="db235-186">Quando è terminato e passare alla riga successiva, la finestra di progettazione simile a questa illustrazione:</span><span class="sxs-lookup"><span data-stu-id="db235-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Progettazione database WebMatrix dopo la definizione di colonna del titolo per la tabella di filmati](displaying-data/_static/image8.png)

<span data-ttu-id="db235-188">Ripetere questi passaggi per creare una colonna denominata "Genre", ad eccezione di lunghezza, impostarla su appena 30.</span><span class="sxs-lookup"><span data-stu-id="db235-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="db235-189">Creare un'altra colonna denominata "Year".</span><span class="sxs-lookup"><span data-stu-id="db235-189">Create another column named "Year."</span></span> <span data-ttu-id="db235-190">Per il tipo di dati, scegliere **nchar** (non **nvarchar**) e impostare la lunghezza su 4.</span><span class="sxs-lookup"><span data-stu-id="db235-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="db235-191">Per l'anno, che si intende utilizzare un numero di 4 cifre come "1995" o "2010", in modo che non sia richiesto una colonna a lunghezza variabile.</span><span class="sxs-lookup"><span data-stu-id="db235-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="db235-192">Ecco come appare la progettazione terminata:</span><span class="sxs-lookup"><span data-stu-id="db235-192">Here's what the finished design looks like:</span></span>

![Progettazione di database WebMatrix dopo tutti i campi definiti per la tabella di filmati](displaying-data/_static/image9.png)

<span data-ttu-id="db235-194">Premere Ctrl + S oppure scegliere di **salvare** sulla barra degli strumenti accesso rapido.</span><span class="sxs-lookup"><span data-stu-id="db235-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="db235-195">Chiudere la finestra di progettazione di database, chiudere la scheda.</span><span class="sxs-lookup"><span data-stu-id="db235-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="db235-196">Aggiunta di alcuni dati di esempio</span><span class="sxs-lookup"><span data-stu-id="db235-196">Adding Some Example Data</span></span>

<span data-ttu-id="db235-197">Più avanti in questa serie di esercitazioni si creerà una pagina in cui è possibile immettere nuovi film in un form.</span><span class="sxs-lookup"><span data-stu-id="db235-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="db235-198">Per il momento, tuttavia, è possibile aggiungere alcuni dati di esempio che è quindi possibile visualizzare in una pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="db235-199">Nel **Database** area di lavoro di WebMatrix, si noti che sono una struttura ad albero che mostra il *sdf* file creato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="db235-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="db235-200">Aprire il nodo per il nuovo *sdf* file e quindi aprire il **tabelle** nodo.</span><span class="sxs-lookup"><span data-stu-id="db235-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Area di lavoro di WebMatrix Database con struttura ad albero aprire alla tabella di filmati](displaying-data/_static/image10.png)

<span data-ttu-id="db235-202">Fare doppio clic su di **filmati** nodo e quindi scegliere **dati**.</span><span class="sxs-lookup"><span data-stu-id="db235-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="db235-203">WebMatrix verrà visualizzata una griglia in cui è possibile immettere dati per il *filmati* tabella:</span><span class="sxs-lookup"><span data-stu-id="db235-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Griglia di voce di database in WebMatrix (vuoto)](displaying-data/_static/image11.png)

<span data-ttu-id="db235-205">Fare clic su di **titolo** colonna e immettere "Quando Harry soddisfatti Sally".</span><span class="sxs-lookup"><span data-stu-id="db235-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="db235-206">Spostare il **Genre** colonna (è possibile utilizzare il tasto Tab) e immettere "Romantico Commedia".</span><span class="sxs-lookup"><span data-stu-id="db235-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="db235-207">Spostare il **anno** colonna e immettere "1989":</span><span class="sxs-lookup"><span data-stu-id="db235-207">Move to the **Year** column and enter "1989":</span></span>

![Griglia di voce di database in WebMatrix con un record](displaying-data/_static/image12.png)

<span data-ttu-id="db235-209">Premere INVIO e WebMatrix Salva il nuovo film.</span><span class="sxs-lookup"><span data-stu-id="db235-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="db235-210">Si noti che il **ID** colonna è stata compilata.</span><span class="sxs-lookup"><span data-stu-id="db235-210">Notice that the **ID** column has been filled in.</span></span>

![Griglia di voce di database in WebMatrix con un record e l'ID generato automaticamente](displaying-data/_static/image13.png)

<span data-ttu-id="db235-212">Immettere un altro filmato (ad esempio, "effettuato con il vento", "Serie", "1939").</span><span class="sxs-lookup"><span data-stu-id="db235-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="db235-213">La colonna ID viene inserita nuovamente:</span><span class="sxs-lookup"><span data-stu-id="db235-213">The ID column is filled in again:</span></span>

![Griglia di voce di database in WebMatrix con due record e l'ID generato automaticamente](displaying-data/_static/image14.png)

<span data-ttu-id="db235-215">Immettere un terzo filmato (ad esempio, "Ghostbusters", "Commedia").</span><span class="sxs-lookup"><span data-stu-id="db235-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="db235-216">Per un esperimento, lasciare il **anno** colonna vuota e quindi premere INVIO.</span><span class="sxs-lookup"><span data-stu-id="db235-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="db235-217">Poiché è stata deselezionata l'opzione di **Ammetti** opzione, il database viene visualizzato un errore:</span><span class="sxs-lookup"><span data-stu-id="db235-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

!['Data non valida' errore visualizzato se un valore di colonna richiesta è vuoto.](displaying-data/_static/image15.png)

<span data-ttu-id="db235-219">Fare clic su **OK** per tornare indietro e correggere la voce (l'anno per "Ghostbusters" è 1984) e quindi premere INVIO.</span><span class="sxs-lookup"><span data-stu-id="db235-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="db235-220">Compilare filmati diversi fino a quando non si dispone di 8 o in modo.</span><span class="sxs-lookup"><span data-stu-id="db235-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="db235-221">(Immettere 8 rende più facile lavorare con paging in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="db235-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="db235-222">Ma se è un numero eccessivo, immettere solo alcune per ora). I dati effettivi non ha importanza.</span><span class="sxs-lookup"><span data-stu-id="db235-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Griglia di voce di database in WebMatrix con entrambi i record](displaying-data/_static/image16.png)

<span data-ttu-id="db235-224">Se è stato immesso tutti i film senza errori, i valori di ID sono sequenziali.</span><span class="sxs-lookup"><span data-stu-id="db235-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="db235-225">Se si tenta di salvare un record incompleti film, i numeri di ID potrebbero non essere sequenziali.</span><span class="sxs-lookup"><span data-stu-id="db235-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="db235-226">In questo caso, che è accettabile.</span><span class="sxs-lookup"><span data-stu-id="db235-226">If so, that's okay.</span></span> <span data-ttu-id="db235-227">L'unico elemento importante è che questi sistemi univoci in e i numeri non hanno alcun significato intrinseco di *filmati* tabella.</span><span class="sxs-lookup"><span data-stu-id="db235-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="db235-228">Chiudere la scheda che contiene la finestra di progettazione di database.</span><span class="sxs-lookup"><span data-stu-id="db235-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="db235-229">Ora è possibile attivare la visualizzazione di dati in una pagina web.</span><span class="sxs-lookup"><span data-stu-id="db235-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="db235-230">Visualizzazione dei dati in una pagina con il supporto WebGrid</span><span class="sxs-lookup"><span data-stu-id="db235-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="db235-231">Per visualizzare i dati in una pagina, che si intende utilizzare il `WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="db235-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="db235-232">Questo supporto produce una visualizzazione in una griglia o una tabella (righe e colonne).</span><span class="sxs-lookup"><span data-stu-id="db235-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="db235-233">Come si vedrà, sarà in grado di definire la griglia con formattazione e altre funzionalità.</span><span class="sxs-lookup"><span data-stu-id="db235-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="db235-234">Per eseguire la griglia, sarà necessario scrivere poche righe di codice.</span><span class="sxs-lookup"><span data-stu-id="db235-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="db235-235">Alcune di queste righe fungerà da un tipo di modello per la maggior parte dell'accesso ai dati di questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="db235-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="db235-236">In realtà offrono diverse opzioni per la visualizzazione dei dati in una pagina. il `WebGrid` helper è solo una.</span><span class="sxs-lookup"><span data-stu-id="db235-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="db235-237">Abbiamo scelto, per questa esercitazione perché è il modo più semplice per visualizzare i dati e, in quanto è abbastanza flessibile.</span><span class="sxs-lookup"><span data-stu-id="db235-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="db235-238">Nel set di esercitazione successivo, si noterà come utilizzare un altro modo "manual" per funzionare con i dati nella pagina, che fornisce un controllo più diretto su come visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="db235-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>


<span data-ttu-id="db235-239">Nel riquadro a sinistra in WebMatrix, fare clic su di **file** dell'area di lavoro.</span><span class="sxs-lookup"><span data-stu-id="db235-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="db235-240">Il nuovo database creato è il *App\_dati* cartella.</span><span class="sxs-lookup"><span data-stu-id="db235-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="db235-241">Se la cartella non esiste già, WebMatrix creato per il nuovo database.</span><span class="sxs-lookup"><span data-stu-id="db235-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="db235-242">(La cartella potrebbe esistito se è stato installato in precedenza helper.)</span><span class="sxs-lookup"><span data-stu-id="db235-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="db235-243">Nella visualizzazione albero, selezionare la radice del sito Web.</span><span class="sxs-lookup"><span data-stu-id="db235-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="db235-244">È necessario selezionare la radice del sito Web; in caso contrario, il nuovo file potrebbe essere aggiunti all'App\_cartella dati.</span><span class="sxs-lookup"><span data-stu-id="db235-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="db235-245">Nella barra multifunzione, fare clic su **New**.</span><span class="sxs-lookup"><span data-stu-id="db235-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="db235-246">Nel **scegliere un tipo di File** scegliere **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="db235-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="db235-247">Nel **nome** casella, denominare la nuova pagina "Movies.cshtml":</span><span class="sxs-lookup"><span data-stu-id="db235-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![La finestra di dialogo ', scegliere un tipo di File' che illustra la pagina 'Film'](displaying-data/_static/image17.png)

<span data-ttu-id="db235-249">Fare clic su di **OK** pulsante.</span><span class="sxs-lookup"><span data-stu-id="db235-249">Click the **OK** button.</span></span> <span data-ttu-id="db235-250">WebMatrix consente di aprire un nuovo file con alcuni elementi della struttura in essa contenuti.</span><span class="sxs-lookup"><span data-stu-id="db235-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="db235-251">Innanzitutto verrà scritto il codice per passare a ottenere i dati dal database.</span><span class="sxs-lookup"><span data-stu-id="db235-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="db235-252">Si aggiungeranno quindi markup alla pagina per visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="db235-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="db235-253">Scrittura del codice di Query di dati</span><span class="sxs-lookup"><span data-stu-id="db235-253">Writing the Data Query Code</span></span>

<span data-ttu-id="db235-254">Nella parte superiore della pagina, tra il `@{` e `}` caratteri, immettere il codice seguente.</span><span class="sxs-lookup"><span data-stu-id="db235-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="db235-255">(Assicurarsi immettere questo codice tra parentesi graffe di chiusura di apertura e).</span><span class="sxs-lookup"><span data-stu-id="db235-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="db235-256">La prima riga consente di aprire il database creato in precedenza, che è sempre il primo passaggio prima di procedere con il database.</span><span class="sxs-lookup"><span data-stu-id="db235-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="db235-257">Indicare il `Database.Open` nome del metodo del database da aprire.</span><span class="sxs-lookup"><span data-stu-id="db235-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="db235-258">Si noti che non si includono *sdf* nel nome.</span><span class="sxs-lookup"><span data-stu-id="db235-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="db235-259">Il `Open` metodo presuppone che sta cercando un *sdf* file (vale a dire *WebPagesMovies.sdf*) e che il *sdf* file è nel *App\_ Dati* cartella.</span><span class="sxs-lookup"><span data-stu-id="db235-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="db235-260">(In precedenza è stato indicato che il *App\_dati* cartella è riservata, questo scenario è una delle posizioni in cui ASP.NET ipotizza tale nome.)</span><span class="sxs-lookup"><span data-stu-id="db235-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="db235-261">Quando il database è aperto, un riferimento a esso viene inserito nella variabile denominata `db`.</span><span class="sxs-lookup"><span data-stu-id="db235-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="db235-262">(Che può essere modificato.) Il `db` variabile è il termine, verrà visualizzata l'interazione con il database.</span><span class="sxs-lookup"><span data-stu-id="db235-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="db235-263">La seconda riga effettivamente recupera i dati del database utilizzando il `Query` metodo.</span><span class="sxs-lookup"><span data-stu-id="db235-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="db235-264">Si noti il funzionamento di questo codice: il `db` variabile contiene un riferimento al database aperto e si richiama il `Query` metodo tramite il `db` variabile (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="db235-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="db235-265">La query stessa è un database SQL `Select` istruzione.</span><span class="sxs-lookup"><span data-stu-id="db235-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="db235-266">(Per una breve introduzione su SQL, vedere la spiegazione in un secondo momento). Nell'istruzione, `Movies` identifica la tabella alla query.</span><span class="sxs-lookup"><span data-stu-id="db235-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="db235-267">Il `*` carattere specifica che la query deve restituire tutte le colonne dalla tabella.</span><span class="sxs-lookup"><span data-stu-id="db235-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="db235-268">(È stato possibile anche elenco colonne singolarmente, separati da virgole.)</span><span class="sxs-lookup"><span data-stu-id="db235-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="db235-269">I risultati della query, se presenti, vengono restituiti e reso disponibili nel `selectedData` variabile.</span><span class="sxs-lookup"><span data-stu-id="db235-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="db235-270">Nuovamente, la variabile può essere modificata.</span><span class="sxs-lookup"><span data-stu-id="db235-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="db235-271">Infine, la terza riga indica ad ASP.NET che si desidera utilizzare un'istanza di `WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="db235-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="db235-272">Create (*creare un'istanza di*) l'oggetto helper utilizzando il `new` (parola chiave) e passare i risultati della query tramite il `selectedData` variabile.</span><span class="sxs-lookup"><span data-stu-id="db235-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="db235-273">Il nuovo `WebGrid` oggetto, insieme ai risultati della query sul database, vengono resi disponibili nel `grid` variabile.</span><span class="sxs-lookup"><span data-stu-id="db235-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="db235-274">È necessario che in un momento per visualizzare i dati nella pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="db235-275">In questa fase, il database è stato aperto, è stato utilizzato i dati desiderati e preparato il `WebGrid` helper con tali dati.</span><span class="sxs-lookup"><span data-stu-id="db235-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="db235-276">Successivamente consiste nel creare il markup della pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="db235-277">**Structured Query Language (SQL)**</span><span class="sxs-lookup"><span data-stu-id="db235-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="db235-278">SQL è un linguaggio che viene utilizzato nella maggior parte dei database relazionali per la gestione dei dati in un database.</span><span class="sxs-lookup"><span data-stu-id="db235-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="db235-279">Include i comandi che consentono di recuperare i dati e l'aggiornamento e che consentono di creare, modificare e gestire i dati nelle tabelle di database.</span><span class="sxs-lookup"><span data-stu-id="db235-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="db235-280">SQL è diverso da un linguaggio di programmazione (ad esempio c#).</span><span class="sxs-lookup"><span data-stu-id="db235-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="db235-281">Con SQL, viene comunicato al database desiderato, ed è il processo del database per scoprire come ottenere i dati o eseguire l'attività.</span><span class="sxs-lookup"><span data-stu-id="db235-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="db235-282">Di seguito sono riportati esempi di alcuni comandi SQL e le operazioni eseguite:</span><span class="sxs-lookup"><span data-stu-id="db235-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="db235-283">Il primo `Select` istruzione Ottiene tutte le colonne (specificato da `*`) dal *filmati* tabella.</span><span class="sxs-lookup"><span data-stu-id="db235-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="db235-284">Il secondo `Select` istruzione recupera le colonne di tipo ID, nome e prezzo dal record di *prodotto* tabella il cui valore della colonna prezzo è maggiore di 10.</span><span class="sxs-lookup"><span data-stu-id="db235-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="db235-285">Il comando restituisce i risultati in ordine alfabetico in base ai valori della colonna nome.</span><span class="sxs-lookup"><span data-stu-id="db235-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="db235-286">Se nessun record corrispondente ai criteri di prezzo, il comando restituisce un set vuoto.</span><span class="sxs-lookup"><span data-stu-id="db235-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="db235-287">Questo comando consente di inserire un nuovo record nel *prodotto* tabella, impostare la colonna nome per il prezzo, la colonna Descrizione "Piacere affidabile A" e "Croissant" 1.99.</span><span class="sxs-lookup"><span data-stu-id="db235-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="db235-288">Si noti che quando si specifica un valore non numerico, il valore è racchiuso tra virgolette singole (non virgolette doppie, come in c#).</span><span class="sxs-lookup"><span data-stu-id="db235-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="db235-289">Utilizzare le virgolette racchiudono i valori di testo o data, ma non per i numeri.</span><span class="sxs-lookup"><span data-stu-id="db235-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="db235-290">Questo comando Elimina i record di *prodotto* tabella la cui colonna Data di scadenza è precedente al 1 gennaio 2008.</span><span class="sxs-lookup"><span data-stu-id="db235-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="db235-291">(Il comando si presuppone che il *prodotto* tabella include una colonna, ovviamente.) La data non viene immesso nel formato MM/GG/AAAA, ma deve essere immesso nel formato utilizzato per le impostazioni locali.</span><span class="sxs-lookup"><span data-stu-id="db235-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="db235-292">Il `Insert` e `Delete` comandi non restituiscono set di risultati.</span><span class="sxs-lookup"><span data-stu-id="db235-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="db235-293">Invece, restituiscono un numero che indica il numero di record è interessato dal comando.</span><span class="sxs-lookup"><span data-stu-id="db235-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="db235-294">Per alcune di queste operazioni (ad esempio, inserimento ed eliminazione di record), il processo che richiede l'operazione deve disporre delle autorizzazioni appropriate nel database.</span><span class="sxs-lookup"><span data-stu-id="db235-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="db235-295">Per tale motivo per il database di produzione è spesso necessario fornire un nome utente e una password quando ci si connette al database.</span><span class="sxs-lookup"><span data-stu-id="db235-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="db235-296">Esistono numerosi comandi SQL, ma seguono un modello, ad esempio i comandi visualizzata qui.</span><span class="sxs-lookup"><span data-stu-id="db235-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="db235-297">È possibile utilizzare i comandi SQL per creare tabelle di database, contare il numero di record in una tabella, calcolare prezzi e molte altre operazioni.</span><span class="sxs-lookup"><span data-stu-id="db235-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>


### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="db235-298">Aggiunta di tag per visualizzare i dati</span><span class="sxs-lookup"><span data-stu-id="db235-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="db235-299">All'interno di `<head>` set contenuto dell'elemento il `<title>` elemento "Filmati":</span><span class="sxs-lookup"><span data-stu-id="db235-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="db235-300">All'interno di `<body>` elemento della pagina, aggiungere quanto segue:</span><span class="sxs-lookup"><span data-stu-id="db235-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="db235-301">È tutto.</span><span class="sxs-lookup"><span data-stu-id="db235-301">That's it.</span></span> <span data-ttu-id="db235-302">Il `grid` variabile è il valore è stato creato al momento della creazione di `WebGrid` oggetto nel codice precedente.</span><span class="sxs-lookup"><span data-stu-id="db235-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="db235-303">Nella visualizzazione albero, WebMatrix destro della pagina e selezionare **Avvia nel browser**.</span><span class="sxs-lookup"><span data-stu-id="db235-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="db235-304">Verrà visualizzato qualcosa di simile a questa pagina:</span><span class="sxs-lookup"><span data-stu-id="db235-304">You'll see something like this page:</span></span>

![Output di supporto WebGrid predefinito dalla tabella di filmati](displaying-data/_static/image18.png)

<span data-ttu-id="db235-306">Fare clic su un collegamento di intestazione di colonna per ordinare in base alla colonna.</span><span class="sxs-lookup"><span data-stu-id="db235-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="db235-307">La possibilità di ordinare facendo clic su un'intestazione è una funzionalità è incorporata il **WebGrid** helper.</span><span class="sxs-lookup"><span data-stu-id="db235-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="db235-308">Il `GetHtml` (metodo), come suggerisce il nome, genera markup che consente di visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="db235-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="db235-309">Per impostazione predefinita, il `GetHtml` metodo genera un elemento HTML `<table>` elemento.</span><span class="sxs-lookup"><span data-stu-id="db235-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="db235-310">(Se si desidera, è possibile verificare il rendering esaminando l'origine della pagina nel browser.)</span><span class="sxs-lookup"><span data-stu-id="db235-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="db235-311">Modifica l'aspetto della griglia</span><span class="sxs-lookup"><span data-stu-id="db235-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="db235-312">Utilizzo di `WebGrid` helper come fatto in precedenza solo è semplice, ma la visualizzazione risultante è normale.</span><span class="sxs-lookup"><span data-stu-id="db235-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="db235-313">Il `WebGrid` helper sono disponibili numerose opzioni che consentono di controllare la modalità di visualizzazione dei dati.</span><span class="sxs-lookup"><span data-stu-id="db235-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="db235-314">Vi sono troppi per esplorare in questa esercitazione, ma in questa sezione verrà farsi un'idea di alcune di queste opzioni.</span><span class="sxs-lookup"><span data-stu-id="db235-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="db235-315">Alcune opzioni aggiuntive verranno descritta nelle esercitazioni successive di questa serie.</span><span class="sxs-lookup"><span data-stu-id="db235-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="db235-316">Specifica di singole colonne da visualizzare</span><span class="sxs-lookup"><span data-stu-id="db235-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="db235-317">Per iniziare, è possibile specificare che si desidera visualizzare solo alcune colonne.</span><span class="sxs-lookup"><span data-stu-id="db235-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="db235-318">Per impostazione predefinita, come si è visto, nella griglia vengono visualizzati tutti e quattro colonne di *filmati* tabella.</span><span class="sxs-lookup"><span data-stu-id="db235-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="db235-319">Nel *Movies.cshtml* file, sostituire il `@grid.GetHtml()` markup appena aggiunto con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="db235-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="db235-320">Per indicare l'helper le colonne da visualizzare, includere un `columns` parametro per il `GetHtml` (metodo) e passare in una raccolta di colonne.</span><span class="sxs-lookup"><span data-stu-id="db235-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="db235-321">Specificare nella raccolta, ogni colonna da includere.</span><span class="sxs-lookup"><span data-stu-id="db235-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="db235-322">Specificare una singola colonna per visualizzare includendo un `grid.Column` , quindi passare il nome della colonna di dati desiderato.</span><span class="sxs-lookup"><span data-stu-id="db235-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="db235-323">(Queste colonne devono essere incluse nei risultati della query SQL, ovvero il `WebGrid` helper non è possibile visualizzare le colonne che non sono state restituite dalla query.)</span><span class="sxs-lookup"><span data-stu-id="db235-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="db235-324">Avviare il *Movies.cshtml* pagina nel browser e questa volta si una visualizzazione simile a quello riportato di seguito (si noti che non esiste una colonna ID viene visualizzata):</span><span class="sxs-lookup"><span data-stu-id="db235-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![Schermata di WebGrid contenente solo le colonne selezionate](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="db235-326">Modificare l'aspetto della griglia</span><span class="sxs-lookup"><span data-stu-id="db235-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="db235-327">Esistono alcune altre opzioni per la visualizzazione delle colonne, alcuni dei quali verrà esaminato nelle esercitazioni successive in questo set.</span><span class="sxs-lookup"><span data-stu-id="db235-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="db235-328">Per il momento in questa sezione verrà presentate modi in cui è possibile applicare uno stile della griglia nel suo complesso.</span><span class="sxs-lookup"><span data-stu-id="db235-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="db235-329">All'interno di `<head>` sezione della pagina, appena prima della chiusura `</head>` tag, aggiungere le seguenti `<style>` elemento:</span><span class="sxs-lookup"><span data-stu-id="db235-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="db235-330">Questo codice CSS definisce classi denominate `grid`, `head`e così via.</span><span class="sxs-lookup"><span data-stu-id="db235-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="db235-331">È anche possibile inserire queste definizioni di stile in un apposito *CSS* file e che crea un collegamento alla pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="db235-332">(In realtà, si eseguirà che più avanti in questa esercitazione set.) Ma per semplificare le cose per questa esercitazione, all'interno della stessa pagina che visualizza i dati.</span><span class="sxs-lookup"><span data-stu-id="db235-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="db235-333">Ora è possibile ottenere il `WebGrid` helper usare queste classi di stile.</span><span class="sxs-lookup"><span data-stu-id="db235-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="db235-334">L'helper è un numero di proprietà (ad esempio, `tableStyle`) per solo a questo scopo, viene assegnato un nome di classe di stile CSS e il nome della classe viene visualizzato come parte del codice che viene eseguito il rendering per il supporto.</span><span class="sxs-lookup"><span data-stu-id="db235-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="db235-335">Modifica il `grid.GetHtml` markup in modo che è ora simile questo codice:</span><span class="sxs-lookup"><span data-stu-id="db235-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="db235-336">Novità in questo campo è di avere aggiunto `tableStyle`, `headerStyle`, e `alternatingRowStyle` parametri per il `GetHtml` metodo.</span><span class="sxs-lookup"><span data-stu-id="db235-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="db235-337">Questi parametri sono stati impostati per i nomi degli stili CSS che è stato aggiunto un istante fa.</span><span class="sxs-lookup"><span data-stu-id="db235-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="db235-338">Eseguire la pagina e questa volta, viene visualizzata una griglia che esegue la ricerca semplice molto meno rispetto a prima:</span><span class="sxs-lookup"><span data-stu-id="db235-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Visualizzazione WebGrid che include i parametri impostati per i nomi delle classi CSS](displaying-data/_static/image20.png)

<span data-ttu-id="db235-340">Per vedere quali il `GetHtml` metodo generato, è possibile esaminare l'origine della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="db235-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="db235-341">Non verrà esaminato in dettaglio di seguito, ma l'aspetto importante è che, specificando i parametri come `tableStyle`, si ha causato la griglia generare i tag HTML simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="db235-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="db235-342">Il `<table>` tag è stato un `class` e viene aggiunto l'attributo che fa riferimento a una delle regole CSS aggiunta in precedenza.</span><span class="sxs-lookup"><span data-stu-id="db235-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="db235-343">Questo codice viene illustrato il modello di base &mdash; diversi parametri per il `GetHtml` consentono di riferimento al metodo CSS classi che il metodo genera quindi con il markup.</span><span class="sxs-lookup"><span data-stu-id="db235-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="db235-344">Operazioni con le classi CSS è responsabilità dell'utente.</span><span class="sxs-lookup"><span data-stu-id="db235-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="db235-345">Aggiunta di paging</span><span class="sxs-lookup"><span data-stu-id="db235-345">Adding Paging</span></span>

<span data-ttu-id="db235-346">Come ultima attività per questa esercitazione, si aggiungerà il paging alla griglia.</span><span class="sxs-lookup"><span data-stu-id="db235-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="db235-347">Ora non è alcun problema per visualizzare tutti i filmati in una sola volta.</span><span class="sxs-lookup"><span data-stu-id="db235-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="db235-348">Ma se è stato aggiunto centinaia di film, visualizzazione della pagina otterrebbe lungo.</span><span class="sxs-lookup"><span data-stu-id="db235-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="db235-349">Nel codice della pagina, modificare la riga che crea il `WebGrid` oggetto per il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="db235-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="db235-350">L'unica differenza da prima è che è stato aggiunto un `rowsPerPage` parametro che è impostato su 3.</span><span class="sxs-lookup"><span data-stu-id="db235-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="db235-351">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="db235-351">Run the page.</span></span> <span data-ttu-id="db235-352">Nella griglia vengono visualizzate 3 righe in un tempo più collegamenti di navigazione che consente di scorrere i film nel database le pagine:</span><span class="sxs-lookup"><span data-stu-id="db235-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Visualizzazione WebGrid con paging](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="db235-354">Esercitazione seguente</span><span class="sxs-lookup"><span data-stu-id="db235-354">Coming Up Next</span></span>

<span data-ttu-id="db235-355">Nella prossima esercitazione, si apprenderà come usare codice Razor e c# per ottenere l'input dell'utente in un form.</span><span class="sxs-lookup"><span data-stu-id="db235-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="db235-356">Si aggiungerà una casella di ricerca per la pagina di filmati in modo che è possibile trovare filmati per titolo o genere.</span><span class="sxs-lookup"><span data-stu-id="db235-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="db235-357">Elenco completo per la pagina di filmati</span><span class="sxs-lookup"><span data-stu-id="db235-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="db235-358">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="db235-358">Additional Resources</span></span>

- [<span data-ttu-id="db235-359">Introduzione alla programmazione Web ASP.NET con sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="db235-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="db235-360">[Precedente](intro-to-web-pages-programming.md)
> [Successivo](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="db235-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
