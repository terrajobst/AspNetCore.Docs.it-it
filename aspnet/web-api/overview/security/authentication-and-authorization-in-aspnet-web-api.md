---
uid: web-api/overview/security/authentication-and-authorization-in-aspnet-web-api
title: Autenticazione e autorizzazione in ASP.NET Web API | Documenti Microsoft
author: MikeWasson
description: Fornisce una panoramica generale di autenticazione e autorizzazione in ASP.NET Web API.
ms.author: aspnetcontent
manager: wpickett
ms.date: 11/27/2012
ms.topic: article
ms.assetid: 6dfb51ea-9f4d-4e70-916c-8ef8344a88d6
ms.technology: dotnet-webapi
ms.prod: .net-framework
msc.legacyurl: /web-api/overview/security/authentication-and-authorization-in-aspnet-web-api
msc.type: authoredcontent
ms.openlocfilehash: 2a4b5ed8a712b061b4afdf5a3adc9378dd72b37f
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/24/2018
---
<a name="authentication-and-authorization-in-aspnet-web-api"></a><span data-ttu-id="dbe51-103">Autenticazione e autorizzazione in ASP.NET Web API</span><span class="sxs-lookup"><span data-stu-id="dbe51-103">Authentication and Authorization in ASP.NET Web API</span></span>
====================
<span data-ttu-id="dbe51-104">da [Mike Wasson](https://github.com/MikeWasson)</span><span class="sxs-lookup"><span data-stu-id="dbe51-104">by [Mike Wasson](https://github.com/MikeWasson)</span></span>

<span data-ttu-id="dbe51-105">Aver creato un'API web, ma ora si desidera controllare l'accesso a esso.</span><span class="sxs-lookup"><span data-stu-id="dbe51-105">You've created a web API, but now you want to control access to it.</span></span> <span data-ttu-id="dbe51-106">In questa serie di articoli, esamineremo alcune opzioni per la protezione di un'API web da utenti non autorizzati.</span><span class="sxs-lookup"><span data-stu-id="dbe51-106">In this series of articles, we'll look at some options for securing a web API from unauthorized users.</span></span> <span data-ttu-id="dbe51-107">Verranno illustrate in questa serie sia l'autenticazione e autorizzazione.</span><span class="sxs-lookup"><span data-stu-id="dbe51-107">This series will cover both authentication and authorization.</span></span>

- <span data-ttu-id="dbe51-108">*Autenticazione* è conoscere l'identità dell'utente.</span><span class="sxs-lookup"><span data-stu-id="dbe51-108">*Authentication* is knowing the identity of the user.</span></span> <span data-ttu-id="dbe51-109">Ad esempio, Alice accede con il nome utente e la password e il server utilizza la password per l'autenticazione di Alice.</span><span class="sxs-lookup"><span data-stu-id="dbe51-109">For example, Alice logs in with her username and password, and the server uses the password to authenticate Alice.</span></span>
- <span data-ttu-id="dbe51-110">*Autorizzazione* deve decidere se un utente è autorizzato a eseguire un'azione.</span><span class="sxs-lookup"><span data-stu-id="dbe51-110">*Authorization* is deciding whether a user is allowed to perform an action.</span></span> <span data-ttu-id="dbe51-111">Ad esempio, Alice dispone dell'autorizzazione per ottenere una risorsa senza creare una risorsa.</span><span class="sxs-lookup"><span data-stu-id="dbe51-111">For example, Alice has permission to get a resource but not create a resource.</span></span>

<span data-ttu-id="dbe51-112">Il primo articolo nella serie offre una panoramica generale di autenticazione e autorizzazione in ASP.NET Web API.</span><span class="sxs-lookup"><span data-stu-id="dbe51-112">The first article in the series gives a general overview of authentication and authorization in ASP.NET Web API.</span></span> <span data-ttu-id="dbe51-113">Altri argomenti vengono descritti scenari comuni di autenticazione per l'API Web.</span><span class="sxs-lookup"><span data-stu-id="dbe51-113">Other topics describe common authentication scenarios for Web API.</span></span>

> [!NOTE]
> <span data-ttu-id="dbe51-114">Grazie a persone esaminato questa serie e forniti suggerimenti utili: Rick Anderson, Levi Broderick, Barry Dorrans, Tom Dykstra, Ge Hongmei, David Matson, Daniel Roth, Tim Teebken.</span><span class="sxs-lookup"><span data-stu-id="dbe51-114">Thanks to the people who reviewed this series and provided valuable feedback: Rick Anderson, Levi Broderick, Barry Dorrans, Tom Dykstra, Hongmei Ge, David Matson, Daniel Roth, Tim Teebken.</span></span>


## <a name="authentication"></a><span data-ttu-id="dbe51-115">Autenticazione</span><span class="sxs-lookup"><span data-stu-id="dbe51-115">Authentication</span></span>

<span data-ttu-id="dbe51-116">API Web si presuppone che l'autenticazione avviene nell'host.</span><span class="sxs-lookup"><span data-stu-id="dbe51-116">Web API assumes that authentication happens in the host.</span></span> <span data-ttu-id="dbe51-117">Per l'hosting web, l'host è IIS che usa i moduli HTTP per l'autenticazione.</span><span class="sxs-lookup"><span data-stu-id="dbe51-117">For web-hosting, the host is IIS, which uses HTTP modules for authentication.</span></span> <span data-ttu-id="dbe51-118">È possibile configurare il progetto per utilizzare uno dei moduli di autenticazione integrati in IIS o ASP.NET o scrivere un modulo HTTP personalizzato per eseguire l'autenticazione personalizzata.</span><span class="sxs-lookup"><span data-stu-id="dbe51-118">You can configure your project to use any of the authentication modules built in to IIS or ASP.NET, or write your own HTTP module to perform custom authentication.</span></span>

<span data-ttu-id="dbe51-119">Quando l'host autentica l'utente, viene creato un *principale*, ovvero un [IPrincipal](https://msdn.microsoft.com/library/System.Security.Principal.IPrincipal.aspx) oggetto che rappresenta il contesto di sicurezza in cui viene eseguito il codice.</span><span class="sxs-lookup"><span data-stu-id="dbe51-119">When the host authenticates the user, it creates a *principal*, which is an [IPrincipal](https://msdn.microsoft.com/library/System.Security.Principal.IPrincipal.aspx) object that represents the security context under which code is running.</span></span> <span data-ttu-id="dbe51-120">L'host collega l'entità per il thread corrente impostando **CurrentPrincipal**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-120">The host attaches the principal to the current thread by setting **Thread.CurrentPrincipal**.</span></span> <span data-ttu-id="dbe51-121">Contiene un oggetto associato all'entità **identità** oggetto che contiene informazioni sull'utente.</span><span class="sxs-lookup"><span data-stu-id="dbe51-121">The principal contains an associated **Identity** object that contains information about the user.</span></span> <span data-ttu-id="dbe51-122">Se l'utente è autenticato, il **Identity.IsAuthenticated** restituisce proprietà **true**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-122">If the user is authenticated, the **Identity.IsAuthenticated** property returns **true**.</span></span> <span data-ttu-id="dbe51-123">Per le richieste anonime, **IsAuthenticated** restituisce **false**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-123">For anonymous requests, **IsAuthenticated** returns **false**.</span></span> <span data-ttu-id="dbe51-124">Per ulteriori informazioni sulle entità, vedere [basata sui ruoli di sicurezza](https://msdn.microsoft.com/library/shz8h065.aspx).</span><span class="sxs-lookup"><span data-stu-id="dbe51-124">For more information about principals, see [Role-Based Security](https://msdn.microsoft.com/library/shz8h065.aspx).</span></span>

### <a name="http-message-handlers-for-authentication"></a><span data-ttu-id="dbe51-125">Gestori di messaggi HTTP per l'autenticazione</span><span class="sxs-lookup"><span data-stu-id="dbe51-125">HTTP Message Handlers for Authentication</span></span>

<span data-ttu-id="dbe51-126">Anziché utilizzare l'host per l'autenticazione, è possibile inserire la logica di autenticazione in un [gestore di messaggi HTTP](../advanced/http-message-handlers.md).</span><span class="sxs-lookup"><span data-stu-id="dbe51-126">Instead of using the host for authentication, you can put authentication logic into an [HTTP message handler](../advanced/http-message-handlers.md).</span></span> <span data-ttu-id="dbe51-127">In tal caso, il gestore di messaggi esamina la richiesta HTTP e imposta l'entità.</span><span class="sxs-lookup"><span data-stu-id="dbe51-127">In that case, the message handler examines the HTTP request and sets the principal.</span></span>

<span data-ttu-id="dbe51-128">Quando è necessario utilizzare gestori di messaggi per l'autenticazione?</span><span class="sxs-lookup"><span data-stu-id="dbe51-128">When should you use message handlers for authentication?</span></span> <span data-ttu-id="dbe51-129">Ecco alcuni svantaggi:</span><span class="sxs-lookup"><span data-stu-id="dbe51-129">Here are some tradeoffs:</span></span>

- <span data-ttu-id="dbe51-130">Un modulo HTTP rileva tutte le richieste che passano attraverso la pipeline ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="dbe51-130">An HTTP module sees all requests that go through the ASP.NET pipeline.</span></span> <span data-ttu-id="dbe51-131">Le richieste indirizzate all'API Web vengono visualizzati solo da un gestore di messaggi.</span><span class="sxs-lookup"><span data-stu-id="dbe51-131">A message handler only sees requests that are routed to Web API.</span></span>
- <span data-ttu-id="dbe51-132">È possibile impostare gestori di messaggi per ogni route, che consente di applicare uno schema di autenticazione a un ciclo specifico.</span><span class="sxs-lookup"><span data-stu-id="dbe51-132">You can set per-route message handlers, which lets you apply an authentication scheme to a specific route.</span></span>
- <span data-ttu-id="dbe51-133">I moduli HTTP sono specifici di IIS.</span><span class="sxs-lookup"><span data-stu-id="dbe51-133">HTTP modules are specific to IIS.</span></span> <span data-ttu-id="dbe51-134">Gestori di messaggi sono indipendente dall'host, pertanto possono essere utilizzati con hosting web e self-hosting.</span><span class="sxs-lookup"><span data-stu-id="dbe51-134">Message handlers are host-agnostic, so they can be used with both web-hosting and self-hosting.</span></span>
- <span data-ttu-id="dbe51-135">I moduli HTTP partecipano registrazione IIS, il controllo e così via.</span><span class="sxs-lookup"><span data-stu-id="dbe51-135">HTTP modules participate in IIS logging, auditing, and so on.</span></span>
- <span data-ttu-id="dbe51-136">I moduli HTTP eseguiti in precedenza nella pipeline.</span><span class="sxs-lookup"><span data-stu-id="dbe51-136">HTTP modules run earlier in the pipeline.</span></span> <span data-ttu-id="dbe51-137">Se si gestisce l'autenticazione in un gestore di messaggi, l'entità non ottenere impostata fino a quando non viene eseguito il gestore.</span><span class="sxs-lookup"><span data-stu-id="dbe51-137">If you handle authentication in a message handler, the principal does not get set until the handler runs.</span></span> <span data-ttu-id="dbe51-138">Inoltre, l'entità, vengono ripristinate l'entità precedente quando la risposta lascia il gestore di messaggi.</span><span class="sxs-lookup"><span data-stu-id="dbe51-138">Moreover, the principal reverts back to the previous principal when the response leaves the message handler.</span></span>

<span data-ttu-id="dbe51-139">In genere, se non è necessario il supporto self-hosting, un modulo HTTP è un'opzione migliore.</span><span class="sxs-lookup"><span data-stu-id="dbe51-139">Generally, if you don't need to support self-hosting, an HTTP module is a better option.</span></span> <span data-ttu-id="dbe51-140">Se è necessario per il supporto self-hosting, prendere in considerazione un gestore di messaggi.</span><span class="sxs-lookup"><span data-stu-id="dbe51-140">If you need to support self-hosting, consider a message handler.</span></span>

### <a name="setting-the-principal"></a><span data-ttu-id="dbe51-141">L'entità di impostazione</span><span class="sxs-lookup"><span data-stu-id="dbe51-141">Setting the Principal</span></span>

<span data-ttu-id="dbe51-142">Se l'applicazione esegua qualsiasi logica di autenticazione personalizzata, è necessario impostare l'entità in due posizioni:</span><span class="sxs-lookup"><span data-stu-id="dbe51-142">If your application performs any custom authentication logic, you must set the principal on two places:</span></span>

- <span data-ttu-id="dbe51-143">**Thread.CurrentPrincipal**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-143">**Thread.CurrentPrincipal**.</span></span> <span data-ttu-id="dbe51-144">Questa proprietà è la modalità standard di set di entità del thread in .NET.</span><span class="sxs-lookup"><span data-stu-id="dbe51-144">This property is the standard way to set the thread's principal in .NET.</span></span>
- <span data-ttu-id="dbe51-145">**HttpContext.Current.User**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-145">**HttpContext.Current.User**.</span></span> <span data-ttu-id="dbe51-146">Questa proprietà è specifica di ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="dbe51-146">This property is specific to ASP.NET.</span></span>

<span data-ttu-id="dbe51-147">Il codice seguente viene illustrato come impostare l'entità:</span><span class="sxs-lookup"><span data-stu-id="dbe51-147">The following code shows how to set the principal:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample1.cs)]

<span data-ttu-id="dbe51-148">Per l'hosting web, è necessario impostare l'entità in entrambe le posizioni; in caso contrario il contesto di sicurezza può diventare incoerente.</span><span class="sxs-lookup"><span data-stu-id="dbe51-148">For web-hosting, you must set the principal in both places; otherwise the security context may become inconsistent.</span></span> <span data-ttu-id="dbe51-149">Per il Self-hosting, tuttavia, **HttpContext. Current** è null.</span><span class="sxs-lookup"><span data-stu-id="dbe51-149">For self-hosting, however, **HttpContext.Current** is null.</span></span> <span data-ttu-id="dbe51-150">Per verificare il codice sia indipendente dall'host, di conseguenza, verificare i valori null prima di assegnare a **HttpContext. Current**, come illustrato.</span><span class="sxs-lookup"><span data-stu-id="dbe51-150">To ensure your code is host-agnostic, therefore, check for null before assigning to **HttpContext.Current**, as shown.</span></span>

## <a name="authorization"></a><span data-ttu-id="dbe51-151">Autorizzazione</span><span class="sxs-lookup"><span data-stu-id="dbe51-151">Authorization</span></span>

<span data-ttu-id="dbe51-152">Si verifica l'autorizzazione in un secondo momento nella pipeline, più vicino al controller.</span><span class="sxs-lookup"><span data-stu-id="dbe51-152">Authorization happens later in the pipeline, closer to the controller.</span></span> <span data-ttu-id="dbe51-153">Che consente di effettuare scelte più granulare, quando si concede l'accesso alle risorse.</span><span class="sxs-lookup"><span data-stu-id="dbe51-153">That lets you make more granular choices when you grant access to resources.</span></span>

- <span data-ttu-id="dbe51-154">*Filtri di autorizzazione* eseguire prima l'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="dbe51-154">*Authorization filters* run before the controller action.</span></span> <span data-ttu-id="dbe51-155">Se la richiesta non è autorizzata, il filtro restituisce una risposta di errore e non viene richiamata l'azione.</span><span class="sxs-lookup"><span data-stu-id="dbe51-155">If the request is not authorized, the filter returns an error response, and the action is not invoked.</span></span>
- <span data-ttu-id="dbe51-156">All'interno di un'azione del controller, è possibile ottenere l'entità corrente dal **ApiController.User** proprietà.</span><span class="sxs-lookup"><span data-stu-id="dbe51-156">Within a controller action, you can get the current principal from the **ApiController.User** property.</span></span> <span data-ttu-id="dbe51-157">Ad esempio, è possibile filtrare un elenco di risorse in base al nome utente, restituisce solo le risorse che appartengono a tale utente.</span><span class="sxs-lookup"><span data-stu-id="dbe51-157">For example, you might filter a list of resources based on the user name, returning only those resources that belong to that user.</span></span>

![](authentication-and-authorization-in-aspnet-web-api/_static/image1.png)

<a id="auth3"></a>
### <a name="using-the-authorize-attribute"></a><span data-ttu-id="dbe51-158">Utilizzando il [autorizzare] attributo</span><span class="sxs-lookup"><span data-stu-id="dbe51-158">Using the [Authorize] Attribute</span></span>

<span data-ttu-id="dbe51-159">API Web fornisce un filtro di autorizzazione predefiniti, [AuthorizeAttribute](https://msdn.microsoft.com/library/system.web.http.authorizeattribute.aspx).</span><span class="sxs-lookup"><span data-stu-id="dbe51-159">Web API provides a built-in authorization filter, [AuthorizeAttribute](https://msdn.microsoft.com/library/system.web.http.authorizeattribute.aspx).</span></span> <span data-ttu-id="dbe51-160">Questo filtro controlla se l'utente è autenticato.</span><span class="sxs-lookup"><span data-stu-id="dbe51-160">This filter checks whether the user is authenticated.</span></span> <span data-ttu-id="dbe51-161">In caso contrario, restituisce il codice di stato HTTP 401 (Unauthorized), senza richiamare l'azione.</span><span class="sxs-lookup"><span data-stu-id="dbe51-161">If not, it returns HTTP status code 401 (Unauthorized), without invoking the action.</span></span>

<span data-ttu-id="dbe51-162">È possibile applicare il filtro a livello globale, a livello di controller o al livello di inidivual azioni.</span><span class="sxs-lookup"><span data-stu-id="dbe51-162">You can apply the filter globally, at the controller level, or at the level of inidivual actions.</span></span>

<span data-ttu-id="dbe51-163">**A livello globale**: per limitare l'accesso per ogni controller API Web, aggiungere il **AuthorizeAttribute** filtro all'elenco di filtri globali:</span><span class="sxs-lookup"><span data-stu-id="dbe51-163">**Globally**: To restrict access for every Web API controller, add the **AuthorizeAttribute** filter to the global filter list:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample2.cs)]

<span data-ttu-id="dbe51-164">**Controller**: per limitare l'accesso per un controller specifico, aggiungere il filtro come attributo per il controller:</span><span class="sxs-lookup"><span data-stu-id="dbe51-164">**Controller**: To restrict access for a specific controller, add the filter as an attribute to the controller:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample3.cs)]

<span data-ttu-id="dbe51-165">**Azione**: per limitare l'accesso per azioni specifiche, aggiungere l'attributo al metodo di azione:</span><span class="sxs-lookup"><span data-stu-id="dbe51-165">**Action**: To restrict access for specific actions, add the attribute to the action method:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample4.cs)]

<span data-ttu-id="dbe51-166">In alternativa, è possibile limitare il controller di e quindi consentire l'accesso anonimo ad azioni specifiche usando il `[AllowAnonymous]` attributo.</span><span class="sxs-lookup"><span data-stu-id="dbe51-166">Alternatively, you can restrict the controller and then allow anonymous access to specific actions, by using the `[AllowAnonymous]` attribute.</span></span> <span data-ttu-id="dbe51-167">Nell'esempio seguente, il `Post` metodo è limitato, ma la `Get` metodo consente l'accesso anonimo.</span><span class="sxs-lookup"><span data-stu-id="dbe51-167">In the following example, the `Post` method is restricted, but the `Get` method allows anonymous access.</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample5.cs)]

<span data-ttu-id="dbe51-168">Negli esempi precedenti, il filtro consente agli utenti autenticati di accedere ai metodi con restrizioni; solo gli utenti anonimi vengono mantenuti. È inoltre possibile limitare l'accesso a utenti specifici o per gli utenti a ruoli specifici:</span><span class="sxs-lookup"><span data-stu-id="dbe51-168">In the previous examples, the filter allows any authenticated user to access the restricted methods; only anonymous users are kept out. You can also limit access to specific users or to users in specific roles:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample6.cs)]

> [!NOTE]
> <span data-ttu-id="dbe51-169">Il **AuthorizeAttribute** filtro per controller API Web si trova nella **System.Web.Http** dello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="dbe51-169">The **AuthorizeAttribute** filter for Web API controllers is located in the **System.Web.Http** namespace.</span></span> <span data-ttu-id="dbe51-170">È presente un filtro per i controller MVC simile di **System** spazio dei nomi, che non è compatibile con i controller API Web.</span><span class="sxs-lookup"><span data-stu-id="dbe51-170">There is a similar filter for MVC controllers in the **System.Web.Mvc** namespace, which is not compatible with Web API controllers.</span></span>


### <a name="custom-authorization-filters"></a><span data-ttu-id="dbe51-171">Filtri di autorizzazione personalizzato</span><span class="sxs-lookup"><span data-stu-id="dbe51-171">Custom Authorization Filters</span></span>

<span data-ttu-id="dbe51-172">Per scrivere un filtro di autorizzazione personalizzato, derivare da uno dei seguenti tipi:</span><span class="sxs-lookup"><span data-stu-id="dbe51-172">To write a custom authorization filter, derive from one of these types:</span></span>

- <span data-ttu-id="dbe51-173">**AuthorizeAttribute**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-173">**AuthorizeAttribute**.</span></span> <span data-ttu-id="dbe51-174">Estendere questa classe per eseguire la logica di autorizzazione in base all'utente corrente e i ruoli dell'utente.</span><span class="sxs-lookup"><span data-stu-id="dbe51-174">Extend this class to perform authorization logic based on the current user and the user's roles.</span></span>
- <span data-ttu-id="dbe51-175">**AuthorizationFilterAttribute**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-175">**AuthorizationFilterAttribute**.</span></span> <span data-ttu-id="dbe51-176">Estendere questa classe per eseguire la logica di autorizzazione sincrone che non è necessariamente basata su utente corrente o del ruolo.</span><span class="sxs-lookup"><span data-stu-id="dbe51-176">Extend this class to perform synchronous authorization logic that is not necessarily based on the current user or role.</span></span>
- <span data-ttu-id="dbe51-177">**IAuthorizationFilter**.</span><span class="sxs-lookup"><span data-stu-id="dbe51-177">**IAuthorizationFilter**.</span></span> <span data-ttu-id="dbe51-178">Implementare questa interfaccia per eseguire la logica asincrona autorizzazione; ad esempio, se la logica di autorizzazione effettua chiamate asincrone dei / o o di rete.</span><span class="sxs-lookup"><span data-stu-id="dbe51-178">Implement this interface to perform asynchronous authorization logic; for example, if your authorization logic makes asynchronous I/O or network calls.</span></span> <span data-ttu-id="dbe51-179">(Se la logica di autorizzazione è associato alla CPU, è più semplice da cui derivare **AuthorizationFilterAttribute**, perché è necessario scrivere un metodo asincrono.)</span><span class="sxs-lookup"><span data-stu-id="dbe51-179">(If your authorization logic is CPU-bound, it is simpler to derive from **AuthorizationFilterAttribute**, because then you don't need to write an asynchronous method.)</span></span>

<span data-ttu-id="dbe51-180">Il diagramma seguente mostra la gerarchia di classi per la **AuthorizeAttribute** classe.</span><span class="sxs-lookup"><span data-stu-id="dbe51-180">The following diagram shows the class hierarchy for the **AuthorizeAttribute** class.</span></span>

![](authentication-and-authorization-in-aspnet-web-api/_static/image2.png)

### <a name="authorization-inside-a-controller-action"></a><span data-ttu-id="dbe51-181">Autorizzazione all'interno di un'azione del Controller</span><span class="sxs-lookup"><span data-stu-id="dbe51-181">Authorization Inside a Controller Action</span></span>

<span data-ttu-id="dbe51-182">In alcuni casi, è possibile consentire a una richiesta per proseguire, ma modificare il comportamento in base all'entità.</span><span class="sxs-lookup"><span data-stu-id="dbe51-182">In some cases, you might allow a request to proceed, but change the behavior based on the principal.</span></span> <span data-ttu-id="dbe51-183">Ad esempio, le informazioni che restituiscono potrebbero cambiare a seconda del ruolo dell'utente.</span><span class="sxs-lookup"><span data-stu-id="dbe51-183">For example, the information that you return might change depending on the user's role.</span></span> <span data-ttu-id="dbe51-184">All'interno di un metodo del controller, è possibile ottenere il principio corrente dal **ApiController.User** proprietà.</span><span class="sxs-lookup"><span data-stu-id="dbe51-184">Within a controller method, you can get the current principle from the **ApiController.User** property.</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample7.cs)]
